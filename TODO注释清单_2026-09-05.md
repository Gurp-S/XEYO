# XEYO 源码 TODO 注释清单

> 收集时间：2026-09-05。范围：`python/`、`gui/src`、`cli-ts/src`（已排除 node_modules/dist/__pycache__）。
> 说明：`gui/src` 与 `cli-ts/src` 中匹配到的 "todo" 均为代码标识符（如 `TodoList` 组件、`todos` 数组），真正的 TODO 注释全部集中在 python 工具层，共 **24 条**，且内容已全部是中文。

## python/tools/file_read_tool/file_read_tool.py
- L5 `# TODO: [媒体] 图片 / PDF 原生读取与多模态输出（.ipynb 已返回 cell 摘要）`
- L6 `# TODO: [去重] GrowthBook killswitch / 更精细的 unchanged stub 策略`
- L7 `# TODO: [token] API 级 token 计数（现用 chars/4 粗估）`
- L8 `# TODO: [UI] 专用终端渲染 / Hooks / 遥测`

## python/tools/file_edit_tool/file_edit_tool.py
- L5 `# TODO: [历史] fileHistory 备份 / structuredPatch 展示`
- L6 `# TODO: [集成] VSCode diff / settings 文件特殊校验`
- L7 `# TODO: [UI] 专用终端渲染 / Hooks / 遥测`

## python/tools/file_write_tool/file_write_tool.py
- L5 `# TODO: [历史] fileHistory 备份 / 结构化 patch 展示`
- L6 `# TODO: [集成] VSCode diff 通知`
- L7 `# TODO: [密钥] team-memory secret 扫描`
- L8 `# TODO: [UI] 专用终端渲染 / Hooks / 遥测`

## python/tools/grep_tool/grep_tool.py
- L5 `# TODO: [权限] preparePermissionMatcher：按 pattern 做 shell 通配规则匹配`
- L6 `# TODO: [ignore] 接入权限 ignore 列表（gitignore / 用户 deny 路径 / 插件缓存排除等）`
- L7 `# TODO: [校验] 用严格 schema（如 pydantic/zod 等价物）做 input/output 运行时校验`

## python/tools/glob_tool/glob_tool.py
- L16 `# TODO: [权限] preparePermissionMatcher：按 pattern 做 shell 通配规则匹配`
- L17 `# TODO: [路径] 绝对路径 pattern 拆分 baseDir + relativePattern（对齐 extractGlobBaseDirectory）`
- L18 `# TODO: [取消] abort 时杀掉已运行的 rg 子进程（现在只在 execute 前后检查 abort）`
- L19 `# TODO: [校验] 用严格 schema（如 pydantic/zod 等价物）做 input/output 运行时校验`
- L20 `# TODO: [UI] 专用终端渲染 / Hooks / 遥测接入（现仅 glob_stats() + metadata）`

## python/tools/bash_tool/bash_tool.py
- L6 `# TODO: [后台] 超时自动转后台、与 TaskOutput 打通`
- L7 `# TODO: [sed] _simulatedSedEdit 预览写盘`
- L8 `# TODO: [取消] 后台任务 abort/kill`
- L9 `# TODO: [测试] 语义 exit、截断、Windows 编码、复合命令`

## python/engine/query_engine.py
- L166 `# TODO: 后续实现 permission_denials / total_usage / read_file_state`

## 备注
- glob_tool.py L17 的"对齐 extractGlobBaseDirectory"指内部函数名，非外部产品引用，TODO 行保留原样。
- 上述 TODO 均未删除（按你的要求仅收集）；如需一并清理请告知。
