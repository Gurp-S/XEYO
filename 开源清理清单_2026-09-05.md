# 开源前清理清单（2026-09-05）

> ⚠️ **本文件本身是内部审计产物，发布仓库前必须删除/移出，绝不能随仓库公开。**
> 状态：只读盘点，**未删除/未修改任何文件**。所有"删除"均为建议，由你逐项确认后自行执行。
> 方法：`git ls-files`（1769 个跟踪文件）+ 磁盘核对 + 密钥模式扫描（sk-/AKIA/ghp/xox/AIza + 通用赋值模式）+ 历史删除文件名检查 + 未跟踪清单（60 项）。

---

## 一、🚫 必须移出仓库（敏感/内部，开源硬阻断）

| # | 文件 | 状态 | 理由 |
|---|------|------|------|
| 1 | `.workbuddy/memory/MEMORY.md` | **已被 git 跟踪**（commit ca24d84） | 内含 AI 助手工作记忆：审计结论、事故细节、内部约定。已在历史里 |
| 2 | `XEYO_七层架构审计_2026-09-04.md` | 磁盘未跟踪（100KB） | 本轮审计报告：完整暴露全部安全缺口与利用面，绝不可公开 |
| 3 | `python/.xeyo_session_index.json` | **被跟踪** | 内部会话 ID 索引（本地运行数据） |
| 4 | `XEYO-merged-plan.md` | **被跟踪** | 内部合并计划文档 |
| 5 | `docs/`（133 个跟踪文件，其中 124 个为 `docs/设计`、`docs/实施计划` 等中文内部设计文档） | **被跟踪** | **需你决策**：①整体移出开源仓库；②精选公开（删除"实施计划/事故复盘"类，保留架构设计）；③全部公开。历史里同样存在，若不公开需配合"新仓库发布"（见第四节） |
| 6 | `.agents/`（54 个跟踪 skill 文件） | **被跟踪** | 检查来源：若从市场/他人仓库复制，需核对许可证与署名；若是自研可保留 |
| 7 | `AGENTS.md`、`XEYO.md`、`python/ARCHITECTURE.md`、`python/XEYO.md` | **被跟踪** | 内部工程规约与架构速记。**需你决策**：开源社区通常欢迎此类文件，但请通读一遍确认无内网路径/人名/内部系统名 |

## 二、🗑️ 垃圾与产物（建议删除，均可再生或无价值）

**已被 git 跟踪（需 `git rm`）：**
| 文件 | 说明 |
|------|------|
| `python/.cgraph_v7/index.sqlite3` | **32MB** 二进制索引，可由 cgraph 重建（`incremental_update`）——仓库最大文件，必删 |
| `0` | 根目录零字节杂讯 |
| `count_chars.py`、`fizzbuzz_class.py`、`fizzbuzz_generator.py`、`fizzbuzz_list.py` | 练手/临时脚本 |
| `_pytest_detached.py`、`_run_batch.py`、`_verify_remaining.py`、`run_p0b_tail_batches.py` | 会话期临时驱动脚本 |
| `python/_full_args.json`、`python/_ns_args.json`、`python/c2_ab_pressure.json`、`python/c2_ab_results.json`、`python/c2_benefit.json` | 实验输出数据 |
| `assets/pet_XY/xeyo-sea-sprite-plus-bubbles-final.zip`（10MB） | 设计源文件包——**决策**：保留（资产）或移到 release 附件 |
| `gui/context_island/`（68 个文件 ≈25MB） | 设计管线中间产物（input/cleaned/traced/upscale/result）；`gui/public/` 里的最终版已够用——**决策**：全删中间态或保留一张代表图 |

**仅磁盘存在（未被跟踪，直接删）：**
- 调试转储：`_dsh_audit.js/.json`、`_dsh_edits_*.json/txt`、`_dsh_*.js/txt`（12 个，~350KB）、`_orphan_scan.js`
- 损坏现场与临时目录：`.git.broken-20260905/`、`.tmp/`、`.xy-trash/`、`.archify/`、`.baseline-wt/`、`.workbuddy/*.tmp`、`interactive`、`gui/.test_write_probe`、`java/AnotherTest.java`
- pytest/offload 本地数据：`python/.pytest-tmp/`、`python/.pytest_tmp/`、`python/.pytest_tmp_sys/`、`python/.xeyo_offload/`、`python/.xeyo_offload_tmp/`
- python/ 根日志与杂项：`python/_b9.log`、`python/_b10.log`、`python/_full.*`、`python/_ns*`、`python/_p4*`、`python/_test_out.txt`、`backend*.log`、`history-fix-server.*.log`、`check-memory-index-p0.bat` 等若未跟踪
- `docs/image/亲手冒烟测试/*.png`（5 个未跟踪截图）、`docs/scr1.png`、`docs/_a3_check.js`

**工作流脚本（决策：精简为 1-2 个入口，其余删）：**
`A3_daily_monitor.bat`、`C2_gray_off/on.bat`、`check-batch234.bat`、`check-p1-t-now.bat`、`check-peer-topics-toolization.bat`、`preview-t-now.bat`、`run-gui-check-*.bat`、`run-locomo.bat`、`run-memory-eval.bat`、`run-pytest-baseline/p0a/p0b*.bat`、`start-remote-tunnel.bat`、`v61_gate_test.bat`、`XEYO-bench.bat`、`一键测试.cmd`、`python/scripts/_chrome_shot.bat`
——这些是内部 P0 门/实验流程（内容无密钥，`start-remote-tunnel.bat` 从 `.env` 读 token，干净），但 24 个 bat 对开源用户是噪音。建议保留 `XEYO.bat`、`XEYO-CLI.bat`、`start-tauri-dev.bat` + 一个统一测试入口。

**决策项：`python/scripts/`（46 个跟踪文件）**
一次性实验/AB 基准脚本。两难：它们是"数据准入"证据链的组成部分（审计评价为亮点），但对 outsider 是噪音。建议：保留 `v61_evidence_gate.py`、`kv_cache_probe.py` 等有代表性的 5-10 个 + README 说明，其余删。

## 三、⛔ 千万别删（未提交的真实源码——先 `git add`！）

以下 **30 个未跟踪文件是真实功能源码**（插件系统、offload 系统），只是从未 commit。开源清理时最容易误删：

```
python/extension/hooks.py  plugin_fetcher.py  plugin_store.py
python/server/plugin_market.py  server/routers/plugins.py
python/memory/offload.py  python/tools/offload_read_tool.py
python/scripts/offload_read_eval.py  pointer_retrieve_eval.py
python/scripts/synth_extract_generative.py  synth_l3_eval.py  synth_llm_summary_eval.py
python/tests/plugin/  python/tests/test_plugins_api.py
gui/e2e/helpers/uiScan.ts  gui/e2e/ui-audit.spec.ts  gui/playwright.ui-audit.config.ts
```

另有 **13 个已修改未提交的跟踪文件**（`goal_state.py`、`chat.py`、`sessions.py`、`rewind.py`、`job_registry.py` 等）——按 AGENTS 提交门处理：跑完测试后 commit，或明确丢弃。**发布前工作树必须干净。**

## 四、发布流程建议（按序执行，本次未执行）

1. **补 LICENSE**（根目录无任何 LICENSE 文件——开源硬前置）。
2. **决策第五节的 docs/AGENTS 去留**，然后：
3. **增补 .gitignore**：`*.sqlite3`、`.cgraph_v7/`、`.workbuddy/`、`.pytest*tmp*/`、`.xeyo_offload*/`、`.git.broken-*/`、`.tmp/`、`.xy-trash/`、`_dsh_*`、`interactive`、`gui/.test_write_probe`、`*.tmp`。
4. **处理第二节**：`git rm` 跟踪垃圾 + 删磁盘垃圾；`git rm --cached .workbuddy/` 并确认 docs 决策。
5. **提交第三节**的未跟踪源码与在途修改。
6. **密钥核验**：本次已验证——跟踪文件中 sk- 命中均为测试占位符（`sk-abcdefghijklmnopqrstuv`）；无 AKIA/ghp/xox/AIza 命中；`.env` 已 ignore 且未跟踪；历史删除文件名无 credential 类。仍建议发布前跑一次 `gitleaks detect`（pack 仅 56MiB，秒级）扫全历史内容级泄漏（截图/文档内嵌 token 无法用文件名扫描覆盖——`docs/` 若公开请人工翻一遍截图）。
7. **历史策略（关键决策）**：`.workbuddy/MEMORY.md`、`docs/设计` 等已进历史（如 ca24d84）。两选一：
   - **A（推荐）**：新仓库单 squash 提交发布——彻底摆脱历史，最干净；
   - **B**：保留历史 → 必须先完成第 6 步 gitleaks 全历史零告警 + docs 决策为"全部公开"，且接受历史中一切内部痕迹。
8. **README**：已有 7KB README，补：开源定位、架构图（可引用审计文档 §二 的全局 Mermaid 重绘）、构建/测试入口、LICENSE、致谢。

## 五、一句话总账

- **仓库健康度不错**：.gitignore 已覆盖大头，无跟踪的 node_modules/pycache，无真实密钥入库，pack 仅 56MB。
- **三颗雷**：①32MB 的 `.cgraph_v7/index.sqlite3` 被跟踪；②`.workbuddy/MEMORY.md`（审计记忆）被跟踪且在历史里；③30 个真实源码文件还没 commit——清理时**最危险的动作是误删它们**。
- **两个必补**：LICENSE、工作树清干净（13 个在途修改 + 30 个未跟踪源码先落定）。

---

## 六、全部 `.md` 文件分类清单（补充节，2026-09-05）

共 **163 个被跟踪 + 6 个未跟踪**。逐类处置：

### A. 保留（对外有价值）
| 文件 | 说明 |
|------|------|
| `README.md`（根） | 开源主文档，保留并可扩充 |
| `cli-ts/README.md`、`gui/e2e/README.md`、`gui/design-assets/README.md` | 子目录说明，保留 |
| `gui/src/bench/BASELINE.md` | 基准记录，保留（查无内部敏感信息后） |
| `gui/ARCHITECTURE.md`、`gui/01-entry-interaction-layer.md` | GUI 架构说明，**建议保留**（同 AGENTS 决策口径） |

### B. 删除（内部/垃圾）
| 文件 | 理由 |
|------|------|
| `XEYO-merged-plan.md`（根，被跟踪） | 内部合并计划，`git rm` |
| `.workbuddy/memory/MEMORY.md`（被跟踪）+ `.workbuddy/memory/2026-09-04.md`、`2026-09-05.md`（未跟踪） | AI 工作记忆：含审计结论/事故细节，**历史里也有**→ 需新仓库方案兜底 |
| `XEYO_七层架构审计_2026-09-04.md`（未跟踪） | 本轮审计报告，发布前删 |
| `开源清理清单_2026-09-05.md`（未跟踪，本文件） | 自身也是内部产物，发布前删 |
| `python/.pytest-tmp/**/session.md` | pytest 临时产物，随目录删 |
| `gui/context_island/processing-findings.md`、`gui/context_island/final/README.md` | 随 `context_island/` 中间产物目录一起处置（见第二节） |
| `docs/_a3_check.js`（非 md，顺带） | 调试残留 |

### C. 决策项（内部规约与设计文档）
| 文件组 | 数量 | 说明 |
|--------|------|------|
| `docs/`（`设计/` + `实施计划/` 等） | 89 | 内部设计文档主体（含刚写的 `docs/设计/47-记忆系统全景机制.md` 未跟踪）。三选一：整体不公开 / 精选公开（保留架构类，删实施计划与事故类）/ 全公开。**若不公开且保留历史，历史里仍有** → 与第四节"新仓库发布"联动 |
| `AGENTS.md`（根） | 1 | 内部工程规约（提交门/反巨石/T_now 纪律）。开源社区通常欢迎，保留前通读查内部人名/系统名 |
| `XEYO.md`（根）、`python/XEYO.md`、`python/ARCHITECTURE.md` | 3 | 引擎速记/架构。同上口径，建议保留（是项目亮点） |
| `.agents/skills/**` | 54 | **第三方技能副本**（million-js、react-doctor、tailwind 等来自市场）。两点：①与产品无关，建议整目录移出开源仓库；②若保留必须核对每个技能的原许可证 |
| `assets/pet_XY/Xeyo Sea Sprite Plus.md` | 1 | 随宠物资产去留（若 zip 删则一并删） |
| `scripts/*.md` | 4 | 一性期脚本附带说明，随 scripts 决策 |

### D. 小结
- **无争议动作**：`git rm` XEYO-merged-plan.md + .workbuddy/MEMORY.md；磁盘删 2 个审计/清单 md 与 pytest tmp。
- **一条原则**：所有"决策保留"的 md，发布前用 `grep -riE '内网|http://[0-9]+\.|C:\\\\Users|48522|真实姓名'` 过一遍路径/人名/IP 泄漏。
- 未跟踪的 `docs/设计/47-*.md` 若 docs 决策保留则记得 `git add`（它是正文级设计文档）。

---

## 七、企业级结构改造蓝图（只读评估，2026-09-05）

> 目标：发布后的仓库结构达到一线开源项目（如 vs code / fastapi / tauri）的观感与工程标准。本节全部为**建议**，未执行任何改动。

### 7.1 现状评分（10 维）

| 维度 | 现状 | 企业级标准 | 差距 |
|------|------|-----------|------|
| 开源门面 | README ✔ / LICENSE ✖ / CONTRIBUTING ✖ / SECURITY.md ✖ / CODEOWNERS ✖ / CHANGELOG ✖ / notice ✖ | 全套齐 | **大**：缺 6 个门面文件 |
| 仓库卫生 | 根目录 100+ 项：24 个 bat、fizzbuzz/练手脚本、12 个 `_dsh_*` 调试转储、7 个运行时输出目录（artifacts/logs/screenshots/stream-previews/usage-previews/workspace-layouts/.xeyo_*） | 根目录 ≤15 项、全部有明确职责 | **大** |
| 跨平台一致性 | **无 .gitattributes / .editorconfig**；重度 Windows 依赖（Job Object/pwsh） | `.gitattributes` 强制 LF+二进制标注；`.editorconfig` | 中（且曾发生过 CRLF/中文路径事故） |
| Lint/Format | Python **无 ruff/mypy**；GUI **无 eslint/prettier**（只有 typecheck+vitest） | ruff+pytest marker / eslint+prettier | 中 |
| CI | 3 jobs（pytest非live+manifest drift / gui typecheck+vitest / cli-ts typecheck），**仅 ubuntu** | +lint job、+Windows matrix（本项目核心行为在 Windows！）、+gitleaks、+构建产物 job | 中 |
| 打包元数据 | root package.json（monorepo 编排 ✔）、pyproject（setuptools/console-script ✔）、`.env.example`+`bash_rules.example.json`+`.xeyo-policy.example.json` ✔ | 同 | **小**（底子好） |
| Monorepo 命名 | root=xeyo、gui=**xeyo**（撞名）、cli-ts=**@xeyo/cli** ✔、pyproject=xeyo | `@xeyo/gui`、`@xeyo/engine` 等命名空间统一 | 小 |
| 测试组织 | 268 文件/45k 行、conftest+bootstrap ✔、markers(live/slow/channel) ✔ | +`p0` marker 单一权威（四轮已建议） | 小 |
| 文档体系 | docs/ = 89 个中文内部文档（设计/实施计划/任务书/交接/交付存档/验收/参考调研），含与其他产品（Codex/DSH）对比报告 | 公开 docs/（英文路径）+ 内部文档出库 | **大**（决策+重组） |
| 遗留物 | `java/`（被跟踪的 xeyo.rpc JsonRpcServer 子项目，**六轮审计均未覆盖**）、`cli/`（遗留 2 个 tsx，被 cli-ts 取代）、`gui/src-tauri/icons/henry.w@example.net` 垃圾文件被跟踪 | 无未说明的子项目 | 中（java 需决策：用→补审计+入 CI；不用→移出） |

### 7.2 目标根目录（After 蓝图）

```
xeyo/
├── README.md  LICENSE  CONTRIBUTING.md  SECURITY.md  CHANGELOG.md  CODEOWNERS
├── .github/            # workflows(ci+release+gitleaks) + ISSUE_TEMPLATE + pull_request_template
├── .editorconfig  .gitattributes  .gitignore  .env.example  .xeyo-policy.example.json
├── package.json        # monorepo 编排（现有，扩展 release 脚本）
├── pnpm-workspace.yaml # 或 npm workspaces，统一三个前端的依赖提升
├── apps/
│   ├── gui/            # 现 gui/（Tauri 壳）
│   └── cli/            # 现 cli-ts/
├── engine/             # 现 python/（pyproject 就位，仅移动）
├── docs/               # 仅公开文档：architecture/ guides/ operations/（英文文件名）
├── scripts/            # 保留的 1-2 个入口 bat + 统一测试入口
└── assets/
```

> 说明：`apps//engine/` 分层是"可选项"——若不想动 1769 个文件的路径（IDE/脚本/引用成本），**保平铺 `gui/ cli-ts/ python/` 也可接受**（tauri/vs code 也是平铺）。硬性项只有：根目录清场、`java/`+`cli/` 决策、docs 重组。

### 7.3 P0 门面补齐清单（每项 5-30 分钟）

1. `LICENSE`（先定协议：MIT/Apache-2.0——含 GUI 资产与第三方 skill 时 Apache-2.0 附 NOTICE 更稳）
2. `CONTRIBUTING.md`（提交门：tsc+vitest+pytest、分支/PR 流程、中文/英文自定）
3. `SECURITY.md`（漏洞上报邮箱；结合审计：明确"本机单用户设计，非多租户安全边界"的声明很有价值）
4. `CODEOWNERS` + `.github/ISSUE_TEMPLATE/`（bug/feature 两模板）+ `pull_request_template.md`
5. `.gitattributes`：`* text=auto eol=lf`、`*.png/*.jpg/*.ico/*.webp/*.zip/*.sqlite3 binary`、`*.bat eol=crlf`（曾出过 CRLF/编码事故，此项是刚需）
6. `.editorconfig`（root=true；py=4空格+utf8；ts=2空格；bat 需 crlf）
7. `CHANGELOG.md`（Keep a Changelog 格式，从 0.1.0 起记）
8. `python/pyproject.toml` 补 `[project] classifiers/license/readme` + `[tool.ruff]`（line-length 与现有 4 空格+tab 风格对齐需先跑一轮 format 决策）+ `[tool.pytest.ini_options] markers += "p0"`

### 7.4 工具链与 CI 升级（P1）

1. **Python**：引入 ruff（lint+format 二合一），首跑 `ruff format` 会大改 → 单独一个 PR；mypy 可先 `宽松模式` 只覆盖 `engine/`；`p0` marker 落地后 8+ 个 bat 的清单统一为 `-m p0`。
2. **GUI/CLI**：补 eslint(+react hooks 规则) + prettier；`gui/package.json` name 改 `@xeyo/gui`。
3. **CI 增 job**：`lint`（ruff+eslint）、`gitleaks`（`gitleaks/gitleaks-action`，同步兑现清理清单第 6 步）、**`windows` matrix**（pytest -m p0 on windows-latest——本项目的 Job Object/pwsh/bash 策略在 ubuntu 上测不到，这是当前 CI 最大的盲区）、release job（tag 触发 tauri build + py wheel）。
4. **版本单一来源**：目前 0.1.0 写死在 root/gui/cli-ts/pyproject 四处 → 统一从 root package.json 读取或引入 release-please。

### 7.5 文档重组（P1，联动第五节决策）

- **公开层**：`docs/architecture/`（从 python/ARCHITECTURE.md、gui/ARCHITECTURE.md 扩写）、`docs/guides/`（安装/首跑/斜杠命令/扩展开发）、`docs/operations/`（权限策略、审计、rewind 运维）。文件名**英文**（中文文件名在 CI/部分 git 托管上有编码风险，内容可中文）。
- **内部层出库**：`交接/ 交付存档/ 任务书/ 验收/ 实施计划/ 参考调研/ 总纲与路线/` 及 Codex/DSH 对比报告 → 私有仓库或本地归档，不入开源库。
- README 补三件套：一句话定位 + 全局架构图（审计文档 §二 Mermaid 可直接用）+ Quick Start（XEYO.bat / XEYO-CLI.bat / `py -3.11 -m cli serve`）。

### 7.6 迁移顺序（与第五节清理流程合并执行）

```
①清场（第二节清单）→ ②门面八件套（7.3）→ ③未跟踪源码 git add + 在途修改落定
→ ④docs 决策与重组（7.5）→ ⑤工具链 PR（ruff/eslint/p0 marker，7.4.1-2）
→ ⑥CI 升级（7.4.3）→ ⑦gitleaks 零告警 → ⑧新仓库 squash 单提交发布 → ⑨打 v0.1.0 tag + CHANGELOG
```

> **总评**：底子好于预期——monorepo 编排、pyproject、example 三件套、conftest、CI 骨架都已存在；离企业级的距离主要在**门面 8 缺 6、根目录卫生、docs 双层化、Windows CI 盲区**四件事，全部是"补文件+挪目录"级工作量（不含 ruff 首轮 format 的代码噪声），预估 2-3 个工作日可完成 ①-⑨。
