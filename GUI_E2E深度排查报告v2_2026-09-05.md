# XEYO GUI 深度 E2E 排查报告 v2（只读探针，未改产品代码）

日期：2026-09-05 下午 ｜ 前置：上一轮报告 `GUI_E2E排查报告_2026-09-05.md`（已修复 rewind.spec locator + default 配置 testIgnore，验证 4/4 过）。

## 一、方式

自写 Playwright 探针（`gui/test-results/probe/probe2.cjs`，不入库、自管服务生命周期：隔离后端 8178 + vite 5176 + 临时工作区/隔离 HOME，结束即 taskkill 清理）。11 个场景覆盖上一轮 spec 未触及的功能面，全程抓取 console.error / pageerror / 全部 /v1 4xx。

## 二、结果：11/11 场景通过，抓到 4 个真实信号

### 真实问题（新增，均为低危）

1. **未绑工作区的会话反复请求 rewind 端点 → 400**（每次跑 7~9 条）
   - `GET /v1/sessions/{id}/rewind` → 400 `"session has no workspace"`。
   - 来源：`rewindV3Store.ts:940 loadPills()`（切会话/加载时拉 pill 数据源），错误被 947 行静默吞掉。
   - 影响：纯噪音+浪费请求，功能不坏；但建议前端对无 workspace 会话跳过，或后端改 200 空列表。
2. **空会话可点「分叉会话」→ 后端 404**
   - `POST /v1/sessions/{id}/fork` → 404 `"source session has no transcript"`；前端 toast「分叉失败」兜底（Sidebar.tsx:486）。
   - UX 建议：空 transcript 会话的菜单项置灰。低优先。
3. **ModelPicker aria 不一致**：trigger `aria-haspopup="listbox"` 但面板无 `role="listbox"`（ModelPicker.tsx）。a11y 小瑕疵。
4. **观察项**：一次 `502 GET /v1/workspace/git/log?limit=500`（`backend_unreachable`，vite 代理瞬时够不到后端；后端前后均存活）。单次出现，建议后续观察是否与后端单线程处理 git log 阻塞相关。

### 功能确认清单（本轮全过、无问题）

主题切换（data-scheme light→dark）；设置面板 6 tab 全部有控件（外观 switch=7 含记忆开关/模型账号/权限/回溯/桌宠/远程·微信 radio=2）；运行时预设三选项+授权空态；用量/扩展中心互斥与开关；审批模式三选；快捷菜单 Plan/Ask/Multi-Agent/MCP + Plan chip + MCP 面板 + 斜杠建议；ModelPicker 触发；沉浸模式（时钟/右板默认开、Ctrl+B 开合、Esc 逐层）；会话新建/重命名对话框/归档（归档行默认隐藏，需勾「已归档」过滤——设计如此）/恢复；工作区右侧 tab（文件夹根目录/审查/git/终端/浏览器）+ 终端回显；发送 fake 回声；用量详情弹窗 + compact 按钮（C2 gate=1）；/bench/chat（DEV）与非法 /side/ 重定向；FileMenu 全菜单 + 浏览器模式 Exit toast；Esc 分层；Ctrl+N。**全程 0 pageerror**，console 错误全部由上述 4xx 解释。

### 排除的假警报（探针侧）

- 沉浸「Ctrl+B 无效」：右板默认开，Ctrl+B 是关闭；断言语义写反。
- 「归档后行数 0」：归档视图需漏斗菜单勾选「已归档」才显示，设计如此。
- 「重命名无输入框」：promptDialog 用命令式 DOM（xy-id-input），非 role=dialog 结构。
- 首轮 v1 探针 G/H/L 失败：vite 进程被会话回收中途杀死所致；v2 自管生命周期后消失。

## 三、环境坑（对后续测试有用）

1. Git Bash 后台任务里跑 vite 会被回收 → 探针必须自管 spawn + 结束 taskkill /T /F。
2. Node 22 Windows spawn `.cmd` 必须 `shell: true`（否则 EINVAL）。
3. 侧栏会话行三点钮默认 `invisible pointer-events-none`，Playwright 必须 hover 父行再点。
4. 会话行挂在「工作区」分组下且分组默认折叠；store 直调 openFolder 不会展开分组（UI 包装层才会）。

## 四、结论

深度面（设置/用量/插件/沉浸/会话管理/工作区/路由/快捷键）**未发现功能性回归**；新增 2 个低危产品问题（rewind 400 轮询、空会话分叉 404）、1 个 a11y 瑕疵、1 个观察项。上一轮修复保持稳定（rewind 复跑见附注）。
