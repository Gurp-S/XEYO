# XEYO 优化功能收益测试报告（2026-09-05）

> 约束遵守：**零源码改动**。仅新增 2 个文件（`python/tests/test_opt_mechanism_benefit.py` 微基准测试 7 用例、`python/scripts/_audit_bench_codeindex_now.py` 复测脚本）+ 运行仓库既有的收益基准/评测脚本 + 只读聚合生产账本。
> 方法分层：①生产账本真实数据 ②既有离线基准 ③新增微基准 ④历史 AB 结果 ⑤live 实测（受阻项如实标注）。

---

## 一、总表：全部优化项 × 有/无对比 × 收益

| # | 优化机制 | 对比方法 | 「有 vs 无」数据 | 结论 |
|---|---------|---------|-----------------|------|
| 1 | **KV 稳定前缀**（system memo / schemas 冻结 / env 尾插 / C0-C1 字节稳定） | 生产账本校准事件 4198 枪（observe→calibration 闭环的真实观测） | **真实 KV 命中率 median 93.9%**（mean 82.9%，p75 97.2%），累计命中 **1.02 亿 tokens**；按命中价≈未命中 1/10 估算，输入费用省 **~75%（mean）~84%（median）** | ★★★★★ 收益旗舰，生产数据确凿 |
| 2 | **C2 压缩**（确定性摘要+逃生舱） | 历史 simulator AB（200 轮 605 调用会话重放） | frozen/adaptive/p078/p090 四配置 hit_proxy 全部 **0.980-0.985**、tail_overflow=0、改写 8-11 次摊薄；生产 c2_events 1068 次/36 会话（真实会话单会话最多 6 次） | ★★★★ 长会话不撑爆窗口且前缀稳定 |
| 3 | **输出精简三挡**（Bash stdout 精简） | `bench_phase1_benefit.py`（离线，before/after） | pytest 输出 **1126→78 tok（省 93.1%）**、git_log 79.9%、tsc 90.1%；注入开销仅 75-86 tok | ★★★★★ 投入产出比最高 |
| 4 | **Read pack / git touched**（读打包+符号触达） | 同上 | Read 调用 **3→1 次**、token 省 **29.5%**；git touched 省 **69.8%**、命中目标 ✔ | ★★★★ |
| 5 | **codeindex symbols**（符号定位读） | 新增复测脚本（原脚本因硬编码旧符号崩溃） | `query_loop.py`（411 行）symbol 读 **20615→175 tok（省 99.2%）**、1.8ms vs 32.3ms；重名符号歧义引导内置 | ★★★★★ |
| 6 | **输出预算 + spill** | 新增微基准 | 50k 字符工具输出 → 进入上下文 ~8k（预览），全文落盘 `.xeyo_spill/` 可 Read 找回 | ★★★★ 上下文防火墙 |
| 7 | **schemas 冻结缓存** | 新增微基准 | 首建 vs 缓存命中（200 次平均）显著加速，缓存命中路径零重序列化 | ★★★（KV 前提件） |
| 8 | **γ4 不可信围栏** | 新增微基准 | 单次 <1ms，`untrusted="true"` 标注稳定 | 安全开销可忽略 |
| 9 | **T_now 类感知预算** | 新增微基准 | 30 块 inventory 超量 → 裁剪到 6000 token 预算内，directive 全保 | ★★★ |
| 10 | **Bash 透明路由** | `analyze_bash_routing.py`（416 次真实调用审计 + 82 会话重放）+ 微基准 | 误用率 3.85%（16 次），**L2 纠偏有效率 68.8%**；分类判定 <0.2ms 零负担 | ★★★ |
| 11 | **v61 A4 sqlite 索引** | `v61_adopted_benefits.py`（300 笔记 90 查询） | 墙钟 **6.60→5.59s（-15.2%）**，热轮 0 次文件解析，命中数与基线**逐位一致** | ★★★ |
| 12 | **v61 A5 session.md 差分重写** | 同上 | 稳定型（真实长任务常态）delta **80%**；回滚从"整删失忆"→按 turn 精确截断；单条损坏不毁历史 | ★★★ |
| 13 | **v61 A2/A3 压缩可还原**（retrieve 逃生舱） | 同上 | 最新栈字节级找回 ✔、旧栈折叠但 retrieve 可回；极小预算下路径 3 条全保 | ★★★ |
| 14 | **v61 A6 源健康门** | 同上 | 病态源（循环副本）拒绝=True、健康源放行=True | 防污染闸 |
| 15 | **v61 A1 ω 冷却平滑** | 同上（挂机 4h 场景 27 状态扫描） | 本场景收益 **0**（决策无右移、零回归护栏 ✔） | 诚实结论：当前场景无收益 |
| 16 | **v61 B1/B2/B3 条件采纳**（s_i/θ 重校准/动态 R） | `v61_evidence_gate.py`（门限裁决） | **全部"不采纳"**：保栈靠全 keep 换来，成本超 +2% 门限 | 证据门有效否决，防过度工程 |
| 17 | **46 号侧挂**（trigram 缓存/污染门/报告口径/冷记忆） | `bench_cursor_benefit.py` | content_index 二建 **309.8→46.7ms（-85%）**重算 300→0；污染环境拦 ✔；4 字段报告口径 ✔ | ★★★ |
| 18 | **全工具性能基线** | `audit_tool_perf.py` | Read symbol 1.0ms/257tok vs 全量 3505tok；Grep content 18.8ms/738tok（cap 20000）；Bash 系亚秒 | 基线留档 |
| 19 | **老化 aging** | `aging_canary.py` | ⚠️ **脚本损坏**：写死的 `XEYO_C2_GATE` 开关已从注册表删除（ValueError）——待修脚本后补测 | 待补 |
| 20 | **live KV probe / formula_live_ab / memory_stack_eval / bench_output_compact** | live 实测 | ⚠️ **DeepSeek 402 Insufficient Balance**（欠费），全部受阻；账本 cache_hit 全 0 恰好印证审计发现（主客户端未开 include_usage） | 待充值后补测 |

---

## 二、分节详述（关键数据）

### 2.1 KV 稳定前缀——生产环境真实收益（最有说服力的一项）

数据源：`~/.xeyo/usage/calibration_events.jsonl`（memory/observe 逐枪记录 predicted vs observed，喂给 simulator 校准 ρ̂）。

- 样本：**4198 枪**（有效 3783），横跨 deepseek-v4-flash(-vision)/glm-4.5-air/glm-5.3-flash/glm-5 等模型，共 5094 次真实请求、账面 41.5 元。
- **真实命中 tokens / prompt tokens：median 93.9%，mean 82.9%，p75 97.2%**。
- 累计命中 **101,960,445 tokens**。按 DeepSeek 缓存命中价 ≈ 未命中 1/10 保守估算：输入费用节省 **~75%（mean 口径）~84%（median 口径）**。
- 反向印证：主账本 `events.jsonl` 的 cache_hit 全为 0——因为主客户端没开 `stream_options.include_usage`（正是 `kv_cache_probe.py` 存在的理由，也对应六轮审计 model 层"用量归因缺口"发现）。

### 2.2 C2 压缩：历史 simulator AB（c2_benefit.json / c2_ab_results.json / c2_ab_pressure.json）

200 轮、605 次模型调用的合成重放会话四配置对照：frozen（11 次推进）/adaptive（9）/p078（8）/p090（8），前缀命中代理 0.980→0.985，尾溢出全部为 0；单次改写体量 117k-161k 字符被 12000 字符确定性摘要替换（**原始约 1.29M 字符 → 摘要 8×~14k-16k**）。生产侧 `c2_events.jsonl`：36 个会话累计 1068 次 C2（压力测试会话占 954，真实会话单会话最多 6 次）。
1M 窗口压力单选优（c2_ab_1m.py）运行中，结果将落盘 `.diag_memory_cost/_audit_c2ab1m_out.txt`。

### 2.3 输出精简三挡（bench_phase1_benefit.py）

```
1) Bash stdout   pytest  1126→78 tok  省 93.1%   | git_log 79.9% | tsc 90.1%
2) Read pack     调用 3→1，468→330 tok，省 29.5%，accuracy_ok=True
3) Git touched   调用 3→1，106→32 tok，省 69.8%，hit_target=True
4) 注入开销      lite 75 tok / full 86 tok / ultra 86 tok（budget 6000 内）
```

### 2.4 codeindex/symbols（新增复测，原脚本崩溃原因记录）

原 `bench_codeindex_benefit.py` 硬编码 `rollbackSlice.ts` 的 `continueAndRevert` 符号——该符号在当前代码中已不存在，脚本 `StopIteration` 崩溃（脚本维护债：无参数化、目标会过期）。新增 `scripts/_audit_bench_codeindex_now.py` 用运行时动态选符号复刻同口径：

```
Read 全量 vs symbol（python/engine/query_loop.py，411 行）
  全量  32.3ms / 20615 tok
  symbol(_content_parts)  1.8ms / 175 tok   → token 节省 99.2%
Grep content vs symbols   同任务返回体积 1.0x（symbols 的价值在"定位定义"而非"省返回"）
符号歧义防护              唯一符号一次命中 ✔，重名引导内置
```

### 2.5 新增微基准（`python/tests/test_opt_mechanism_benefit.py`，7/7 通过，0.51s）

- **输出预算+spill**：50k 字符工具输出 → 上下文仅 ~8k 预览，`.xeyo_spill/audit/....txt` 找回路径直接写在返回内容里（fail-open：spill 失败不丢内容已由既有单测覆盖）。
- **schemas 冻结缓存**：首建 vs 200 次缓存命中，命中路径零重序列化（KV 前缀的地基件）。
- **γ4 围栏**：单次 <1ms，`untrusted="true"` 注入稳定。
- **T_now 类感知预算**：30 块 inventory 超量输入 → 裁剪进 6000 token 预算，directive 全保。
- **Bash 透明路由**：`_classify` 单次 <0.2ms（路由本身零负担）。
- **bash 策略裁决吞吐**：evaluate_policy_impl 单次 <20ms（实测亚毫秒级）——安全层零感知成本。

### 2.6 v61 证据门：六项采纳 + 三项否决（工程诚实样本）

```
采纳（A1-A6）：A2/A3 压缩可还原 ✔ | A4 索引 -15.2% 墙钟 | A5 差分 80%/可回放 ✔ | A6 源健康门 ✔ | A1 ω（零收益但零回归，护栏价值）
否决（B1-B3）：栈存活达标但靠"整窗 keep"换来 → 成本超 +2% 门限 → 全部不采纳
```

### 2.7 受阻与待办（如实记录）

| 项 | 受阻原因 | 补测路径 |
|---|---------|---------|
| live KV probe / formula_live_ab / memory_stack_eval / bench_output_compact / hitrate_live | DeepSeek 402 欠费 | 充值后各跑一轮（命令已在脚本 docstring） |
| aging 关/开对比 | `aging_canary.py` 写死已删除的 `XEYO_C2_GATE` 开关 → ValueError | 修脚本开关名（需改源码，本次禁改）或授权后代修 |
| bench_codeindex_benefit | 硬编码旧符号崩溃 | 已用新增脚本复刻替代；根治需参数化（禁改） |
| 1M 窗口压力单选优 | 运行中（离线模拟器重负载） | 结果落盘 `.diag_memory_cost/_audit_c2ab1m_out.txt` |

---

## 三、总结论

1. **收益最确凿的三个优化**：KV 稳定前缀（median 93.9% 真实命中率 → 输入费省约八成）、Bash/Read 输出精简（79-93% token 节省，注入开销 <90 tok）、codeindex symbol 定位读（99.2% token 节省）。
2. **机制收益到位且附带安全/可回放红利**：spill 防火墙、γ4 围栏、T_now 预算、session.md 差分回滚——这些的收益形态是"防失控"而非"省 token"，微基准证明其开销可忽略。
3. **证据门在真实工作**：B1/B2/B3 三项被自己的门限否决——这套"数据准入"纪律不是摆设（对应六轮审计的 A- 评级）。
4. **两处脚本维护债**（aging_canary 开关漂移、codeindex bench 硬编码符号）恰好印证了审计"prompt/常量双写漂移"的模式——修复需改源码，本次严格遵守未动。
5. 新增文件（本次唯一增量）：`python/tests/test_opt_mechanism_benefit.py`（7 用例全绿，可进 CI）、`python/scripts/_audit_bench_codeindex_now.py`、本报告、`.diag_memory_cost/_audit_kv_probe.json`（402 错误现场）。
