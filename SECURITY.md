# Security Policy

## 支持范围（Supported Versions）

XEYO 处于开源早期（v0.1 前后）。当前仅维护最新发布版与仓库 `main` 分支。

| 版本 | 支持 |
|---|---|
| main（未发布开发线） | ✅ |
| 已发布 tag | ✅（最近一个） |
| 更早版本 | ❌ |

## 上报漏洞（Reporting a Vulnerability）

请**不要**在公开 Issue 中披露安全漏洞。优先私信上报：

- 通过仓库维护者的安全联系渠道（发布后于仓库 About 区公布）；
- 或向维护者邮箱发送加密描述（发布前暂以仓库 Discussion 私密分类接收）。

请包含：

1. 影响的组件与版本（后端引擎 / GUI / tui / MCP 扩展层）；
2. 复现步骤（尽量最小化）；
3. 影响评估（如：是否可越权访问文件、是否可注入、是否可导致密钥泄漏）。

**响应承诺**：确认后 7 天内给出首次答复与修复计划；修复后发布安全公告。

## 已知安全边界（本仓库设计约束）

- **本机优先模型**：`/v1` API 默认 `require_loopback`（仅本机客户端可访问），不要在生产网络暴露；
- **权限沙箱**：工作区外路径默认 deny；Bash 读命令透明路由到专用工具（`bash_routing`）；
- **MCP/SKILL 扩展默认关闭**：需 `<workspace>/.xeyo/settings.json` 显式启用；
- **密钥管理**：`.env` 不入库，`api_key.txt` 已入 `.gitignore`，API Key 建议经应用内设置面板持久化；
- 依赖第三方 agent 技能（发布前会从公开树移除，见归档目录）需自行核验许可证与来源。

> 若发现 `.env`、密钥、内部文档被误提交到公开仓库，请立即私信上报并按发布阶段流程轮换密钥。
