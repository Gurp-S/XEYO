# XEYO GUI 全量 E2E 排查报告（只读，未改任何代码）

日期：2026-09-05 ｜ 方式：项目自带 Playwright 套件 × 7，全部使用临时隔离目录（`os.tmpdir()`），未触碰仓库与真实会话数据。

## 一、结果矩阵

| 套件（配置） | 用例数 | 结果 | 说明 |
|---|---|---|---|
| main-path（default，fake 后端） | 2 | ✅ 全过 | 发送气泡上屏+流式落定、停止按钮中断 |
| rewind.spec（default） | 4 | ⚠️ 2 过 2 败 | 失败为**测试 locator 命中隐藏 DOM 副本**，产品功能经取证正常（见 §2.1） |
| rewind-fullstack（全栈 mock_llm） | 1 | ✅ 过 | Write→Restore 删除→Undo 写回 全链路 |
| fullstack（全栈 mock_llm） | 3 | ✅ 2 过 1 skip（并行跑时曾败，干净重跑通过，见 §2.2） | 审批面板允许/Esc 取消、Bash 工具卡、compact 按钮 |
| extensions（专用配置） | 1 | ✅ 过 | 扩展中心：三 tab→MCP 搜索→技能→互斥→返回对话→Manage 跳转 |
| ui-audit（5174 纯前端） | 1 | ✅ 过 | 窗口×侧栏矩阵几何错位扫描 |
| ui-audit-full（5175 纯前端） | 4 | ✅ 全过 | 主页几何+样式矩阵、交互遍历、bench 流式回放卡顿、Ctrl+K 探针 |

## 二、发现的问题（按定性排序）

### 2.1 【稳定复现·测试问题为主】rewind 两个用例失败：隐藏 DOM 副本干扰 locator
- 失败点：`rewind.spec.ts:115`（`getByText('hello v2').first()`）与 `:176`（reload 后 `getByText('hello').first()`），元素解析成功但 `hidden`，20s 超时。**串行干净环境仍 100% 复现（3 次）**。
- 取证（截图 + aria 快照 + API 断言）证明产品行为正常：
  - 「回溯完成」弹窗正常（撤销回溯/完成按钮齐全）；
  - 编辑后文案与 `ok: hello v2` 回复**已可见上屏**（aria 树里有 `paragraph: "ok: hello v2"`）；
  - 服务端事件流断言全部通过（continue 事件 committed、pill_summary.edited_digest、removed_rows、after_message_id 均正确），刷新后截断持久化正确。
- 根因：DOM 中存在**隐藏的重复 `.xy-chat-text` 副本**，`.first()` 按 DOM 序命中隐藏副本。隐藏副本类名含 `whitespace-pre-wrap`，与 `gui/src/components/sticky/stickyTypes.ts:137`（sticky 钉住层，#1 sticky 修复引入）完全吻合，疑似 sticky pin/测量副本。
- 定性：**测试可探测性问题**（spec 88-89 行注释已自认此坑，但 115/176 两处断言仍用了裸 `getByText().first()`）。附带一个产品侧测试性坏味道：sticky 层往 DOM 里塞隐藏文本副本。
- 建议方向（未改）：这两处断言改用「编辑这条消息」按钮过滤（同 spec 内 91 行的既有力法）或加 `.locator('visible=true')`；长期可让 sticky 副本加 `data-testid` / `aria-hidden` 供测试排除。

### 2.2 【偶发·环境】fullstack T3 审批面板未出现
- 仅在三个套件并行抢 CPU 时发生（审批面板 20s 未出现）；清理并行负载后干净重跑 2/2 通过。
- 建议：e2e 套件不要并行跑（configs 的 workers=1 只约束套件内，不约束跨套件并行）。

### 2.3 【测试基建】default 配置未排除跨配置 spec，产生一批误报
- `playwright.config.ts` 的 `testIgnore` 只排除 `fullstack/rewind-fullstack`，导致 `extensions.spec / ui-audit*.spec` 被卷进 default 跑：ui-audit-full 在 60s 超时（设计 480s）、extensions 无专用 seed → 一次报告 5 failed，全部为跨配置误伤（各自专用配置下全过）。
- 建议：default `testIgnore` 补上 `**/extensions.spec.ts`、`**/ui-audit*.spec.ts`。

### 2.4 【环境坑】Playwright Windows 下 webServer 子进程泄漏
- 多次跑批后 vite dev server 残留占用 5173/5175/5177，导致后续套件报 "port already used"。需手动 `taskkill` 清理（本次已清）。

## 三、通过面（无问题）
发送/停止主路径、回溯弹窗开关、回溯第二轮后继续发送不卡死、文件检查点 Restore/Undo、扩展中心全流程、几何/样式矩阵、交互遍历、流式回放性能、Ctrl+K 探针。

## 四、未覆盖（现有 spec 之外，如需可另立专项）
Tauri 原生壳交互（窗口拖拽/原生菜单）、T29 断流横幅（设计上 skip）、设置面板细项（Bash 路由/记忆开关/运行时预设）、Usage 图表、RemoteQr/RemoteSse、WorkspaceRevertDialog、SelectionToolbar 等深水区组件。

## 五、结论
**未发现用户可感知的功能性回归。** 1 个测试基建问题（default 配置漏 ignore）、1 个稳定失败（测试 locator 设计缺陷，产品正常）、1 个偶发（并行资源竞争）、1 个环境坑（进程泄漏）。
