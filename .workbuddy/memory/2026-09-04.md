# 2026-09-04 工作日志

## XEYO 七层架构审计
- 产出：`XEYO_七层架构审计_2026-09-04.md`（仓库根目录），按用户给定 7 层（决策/认知/能力/控制/容错/安全/装配）审计 `python/` 引擎源码。
- 方法：每层一个并行 deep-read 子代理（只读 .py/json，禁读 docs/*.md），关键高影响论断经 Grep/Read 抽查复核后写入。文档含 Mermaid 图、逐层理解/优点/不足/证据行号、跨层专题、P0-P2 路线图。
- 主要结论（留存备用）：
  - 决策层：无反思/验证闭环；plan.py 仅人工审批闸；goal 是被动候选；RepeatCallGuard 只提醒不拦截。
  - 认知层：检索纯词法无自动召回；技能(skill_tool)只读、无经验习得回路；memindex edges 表是死代码；nightshift/蒸馏晋升链路健全。
  - 能力层：`mcp__`直挂与 Mcp 网关双执行路径；slash 与工具体系双真相；tools 冻结红线被 MCP `list_changed` 全代替换（清 `_schemas_cache`, tool_registry.py:104）触碰。
  - 控制层：Scheduler(DAG, run_task_batch) 生产未接线；多 agent 协程 gather 伪并行；无墙钟超时/优先级/全局配额；WorkspaceLock 整工作区单 owner。
  - 容错层：WriteStore(子 agent 写路径)不进 rewind journal（record_file_mutation 仅 3 个文件工具调用）；incomplete_tool_uses 只写不读；JSONL 元数据无 GC；shadow_git 已上线（workspace_restore/workspace_revision 在用）。
  - 安全层：裁决子层成熟（DENY 先于 grant、指纹 v2 不含参数）；沙箱纸面化（subprocess 无 OS 隔离）；Bash grant 前缀 token 注入放大；审计 JSONL 无签名/无告警；默认档 risk 非默认拒绝。
  - 装配层：T_now 硬顶现为 22 且仅测试执法；max_tokens 默认 None；env_channel 假对不进 transcript（可观测性缺口）；legacy 回退档说话人混淆残留。
- 交叉模式：①同名异义+双份状态机多发；②"已实现未接线"多发（scheduler/incomplete_tool_uses/blob_gc）；③补偿覆盖面按写入口划分而非数据面。

## 第二轮补遗（22:45，并入同一审计文档 §5）
- rewind 悬案定论：在线主路径 v3=RewindHotpath（app.py:278 挂载，/sessions/{sid}/rewind 与 slash /revert）；v2 RollbackService 保留为 /rollback/* 降级端点，内存对齐契约不同（drop_engine vs resync_after_rewind）。
- 新增高危：server workspace/audit/memory 路由门禁仅 2/20 端点覆盖（PUT/DELETE file 裸奔）；workspace_fs HTTP 直写绕过权限/rewind/审计三链路；channels 无发送者白名单（任意微信用户可远程 /allow）+出站无脱敏；model 层 httpx 流异常不归一（半截消息丢失不可重试）+超时分类盲区；context_limit 断链致 CLI 路径 C2 三触发点全哑；fence 不转义 </tool_output> 可被击穿；bash 换行绕过只读白名单（_COMPOSITE_RX 无 \n）；AskUserQuestion 全程无审计；CLI 单键 a/y 放行无断流恢复；回滚后台 restore 与新写入无互锁；Tauri frame-src 全开 + shell:allow-open 任意路径。
- 修正一轮误记：ENV_FALLBACK_STATUS={400,404,405,413,415,422}（结构类 4xx 声道回退），402/429/5xx 是 retryable 重试而非回退；token 权威口径=ceil(utf8字节/4)（memory/token.py:11-15），C2 经济门误用字符/4。
- 亮点补充：goal_round_driver 合成轮三重护栏（32 轮上限+共享唤醒预算 3+人工恢复）；subagent 写经 WriteStore 注入（base_hashes 乐观锁）；hydrate 修复链 fail-closed。

## 第三轮补遗（23:40，同一文档新增 §6）
- 覆盖：tools 具体实现（glob/grep 基于 rg、bash 透明路由 dup_redirect+tool_registry._route_bash、spill 在用、WebFetch SSRF 门完整）、memory 剩余（蒸馏独立成 note 无合并/失败 candidate 永生、memory.journal=多 agent 变更流水非 rewind、10 开关+5 个 env 影子键）、codeindex(symbols/graph/cgraph v7 证据门原型)、usage 账本（DeepSeekModelClient 零账本引用=账本黑洞）、评测设施（evals 六维+4 反作弊 shadow+simulator 离线推演+v61_evidence_gate——"数据准入"纪律真实存在，评级 B+）、python/cli 三模式（in-process/attach/serve）。
- 三轮新增高危（均经直读核实）：HTTP Bearer=透传供应商 key 无服务端凭据（deps.py:63→chat.py:632），serve 可绑 0.0.0.0；record_transcript 仅 flush 无 fsync（:148）+rotate/append 竞态；rwlock 取消泄漏窗口可饿死写者。
- 正面结论：工具错误信息对模型友好度、输出预算三层统一、"先过证据门才准接工具面"纪律（cgraph 实例）是全仓亮点。
- 审计至此覆盖完毕（引擎七层+server/model/channels/shell/tools/评测/CLI），文档共 25 项排序缺口 + 三档路线图。

## 第四轮收尾（00:14，新增 §7，缺口增至 29 项）
- server 全路由门禁矩阵完成：media/usage/audit/memory 全裸（usage 是开放 SSRF 代理面，X-Base-Url 任意）；local_gate 白名单含 "testclient"（:12）+ XEYO_ALLOW_REMOTE_CONTROL 一键全开（:23）；门禁三态混杂（路由级 Depends/逐端点/没有）是漏网根因；mcp /op approve 一次调用写 trust。
- 测试执法核实（268 文件/45k 行）：真执法=指纹v2/DENY>grant/write_scope/远程收紧/freeze(push-pull)/reconcile/rewind/bash路由/budget；缺口=提交门不存在（无 pre-commit/husky）、xfail 全仓 0 处（deselect 绕过）、P0 清单碎片化 8+ bat、T_now 运行时零校验、list_changed 冻结无测试锁、scheduler 死路径 50+ 用例护驾。
- Tauri Rust（14 个 command 无 ACL）：wait_health 只查 "200"/"ok" 字符串（lib.rs:580）→ 本机 8000 端口劫持可接管 GUI 全部凭据流量；fs/git command 无路径边界（abs_path 仅查绝对路径）；端口文件可诱导 taskkill /T /F 任意进程树；permission pending 全局单槽并发串台；apiKey 明文 localStorage。
- 工具清尾：NotebookEdit 缺 write_scope 拒写兜底（file_edit 有，notebook 零命中，已核实）= 多 agent 写隔离唯一旁路；修正三轮误判——主会话写工具也注入 WriteStore（session_pool.py:500），即全部结构化写共用一个不进 rewind 的缓冲，补偿链缺口比原估更大；Memory update/forget 同步重写索引冻结事件循环；file_edit 的冲突归因（行区间+跨会话 owner_of）是亮点。
- 四轮总计：21 个精读代理，29 项排序缺口，路线图 P0+/P0/P1/P2 四档。

## 第五轮收尾（00:46，新增 §8，缺口增至 32 项，覆盖率 100%）
- 覆盖最后盲区：ilink 密码学（AES-128-ECB 无完整性校验、150 行手写 AES 兜底、无请求签名、bot_token 明文+无 TTL——keyring 迁移 <1h、seen 去重子串启发确定性误杀用户引用 bot 原文+明文留存 840 条对话）、插件供应链（P0：loader.py:92 用 manifest 自声明 source_type 判定信任——GitHub 插件写 "local" 即免批，已直读核实；name=".." 安装逃逸；信任键不绑内容哈希）、GUI 注入面（主聊天 rehype 白名单管道安全；最大面是 browser 预览 iframe：URL 无校验+allow-same-origin+frame-src data:/blob:）、bash 执行器（**修正**：win_job.py:163-171 Job Object 内存限额 512MiB+树终止真实存在="资源护栏有实体，隔离面纸面化"；后台晋升 job 零超时+绕过 rewind）。
- 全仓 346 个运行时 py 文件核对完毕，无实质遗漏（剩 prompt.py 文本级琐碎）。
- 五轮总计：24 个精读代理，32 项排序缺口，P0+ 共 18 项（0a-0r），审计收官。

## 第六轮查漏（01:08，新增 §9-10，缺口 35 项，27 个代理收官）
- prompt 文本 22 个 ≈3.3-4K tokens，抽查无"声称≠实现"漂移（bash prompt 诚实未声称沙箱）。
- evals+simulator 深挖评级 B+→A-：三大疑虑排除（常量单点/decide 同实现 runtime.py:1423/ρ̂ 校准闭环真实）；三硬伤=items.jsonl 跨模型混染（bfcl_lite.py:393-397 无 model 过滤，已核实）、反作弊模块零接线、retrieval_bench 自证。
- GUI 剩余：fill 模式 diff 无虚拟化、extractDiffFence 只取首个围栏、todo error 渲染成成功快照、命令面板搜索切全局 workspace、WorkspaceImg 缓存无界、CLI http_api 未 urlencode+cmd_rm 无确认。
- 正确性小项：todo restore.py:82-102 打断时优先级含混（直读核实，按"语义含混"措辞）；memdir touch_last_used 读热路径写放大；ilink qr_png 自研 QR version≤6 容量 ~106B 登录 URL 会抛错。
- 六轮收官：覆盖声明见文档 §9.4；后续工作=按 §10 路线图修 P0+（0a server 门禁、0o 插件信任最优先）。
