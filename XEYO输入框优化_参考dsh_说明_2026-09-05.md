# XEYO GUI 输入框（命令行 / Skill）优化说明 —— 参考 DeepSeek Harness 源码

> 2026-09-05 · 只读分析，未改动 XEYO 任何代码。
> dsh 源码版本：deepseek-ai/deepseek-harness（开发者预览版，稀疏克隆于 `D:/lea/dsh-src`，MIT）。

---

## 一、结论摘要

XEYO 的 Composer 在上一轮改造后已经具备 dsh 的**形**：光标处 slash 词元检测（`slashTokenAt`）、中途输入可唤起弹层、技能+命令双分组、着色覆盖层、slash 执行网关。但与 dsh 相比还缺**三块骨**：

1. **弹层没有键盘仲裁**——↑↓/Tab/Enter 全部不接管，纯鼠标操作；且 Enter 提交没有 IME 组词守卫（潜在 bug）。
2. **技能直呼靠客户端改写提示词**（`[slash:/name] 请用 Skill 工具加载技能…`），依赖模型自觉调 Skill 工具；dsh 是**宿主 pre-step 确定性注入**渲染后的技能正文，零依赖模型自觉，且是 `disable-model-invocation` 技能的唯一入口。**这是收益最大的一条**。
3. **没有 `@` 引用触发源**——文件/会话引用走「+ 菜单 + 拖拽」，输入框内无 `@` 弹层（dsh 的 `@` 与 `/` 复用同一条流水线，几乎是免费的）。

---

## 二、dsh 是怎么做的（源码导读）

### 2.1 统一触发流水线：`ui-input-trigger`

核心文件：`packages/client/ui-input-trigger/src/client/controller.ts`

- 抽象极简：`InputTriggerSource { trigger: '/' | '@', name, order?, candidates, onPick, matchSpace?, matchEnter? }`。**流水线对「命令」零知识**——命令、技能、@引用都是注册进来的 source，谁实现 match 钩子谁参与裁决。
- 每会话一个 `InputTriggerController`：持唯一权威 hit（含 span + draftRev）、菜单状态机、候选 fetch generation（新键击自动作废旧请求）。
- **combobox 键盘仲裁**（`arbitrate()`）：弹层开着时焦点始终留在编辑器表面，↑↓移动高亮、Enter 选中、Tab 选定或下钻（drill 进目录）、Esc 关闭；**所有仲裁都有 IME composition 守卫，唯一例外 Shift+Enter 无条件放行**。
- 候选按 `query` 过滤但候选 fetch 是异步的，`status: 'ready'` 之前 Enter/Tab 一律「消费但不选中」（不选过期行、也不漏透成发送）。
- 加号按钮（launcher）与键入 `/` 走**同一个 MenuView、同一条 onPick 链**（`toggleSource`），不存在第二套菜单组件。

### 2.2 输入状态机与提交事务（`ui-conversation/input`）

权威笔记：`.agents/notes/implemented/architecture/2026-07-25-web-input-machine-and-slash-pipeline.zh.md`

- 四相状态机：`plain / adjudicating / claimed / submitting`。
- **命令态永远不从 draft 文本推导**，由 pick 路径显式建立 claim；claim 由 `draft.startsWith(token)` 看护，退格破坏自动释放。claim 可带 `hint` 显示 ghost text。
- 提交是事务：SubmitAttempt（seq + AbortSignal）防晚到回灌；失败回填带**漂移守卫**——回车时快照的草稿仅当 live draft 仍等于它才回填，用户已再输入则只发通知、绝不覆盖。
- 跨插件输入改写收敛为三个 scoped bail 事件（`input-begin-command` / `input-insert-reference` / `input-insert-text`），返回 true ⟺ 真的改写了（Space 是否 preventDefault 以返回值为准）。

### 2.3 技能引用（`ui-skill`）：纯文本 + 宿主注入

`packages/client/ui-skill/src/client/index.ts`：

- 技能目录按会话**单飞 RPC + 缓存**，scope 出生即预热（warm）；per-keystroke 过滤只打本地已 settle 的快照，一个会话一次 RPC。
- **pick 不插入任何结构化 chip**——直接落纯文本 `/name `，发送即原文（"plain-text-reference decision"）。
- 宿主侧（`packages/skill/tool-skill/src/index.ts` 的 `agent/pre-step`）扫描 claimed 用户消息首行的 `/name` 词元：命中 user-invocable 技能 → **把渲染后的 SKILL.md 正文作为注入消息追加到本轮上下文**（背景在前、任务材料最后）；查不到的名字保持普通散文。
- 关键推论：**技能触发不依赖模型自觉**，也不占模型一次「决定调用 Skill 工具」的往返；`disable-model-invocation` 技能（不给模型看目录）只能从这条宿主通道进。
- 命令命名空间优先：`/name` 若命中宿主命令，在客户端 adjudication 阶段就被 claim 掉，**不会**变成 prompt 再到宿主侧撞名。

### 2.4 `@` 引用（`ui-reference`）与装饰

- `@` 触发源并行查 **文件** 和 **会话** 两路候选；目录下钻有面包屑（crumb），`@` + 引号是另一种语法形态（quoted）。
- 词边界规则冻结成纯核：行首/空白后的 `/name`、`@name` 才命中；`user@host`、URL `/`、`/nfs-hg/xxx` 永不误触发；`/name` 必须止于空白或行尾。
- 名录（lexicon）是同步热快照：`undefined` = 未热 → 零装饰、永不触发 fetch；配 `subscribeLexicon` 失效通道，目录 settle 后已有草稿里的 token 直接点亮，无需任何菜单交互。
- 已发送气泡侧只在该步骤真的注入过该技能时才把 `/name` 装饰成技能 chip；随手敲的 `/词` 保持普通文本。

### 2.5 编辑器底座

2026-08-20 笔记：textarea 栈（U+FFFC 占位 + occurrence 表 + 自管 undo log）被实测成本推翻，**翻案为 Lexical 文档树**——TextRefNode 实体、共享 scrollport、undo 归 Lexical history。（XEYO 短期不必跟进，textarea + 着色 overlay 够用；但 chip/多色词元做复杂后应考虑。）

---

## 三、XEYO 现状盘点（对照）

已读：`gui/src/components/Composer.tsx`、`gui/src/lib/slash.ts`、`gui/src/lib/slashCommands.ts`、`python/engine`、`python/tools/skill_tool`、根目录 `_dsh_composer_ops.txt`（上一轮改造的编辑记录）。

| 能力 | dsh | XEYO 现状 | 差距 |
|---|---|---|---|
| 触发检测 | detectTrigger 纯核 + 词边界 + 守卫分档 | `slashTokenAt`（光标词元，URL/路径安全）✅ | 基本对齐 |
| 弹层候选 | 异步 fetch + generation + ready 门控 | 本地同步过滤（manifest/技能缓存）✅ | 可接受 |
| **键盘仲裁** | ↑↓/Enter/Tab/Esc/IME 全接管 | **无**；弹层开着 Enter 直接 onSend | ❌ P0 |
| **IME 守卫** | 所有仲裁 composing 即 pass | 着色层有，**Enter 提交无 `isComposing` 检查** | ❌ P0（潜在组词上屏即发送 bug） |
| 命令态 | claim 显式建立 + ghost hint + Space 裁决 | 提交时 `startsWith('/')` 推导 | ⚠️ P1 |
| **技能直呼** | 宿主 pre-step 注入渲染正文，确定性 | 客户端改写 `[slash:/name] 请用 Skill 工具…`，赌模型调用 | ❌ **P1 最大收益** |
| 技能目录 | 单飞 RPC + warm + lexicon 失效订阅 | workspace 缓存，无失效通知 | ⚠️ P2 |
| @ 引用 | `@` 文件+会话，下钻/引号形态 | 无；附件走 + 菜单，payload 拼 `@path` | ❌ P2 |
| 提交事务 | attempt seq + 漂移守卫回填 | 有乐观清稿 + 失败回填雏形，无 CAS | ⚠️ P3 |
| 菜单组件复用 | 加号 launcher = 同一 MenuView | + 菜单（ComposerQuickMenu）与 slash 弹层两套 | ⚠️ P3 |

另外 XEYO 已有而 dsh 值得学的：会话级草稿持久化（含模式）、忙碌时 202 排队 inbox chip——这两点 dsh 的 note 里反而是欠账方向，不必动。

---

## 四、怎么落地（分阶段建议）

遵守 AGENTS.md 硬规矩：新行为旁路上线（feature flag）→ 有收益证据再并入；新逻辑进新模块，Composer.tsx 只留接线点；提交门 tsc+vitest+pytest P0 全绿。

### P0 —— 输入框键盘仲裁 + IME 守卫（纯 GUI，一两天量级）

1. **新建 `gui/src/lib/slashMenuKeys.ts`**（不进 Composer 巨石）：输入 `arbitrate(key, composing, {highlight, items})` 纯函数，照抄 dsh 语义——↑↓移动、Enter 选中（无高亮则放行提交）、Tab 选中、Esc 关闭、`composing` 一律 pass。
2. Composer 的 `onKeyDown` 在 `slashMenuOpen` 时先走仲裁（新模块调用），同时**无条件**补 `if (e.nativeEvent.isComposing) return;`——这是顺手修的既有隐患，单独写进 commit message。
3. 候选项 onMouseDown 已有 `preventDefault`，补高亮 state 与 `aria-selected`，弹层行复用现有按钮。
4. 测试：`slashMenuKeys.test.ts` 纯函数矩阵（dsh 同款"交互矩阵逐行投影"思路）。

### P1 —— 技能直呼宿主化（对应 dsh pre-step，收益最大）

1. **python 侧新模块** `python/engine/skill_preinvoke.py`（旁路，feature flag，如 `XEYO_SKILL_PREINVOKE=on`）：在 QueryEngine 每轮组装 prompt 前，扫描用户消息首行 `/name` 词元——先查 slash registry（命令命名空间优先，GUI 已消费的命令不该到这），再查 skill_store；命中 user-invocable 技能 → 渲染 SKILL.md 正文，按 **T_now 注入管线**（`pre_llm_inject.py` + `T_NOW_BLOCK_REGISTRY` 登记一块，硬顶 20 内）注入本轮上下文，任务材料置后。
2. 注入后在本轮标记 `skill-invocation`，GUI 气泡侧可据此把 `/name` 装饰成技能 chip（对应 dsh 的 SkillNameProjector）。
3. GUI `slashCommands.ts` 的 `[slash:/name]` 改写退役（或仅留 CLI 兼容），`/name`（无参）仍走 `/skills show` 回显卡片。
4. 收益证据：对比模型「自觉调 Skill 工具」的命中率/首 token 延迟（旁路期统计），再决定并入主链路。
5. 红线自查：注入内容走 T_now 声道（env_channel），不进 MessageStore、不碰 tools 数组、不进 system prompt。

### P2 —— `@` 引用源 + 技能目录失效

1. 后端加 `GET /v1/references/files?session&q=`（复用现有文件索引/glob 能力，loopback 门禁同 `/v1/slash`）。
2. `slash.ts` 泛化 token 检测为 trigger 字符表 `{'/': ..., '@': ...}`（`slashTokenAt` 加 trigger 参数，纯函数扩展）；弹层注册两个"source"，`@` 插入 `@相对路径 `（buildPayload 已支持该形态，模型侧按需读文件）。
3. skill_store 变更后由 extensions 路由推一条失效（或 GUI 轮询 ETag），清 `slashSkillsRef` / `skillListCache`——对应 dsh 的 subscribeLexicon。

### P3 —— 工程化收尾（可缓）

- Space 裁决 + ghost hint（claim 显式建立，替代提交时推导）。
- 提交事务补 CAS（draftRev 相等才回填）。
- `ComposerQuickMenu` 与 slash 弹层合一（dsh launcher 模式）。
- 若将来要做真 chip（附件/引用原子化），评估 Lexical——dsh 的翻案笔记值得先读。

---

## 五、dsh 源码索引（本次读过）

- `packages/client/ui-input-trigger/src/client/{contract,controller}.ts` —— 触发流水线与键盘仲裁
- `packages/client/ui-skill/src/client/index.ts` —— 技能 source（纯文本 pick + 目录缓存）
- `packages/client/ui-commands/src/client/{contract,service}.ts` —— 命令 claim / popup / decoration
- `packages/client/ui-reference/src/client/index.ts` —— `@` 文件+会话引用
- `packages/skill/tool-skill/src/index.ts` —— 宿主 pre-step 技能注入 + 目录 catalog
- `.agents/notes/implemented/architecture/2026-07-25-web-input-machine-and-slash-pipeline.zh.md` —— 权威设计笔记
- `.agents/notes/implemented/architecture/2026-08-20-web-composer-lexical-editor.*` —— 编辑器翻案笔记

克隆位置：`D:/lea/dsh-src`（sparse-checkout，仅 client/skill 相关包；需绕过本机 7897 代理直连 GitHub）。

---

## 六、落地记录（2026-09-05 下午，已全部实施并提交）

### P0 键盘仲裁 + IME 守卫 ✅
- 新增 `gui/src/lib/slashMenuKeys.ts`：`arbitrateSlashMenuKey` 纯函数核（↑↓ 环形移动 / Enter·Tab 选中 / Esc 关闭 / IME 组词一律 pass / 无高亮 Enter 放行提交），语义对齐 dsh `InputTriggerController.arbitrate`。
- `Composer.tsx`：弹层开着时按键先经仲裁；hover 与键盘共享同一高亮（后到者胜，dsh 同款）；行级 `aria-selected` + 高亮底色；**全局 `e.nativeEvent.isComposing` 守卫**（顺手修复组词上屏瞬间 Enter 误发送的隐患）。
- 测试：纯函数矩阵 6 用例 + Composer 交互 3 用例（选中不发送 / 无高亮透传发送 / IME 不发送）。

### P1 技能直呼宿主注入 ✅
- 新增 `python/engine/skill_preinvoke.py`（旁路模块）：识别 fresh-user 轮首行 `/name`（token 止于空白，`/nfs-hg/xxx` 路径不命中）→ 命令命名空间优先（slash registry 含中文别名命中即退让）→ 命中 **user_invocable** 技能 → 渲染 SKILL.md 正文 + `$ARGUMENTS` 语义（与 skill_tool 一致）+ 12KB 截断。
- 装配点：`pre_llm_inject.py` 新登记块 `skill_preinvoke`（directive 类，登记表 22/22 满额，`_tag_block` 执法测试通过）。走 T_now env_channel 声道：不进 MessageStore/JSONL、不碰 tools 冻结数组、不进 system prompt。
- GUI `slashCommands.ts`：`/name 任务…` **退役** `[slash:/name] 请用 Skill 工具…` 客户端改写，改为**发送即原文**（dsh plain-text 决策）——JSONL 存真实用户文本，宿主识别手势。`/name`（无参）仍回显技能卡片。
- 开关：`XEYO_SKILL_PREINVOKE=off`（默认开）。**已知限制**：仅 fresh-user 轮注入，工具续写轮不重放（长流程中段依赖模型凭目录重载；如实测有丢规程现象，再评估 after_tools 轮重放）。CLI 侧若把 `/name 任务` 原样发给引擎同样受益（`cli/slash.py` 未知命令拦截路径待验证）。
- 测试：8 用例（词边界/命令优先/门控/参数语义/截断/开关）。

### P2 @ 文件引用源 ✅
- 后端新增 `python/server/routers/references.py`：`GET /v1/references/files?workspace&q&limit`——只读目录扫描，重目录剪枝（node_modules/.git/target…），深度→字典序排序，4000 条扫描硬顶，`require_loopback` 门禁；已注册进 `server/app.py`。
- 前端：`slash.ts` 泛化出 `triggerTokenAt(value, caret, trigger)`（`/` 与 `@` 共用一个词边界核）；新增 `lib/api/references.ts`（120ms 防抖 + AbortController 键击作废）；Composer `@` 弹层（文件路径候选，选中插入 `@相对路径 `，模型侧按需读文件——Cursor 式）。
- 测试：端点 5 用例（枚举/过滤排序/limit/坏 workspace/参数校验）。

### 提交与事故记录
- ⚠️ **二次 git 事故（13:33）**：为验证 7 个 vitest 失败的基线归属执行 `git stash push -- <ASCII pathspec>`，stash 失败并**再次击穿 .git**（refs/heads 清空 + objects 仅剩 7 个文件、无 pack——今晨事故手册预言的最坏形态）。**结论升级：本仓库 stash 一律禁用，不分中英文路径**（已写入项目记忆）。
- 恢复：按晨间手册封存现场（`.git.broken-20260905-1337/`），从完整工作树二次重建档案，重建点 `1f5ac88`（1898 文件，含本次 P0/P1/P2 全部改动与并行会话的 sticky 改动）；bare mirror 备份 `D:/lea/XenYon-git-mirror-20260905-1340.git`（remote 名 `mirror`，已推送）。**注意：13:16 之前的全部提交历史（含今晨 9ee82ee 重建点与并行会话的 sticky 修复提交 84da657）对象已丢失，仅存于 reflog 文本记录。**

### 遗留（下会话接续）
- 7 个 vitest 既有失败（MessageList reasoning 文案 / chatStore busy 超时 / committed_resend_failed 恢复横幅 / mainPaths 联动）——与本次改动无关，属并行会话在途工作，按规矩需归属方 xfail 或修复，勿静默挂账。
- dsh ghost hint 未实装（调研见下节）。

---

## 七、调研：dsh `/goal` 的 ghost hint（参考图）实现方式

图中效果：输入 `/goal` 后，token 右侧跟一段**灰色提示文字**「输入目标，智能体将持续执行」，占位但非草稿内容。源码链路（三段式）：

### 7.1 claim 携带 hint（ui-commands）
宿主命令目录里带 `input` 描述符的命令（如 goal 的 leadingInput）在**claim 建立时**附带 `hint`：
- `packages/client/ui-commands/src/client/service.ts:195`：菜单行 `rows.push({name, description, ...(c.input !== undefined ? {hint: c.input.hint} : {})})`；
- `service.ts:317-322`：`leadingClaim` 形状 `{token: '/goal ', hint?}`——claim 由 pick/Space 裁决显式建立（输入机 phase → `claimed`），**不从 draft 推导**。

### 7.2 渲染判据（ui-conversation InputBar）
`packages/client/ui-conversation/src/client/skeleton/InputBar.tsx:356-373`：
```
claimActive = phase ∈ {claimed, submitting} && draft.startsWith(claim.token)
rawHint    = claimActive && 参数区（token 之后的文本）为空白 ? claim.hint : null
hint       = 字典查 hint.<命令名>（本地化文案优先）→ 未命中回落 claim.hint
```
细节：
- **用户一开始输入参数，ghost 立即消失**（`draft.slice(token.length).trim() === ''` 才显示）；
- 特殊覆盖：goal 已有活动目标时换 `hint.goal.active`（"当前目标进行中。可输入 edit 修改 / pause 暂停 / resume 继续 / clear 清除"）——同一命令按状态换提示；
- 本地化：`locales.ts` 的 `hint.goal` / `hint.goal.active` 键，按命令名动态查字典。

### 7.3 零 DOM 注入的 CSS 呈现
`InputBar.module.css:180-186`：不往文档树插文本——
```
.input p:last-child::after { content: var(--dsh-composer-hint); color: <caption 灰> }
```
InputBar 把 hint 经 `JSON.stringify` 塞进 CSS 变量 `--dsh-composer-hint`（变量不存在时 content 声明非法、什么都不渲染）。纯装饰：不进草稿、不可选中、不参与提交、撤销/剪贴板零特判。

### 7.4 移植到 XEYO 的最短路径（未实施）
XEYO 的 textarea 没有伪元素挂点，但有现成的**镜像覆盖层**（slashOverlayRef）。最短做法：
1. `applySlashPick` 选中命令时记录 `slashClaim = {token: usage + ' ', hint}`（弹层数据源里给常用命令补 hint 文案，如 `/goal → 输入目标，智能体将持续执行`）；
2. 显示判据照抄：`value.startsWith(claim.token) && value.slice(token.length).trim() === ''`；用户退格破坏 claim（`!startsWith`）即清除；
3. 渲染：slashOverlay 镜像层里在 token 后追加 `<span class="text-mute/50">{hint}</span>`，textarea 保持 `text-transparent`（现有着色机制已具备）。
预计改动集中在 Composer + slash manifest 数据，一个会话内可完成；建议作为独立小功能走旁路验证后再定。
