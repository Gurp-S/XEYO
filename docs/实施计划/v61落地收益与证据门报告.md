# v61 落地收益与证据门报告

> 执行依据：`docs/实施计划/v61建议采纳说明.md`（A1–A6 完全采纳落地 + B1/B2/B3 证据门测试）。
> 测量脚本（零 LLM 调用，可复跑）：
> `python\.venv\Scripts\python.exe -m scripts.v61_adopted_benefits`（在 `python/` 下）
> `python\.venv\Scripts\python.exe -m scripts.v61_evidence_gate`
> 原始数据：`python/.diag_memory_cost/v61_adopted_benefits.json`、`v61_evidence_gate.json`

---

## 1. 完全采纳项（A1–A6）离线收益表

| 项 | 开关（默认） | 场景 | 基线 | 采纳后 | 收益 |
|---|---|---|---|---|---|
| **A3 C2 逃生舱** | `XEYO_C2_ESCAPE_HATCH`（**1**） | 长合成左区（噪声+kv+新旧两条栈+工具路径），正常预算 | 栈保真 105 字符；**路径 0 条** | 栈保真 105 字符；**路径 3 条**（notes:msg 引用） | 诊断定位信息补全；逃生舱块开销仅 **+2.85%** 摘要字符 |
| **A3 极小预算压力** | 同上 | 摘要预算压到 400 字符 | **栈整条丢失**（含栈=False） | **栈逐字在场**（含栈=True）+ 3 条路径 | 预算耗尽不再丢现场；先扣预留保证确定性 |
| **A1 ω 冷却平滑** | `XEYO_CACHE_COOLDOWN_OMEGA`（**0**） | 挂机 4h 后决策扫描（27 状态，L 1k–90k） | keep 预测命中 **H=0**（TTL→全 miss） | H = α×floor（0→358…3200 tokens 随 L 增长） | 预测不再被 TTL 打死；压缩决策带右移 **1416 tokens**（窗口内基线误压、ω 保持）；省 1 次 c_action（1128.8 u¥）；J_keep 平均降幅 20.1% |
| **A1 零回归护栏** | 同上 | 温缓存（age=0） | — | **J 表与决策逐位一致** | ω 只抬冷缓存下包络，温行为零改动 |
| **A2 retrieve 还原** | `XEYO_MEMORY_RESTORE`（**1**） | 压缩含旧栈+kv+新栈的左区 | 压缩即有损，旧栈永久丢失 | 碎片入库 4 条（stack/kv）；**最新栈字节级还原=True**；旧栈 retrieve 可回；**因果边 0→1**（tool_use→tool_result） | 压缩从有损→可还原；`Memory(action=retrieve)` 端到端找回（含 notes:msg:<i> 解析与 8k 上限） |
| **A4 sqlite 清单索引** | `XEYO_MEMORY_SQLITE_INDEX`（**0**） | 300 笔记 × 90 次词法查询 | **300 次/查询** 文件解析；5.62s | 热轮 **0 次/查询** 解析；4.38s | 解析开销 −100%（(mtime,size) 签名缓存 + 写穿透）；墙钟 **−22.1%**；**命中数三臂完全一致**（评分逻辑零改动）；fail-open 回退有单测 |
| **A5 session.md 差分重写** | `XEYO_SESSION_MD_DELTA`（**1**） | 40 轮更新 ×2 形态 | 全量重写；**回滚=整删失忆** | 只记 Changed 节；**fold(deltas)==物化文件**（逐步成立） | 推进型增量占比 120%（差分不省字节但换来可回放审计）；稳定型 **80%**；回滚按 turn **精确截断**（phase20 在、phase21 不在）；坏行只废一条 |
| **A6 源健康门** | `XEYO_ALLOW_UNHEALTHY_SOURCE`（未设=拒绝） | 12× 循环副本病态源（408 行，unique_ts=17） vs 健康源（220 轮真录形态） | 无门——病态口径直接进评测 | 病态**报错拒绝**（unique_ts/rows=0.04<0.5、user 轮头重复 12>2）；健康源放行 | 12× 循环副本口径无法再骗门；`--source-health PATH` 可独立诊断 |

> A6 同时已在 `--ab` / `--hitrate-live` 载入长源（rows≥120）时**自动强校验**，并在 hitrate-live 头部打印健康摘要。

---

## 2. 条件采纳项（B1/B2/B3）证据门裁决表

| 项 | 门限（采纳说明 §3） | 实测 | 裁决 |
|---|---|---|---|
| **B1 Pareto 替代 J** | 「丢高价值原子省<5%成本」清零 且（同成本 ΔQ≥+3pp 或 同保真成本−≥5%）且 J_pred 误差不升 | **基线本身未出现病态**（21 场景：冷缓存 14 + 温缓存临界带 7，凡 C2 胜出节省均 >5%）；θ_D=0.05：病态 0 但把全部压缩推到 keep，**成本比 2.18×**、ΔQ+9.1pp（"同成本"不成立）；θ_D=0.2/0.5：**决策零改变、零收益**；J_pred 误差代理 0.0（合成稳态下 p0/p1 重合，不升） | ❌ **不采纳**（保留 env `XEYO_V61_PARETO` 默认关）。D 一刀切硬约束在当前成本结构下要么无收益（θ 宽）要么代价失真（θ 严）；「省小钱丢大脸」在 J 现行参数下并未实际发生。若未来真录重放出现该病态，建议改用「D 超阈 **且** 省钱<5%」的**条件**否决而非无条件硬约束，另立门再测 |
| **B2 s_i 驻留分** | stack 原子存活率 ≥95% 且 整体保真不降 且 成本 ≤+2% | **改对证据门后**（全网格 θ 标定 + 判「部署形态」+ Q 同标尺归一）：标定系数 0.2396→θ'=0.12；栈存活 6/14→**14/14**（达标）、保真归一 0.6755≥0.5775（达标）、但**校准后成本比 1.4439（+44%）超 ≤+2% 门**；**校准决策 keep 14/14、C2 0 次** ——「栈存活 14/14」靠**整窗不压缩**换来 | ❌ **维持不采纳**（env `XEYO_V61_SI` 默认关）。**关键证据：s_i 的「全覆盖死保报错栈」与「省成本」不可兼得**——校准后系统为保栈放弃全部压缩（+44%）。**且 A2（retrieve 字节级还原）+ A3（逃生舱保最近 traceback）已覆盖栈安全需求**，s_i 的边际收益不值 +44% 成本。后续若要启用，须按 scale-drift 护栏（F2）重审并另立更高的成本预算门 |
| **B3 动态 Horizon** | 加性不收紧 且 静态验收不回归 且 档位对齐误差（J_pred 口径）中位 −≥25% | 112 状态网格：**加性收紧违例 0**；P1 缺失3 静态验收**不回归**（窗口 30%/0 待读→仅 4 档；75%/5 待读→16 档参与）；**单档死锁解除 48 状态（42.9%）**；档位对齐误差中位 **0.6905→0.1714（−75.2%）**（门槛 −25%） | ✅ **采纳**（env `XEYO_V61_DYNAMIC_R`，代码已合入、默认关）。按 §3 通用协议，合主干前还差「真录冻结会话（≥3 条含 200+ 轮）重放复验」——待 A6 真录数据到位后跑通道 B 复核，即可翻默认值 |

---

## 3. 回归

- 全量回归 **229 passed, 4 skipped, 0 failed**：交接文必绿清单（test_runtime_c2 / test_c2_llm_summary_t8 / test_memory_switches / test_quality_plan / test_session_md / test_memory_search / test_memory_index_digest / test_memory_summarize / test_fidelity_segmenter / test_cmd_compact）+ simulator 全套 + 本轮新增 5 个测试文件（test_memindex / test_c2_escape_hatch / test_cache_cooldown_omega / test_session_md_deltas / test_memory_retrieve，共 34 项）。
- 已知存量失败（stash 验证与本次改动无关）：`tests/test_rewind_service.py::test_execute_blocks_when_external_file_change_is_detected`（mtime 粒度竞态）、`tests/test_tool_perf.py::test_glob_empty_nomatch_does_not_flood`（rg 对 pytest 临时目录 os error 5）。
- B1/B2/B3 关闭态逐位一致由测试保证：B3 温缓存 J 表逐位相等断言（test_cache_cooldown_omega）+ 决策链默认路径无 env 不触发新分支。

## 4. 变更文件

| 层 | 文件 | 内容 |
|---|---|---|
| 索引 | `python/memory/memindex.py` | **新增**：sqlite 派生索引（notes/rollouts/fragments/edges），签名缓存、fail-open、写穿透、retrieve 碎片/因果边 |
| 读取 | `python/memory/memdir.py` | load_notes 走 sqlite 缓存（fail-open）；write_note 写穿透；split_frontmatter 修复 |
| 运行时 | `python/memory/runtime.py` | A3 逃生舱（栈+路径块，先扣预留）；A2 `_store_c2_fragments`（栈优先/去重/因果边）；A3+A2 接入 apply_c2_messages；B3 接线 avg_turn_tokens |
| 模拟器 | `python/memory/simulator/{params,cache_model}.py` | A1 ω 冷却（half_life/floor/env） |
| 模拟器 | `python/memory/simulator/{decision,state_model,quality_model,r_estimator}.py` | B1 `_pareto_pick`；B2 `atom_kind`+`q_unit_si`；B3 `dynamic_r_base`+加性分级（全部 env 门控） |
| 会话 | `python/memory/session_md.py` | A5 delta 日志（append/fold/rebuild/精确截断） |
| 回滚 | `python/rewind/service.py` | 回滚调 `clear_after_rollback(keep_tool_calls=N)` |
| 工具 | `python/tools/memory_tool/{memory_tool,prompt}.py` | `action=retrieve`（只读，notes:msg:<i>[:kind]） |
| 开关 | `python/memory/memory_switches.py` | 新增 8 个开关注册 |
| 评测 | `python/scripts/memory_stack_eval.py` | A6 `source_health`/`--source-health`/硬门+健康打印 |
| 测量 | `python/scripts/v61_adopted_benefits.py`、`v61_evidence_gate.py` | **新增**：收益表与证据门脚本（可复跑） |
| 测试 | `python/tests/test_{memindex,c2_escape_hatch,cache_cooldown_omega,session_md_deltas,memory_retrieve}.py` | **新增**：34 项 |
