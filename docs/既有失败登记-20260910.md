# 既有失败登记表（xfail 台账）

> 依据 `AGENTS.md` 工程硬规矩第 1 条：**提交门 = `tsc + vitest + pytest P0` 全绿才允许 commit**；
> 既有失败必须**显式登记并注明归属**，不允许静默挂账。
>
> 本表登记「非本次改动引入、当前仍红」的用例。修掉后请删除对应行。
> 每次跑提交门前先看本表，确认红的都在册。

最后核对：2026-09-10（提交 `9288cec` 后）

---

## 一、前端 vitest

全量口径：`cd gui && npx vitest run`
当前结果：**Test Files 2 failed | 99 passed (101)；Tests 5 failed | 743 passed (748)**

### F-FE-1　`src/paths/mainPaths.workflow.test.tsx`（4 例）

| 用例 | 症状 |
| --- | --- |
| 发：空态 → 用户气泡上屏 → 助理流式落定 | `TypeError: Cannot read properties of undefined (reading 'find')` |
| 权限：tool ASK 挂起 → pendingPermission 可见 → resolved 清除 → 工具行落定 | 同上 |
| reattach：崩溃残留孤儿工具 → recoverStuckStream 收敛 → UI 呈现工具行 | 同上 |
| 回溯 v3：openDialog → RewindV3Dialog；v2 preview 已退役 | `AssertionError: the given combination of arguments (undefined and string) is invalid for this assertion` |

- **归属**：**非开源闸门批次引入**。该文件最后改动为 `eb8a709`（2026-09-07 `refactor(gui): 删除生产零引用死代码`）。
- **与本次关系**：本次 6 项 P0 未触碰该文件及其 import 链（未改 MessageList / chatStore / rewindV3 / 权限 UI）。
- **稳定性**：连续两次单跑均稳定复现，**非 flaky**。
- **性质**：疑似测试适配器未跟上被测装配的签名/返回值漂移（`undefined.find` 与断言参数收窄），属**测试维护性失败**。
- **处置**：待排期修复；修前不在本批次内。

### F-FE-2　`src/components/MessageList.chat.test.tsx`（1 例）

| 用例 | 症状 |
| --- | --- |
| shows reasoning when loading without stream text | `TestingLibraryElementError: Unable to find an element with the text: Planning next steps.`（期望文本未渲染；DOM 已挂载） |

- **归属**：**非开源闸门批次引入**。该文件最后改动同为 `eb8a709`（2026-09-07）。
- **稳定性**：稳定复现，**非 flaky**。
- **性质**：思考态/流式文案渲染断言漂移（与 `docs/最终方案-思考态回放.md` 系列改动可能有关，需回溯确认）。
- **处置**：待排期修复。

---

## 二、Python pytest

全量口径：`cd python && ./.venv/Scripts/python.exe -m pytest -m "not live"`

### 曾登记、已修复（保留记录）

| 用例 | 原症状 | 修复 |
| --- | --- | --- |
| `test_job_registry_t42.py` 5 例 | 4 例 `check_permissions` 桩签名漂移（缺 `cwd`）；1 例容量满断言过期；1 例 digest 含 `job_output` 断言过期 | 提交 `d834aa8`：桩改 `**kw` 吸收 + 断言对齐现行语义（**均为测试侧 drift，源码未改**） |

> 当前 python P0 合集（`test_usage_ssrf_guard` / `test_workspace_fs` / `test_server_gate_g51` / `test_bash_promote` / `test_job_registry_t42`）= **72 passed**。

---

## 三、提交门现状（透明披露）

| 门 | 状态 |
| --- | --- |
| `tsc --noEmit` | ✅ 全绿 |
| `cargo check` | ✅ 通过（1 个既有 `spawn_pet never used` 警告） |
| `vitest` | ⚠️ **5 red，全部在册（F-FE-1 ×4 / F-FE-2 ×1）**，非本批次引入 |
| `pytest P0` | ✅ 72 passed |
