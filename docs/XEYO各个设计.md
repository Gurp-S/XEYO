flowchart TD
    U[用户消息 图形界面/命令行/渠道] -->|提交消息| QE[查询引擎]
    QE --> SYS[① 构建系统左段<br/>提示组装器.构建系统各部分<br/>会话级缓存：同一次提交可复用]
    QE --> STORE[消息存储<br/>全量原始内容 · JSONL 永久保存]
    STORE --> PROJ{② 模型投影<br/>XEYO_L5 开关}

    PROJ -->|默认投影策略| C0[C0 单条截断 ≤8192 字符<br/>保留头尾 + 围栏完整]
    C0 --> C1[C1 占位冻结<br/>冻结点之前的工具结果<br/>→ 替换为固定的 [压缩] 占位文本]
    PROJ -->|XEYO_C2_GATE=1 超长会话| C2[C2 压缩策略<br/>优先使用会话记录 → 确定性摘要<br/>首次压缩冻结 + 仅追加扩展<br/>引用锚点 ⟦笔记:消息:索引:类别⟧]
    PROJ -->|XEYO_L5=v61 实验特性| DEC[决策 v6.1 版本<br/>硬上限 → 求最小化 J → 取 4/8/16 轮多票表决<br/>θ0.35 质量门槛 + 经济门槛 + λ_q 变笨成本系数]
    C1 --> TNOW[③ 注入当前轮次内容<br/>预填充注入 分类装配<br/>指令 / 事件 / 清单<br/>预算 6000 / 清单占 2500]

    SYS --> API[④ 组装 API 消息<br/>系统左段 + 投影 + 工具定义（会话级缓存冻结）+ 模式过滤]
    TNOW --> API
    TOOLSC[工具定义缓存<br/>会话内零前缀重复缓存，严守红线] --> API
    API --> LLM[⑤ 调用模型流式接口]
    LLM --> TOOLS[⑥ 工具执行]
    TOOLS --> STORE
    TOOLS --> MEM[记忆工具<br/>写入/更新/遗忘/检索/同行查看]

    MEM --> MEMDIR[⑦ L4 长期记忆存储<br/>~/.xeyo/memory/工作区ID/<br/>MEMORY.md 索引 + topics/*.md 主题文件<br/>+ 删除标记 + candidates.jsonl 候选队列]
    MEMDIR --> SEARCH[词法检索（不调用模型）<br/>命中内容拉入上下文尾部<br/>跨会话的 会话记录 + 推演记录合并进入]
    MEMDIR -->|触发沉淀机制| NS[夜间晋升任务<br/>限频 8 次/轮 · 冷却 6 小时 · 阈值 20 条/2MB<br/>绝对不进入系统左段]
    LLM --> OBS[⑧ 观测校准<br/>预测长度 vs 实际命中对比<br/>仅观测不决策 → 产生校准事件]
    LLM --> USE[⑨ 用量记账<br/>events.jsonl 请求级记录 / 图形界面前端本地累计]

    QE --> SMD[⑩ L5b 会话记录<br/>目标/当前状态/已完成项/...<br/>≥8 次工具调用时写入 · 每 8 轮重写一次]
    SMD -->|会话结束| ROLL[推演归档 ≤20 份<br/>供跨任务检索使用]
    INS[⑪ L1 XEYO.md 指令族<br/>~/.xeyo + 工作区链] -->|按修改时间+大小签名| SYS
    INS -->|读取到达子目录| NESTED[嵌套规则 → 当前轮次懒加载<br/>嵌套指令]
