# 15 条改进 · 全部执行 + 收益裁决报告（2026-09-08）

> 按 AGENTS.md 纪律：新逻辑独立模块 + 旁路 + 离线数据/理论验证——**有收益才并入，
> 无收益即撤回，不确定的列在文末待用户裁决**。全程未向你提问，结论如下。

## ✅ 采纳（8 条，全部带离线测试或实测数据）

| # | 条目 | 落地形态 | 证据（测试/数据） |
|---|---|---|---|
| 8 | 首轮嗅探 | `engine/first_sniff.py` + query_loop turn=0 接线（env 声道、投影-only、有界 ≈2KB、默认开、`XEYO_FIRST_SNIFF=0` 关） | 单元 5 例；**实测数据**：59 条评测轨迹里 **93% 首 agent 步就烧 ls/pwd/find 发现类命令，前 3 步平均白耗 2.88 次调用**——即使 system 已声明 cwd，模型仍要 ls 才动手，嗅探注入的正是这份缺失的清单 |
| 15 | 命令族动态超时 | `tools/bash_tool/timeout_map.py` + parse_input 接线（pip/npm/make/gcc/cargo/apt…；纯查表；worker 不覆盖；env 可调） | 单元 8 例，锁「家族默认 >120s 一刀切」契约 |
| 13 | 截断错误行抢救 + #3 回归锁 | `truncate.py`：head20k+tail8k 之外，尾部无高信号错误行时从中段抢救 ≤8 行错误证据 | 单元 4 例（含旧契约回归锁：短文本原样/双向保留/全量落盘） |
| 10 | 写去重暴露 | FileWriteTool：内容与磁盘一致 → 跳过落盘，返回「unchanged」 | 单元 1 例（计数落盘次数=1 不增） |
| 11 | 语法门错误提示闭环 | `tools/fileio/syntax_check.py` + FileWriteTool 写后自检：**新引入** py/json 解析错误 → 行列文案注入工具结果（只提示不拦截，允许 WIP） | 单元 3 例（py 行号 / json line+col / 旧已错不唠叨） |
| 6 | 软信号审计 | grep engine/prompt/tools 全零命中（唯一命中是 silent 命令的中性 "Done"） | 审计通过，无需改码 |
| 4 | rewind 逆操作自测 | `test_rewind_file_tools`+`test_rewind_journal` 5 例通过 → 写路径 undo 已接线，无需新码 | 自测通过 |
| 1 | BENCH 口径登记 | `docs/BENCH-口径.md`：60.7/70.8/20.7% 口径 + 官方 82.7% 对照说明 + 新分数检查单 | 文档交付 |

## ⛔ 已评估不采纳（附理由，未产生任何代码残留）

| # | 条目 | 撤回/不采纳理由 |
|---|---|---|
| 7 | 路由扩面 head/tail | `head -c`(字节)/多文件/`tail` 无法静态映射到 Read(行)语义——违反模块「宁放勿拦」红线；cat/rg 两大头已覆盖，扩面边际收益 < 误路由风险 |
| 5 | 动态预算解锁 | 前提不存在（无 32 轮硬顶）；「弹性限额」产品收益已由 #15 以命令族超时形式落地 |

## ❓ 待你裁决（3+2 条，已评估、未写码——都涉及产品决策或深层接线，不能乱猜）

1. **#12 后台进程收尾**：旧式后台任务已绑会话 abort（会话结束即杀）；registry job 有 job_list/job_kill+完成通知。缺的是「跨会话孤儿进程」清理——旧式任务不记 pid，崩溃后只剩日志，**无法可靠杀**；自动 kill 用户自启服务又危险。选项：A) 仅收尾可见提示（安全） B) pid 追踪+TTL 强制清 C) 不做。
2. **#14 误删恢复提示**：rewind 已有快照+逆操作；「误删关键文件→提示可恢复」需要定「关键文件」标准 + 出现通道（T_now 事件块需过 23 块硬顶登记）+ 用户确认 UI——三层都需你拍板。
3. **#9 done 提议产物核对**：wrap_gap 只在收尾窗核对；提前到每次 done 提议需动 query_loop 主循环（1973 行巨石），回归风险高。要不要做（需 T_now 新块或旁路）。
4. **#2 todo 完成度提示**：线性 todo 登记表对 TB 取证零责任；做「X/Y 完成度」提示属 UX 增强，收益无数据支撑。
5. **#13 增强可选**：stderr 分段保留权重（当前 stdout/stderr 在 spawn 层已合并，无法还原分段）——只接受 #13 现状即保留 head/tail+excerpt 方案。

## 建议优先验证路径
- 纯前端无需改；引擎改动已过离线单测（22 新 + ~130 相关回归绿，失败全为沙箱缺插件：pydantic/pytest-asyncio/python 不在 PATH）。
- 请手动跑 `run-pytest-p0a.bat` 走完整 P0 门（沙箱不可靠，见仓库记忆）。
- 可选在线验证：#15 设 `XEYO_CMD_TIMEOUT_OVERRIDES`；#8 用 `XEYO_FIRST_SNIFF=0` 消融对比。

---

## 四项裁决落地(2026-09-08 04:20,commit 待见 git log)

用户对四项待裁决给出决定,落地如下:

1. **#12 后台进程收尾 → 采用方案 a,但换实现 = GUI 直接可见(不写引擎收尾逻辑)**
   结论:**已由 42 号 P0 GUI 完整覆盖,零新码**。证据链:
   - `gui/src/components/SessionJobsBadge.tsx`(ChatHeader.tsx:569 挂载,main 模式)= 角标(running+stopping 计数,为零整隐)+ 只读弹层(活跃行每秒推进耗时、终态行弱化保留、确定性排序)。
   - 数据双通道:`lib/api/jobs.ts` GET `/v1/sessions/{sid}/jobs` 5s 轮询(有任务才轮)+ SSE jobs 帧(chatStream.ts:309 → chatStore.sessionJobsById)。
   - 后端:`server/job_registry.py`(per-session owner 边界、容量 10、job_kill 语义)+ `tools/bash_tool/jobs_bridge.py`(start/adopt)+ bash 前台 45s 活进程自动晋升(adopt)。
   - 测试:`SessionJobsBadge.test.tsx` + `lib/api/jobs.test.ts` → vitest **9 passed**。
   与用户的"直接显示 GUI 的 UI 给用户看即可"完全同形;会话收尾引擎侧不 kill、不注入提示(GUI 只读可见,用户自己决定)。
2. **#9 done 提议产物核对 → 延后**(不写码,保持 wrap_gap 收尾窗现状)。
3. **#14 误删恢复提示 → 延后,待细细讨论**(关键文件清单 + T_now 登记 + rewind 恢复 UI 三层未拍板,先不写码)。
4. **#2 完成度提示 → 做**,已落地:
   - 新模块 `python/engine/todo_hint.py`:`build_todo_hint(todos)` 纯函数渲染「已完成 X/Y + 剩余项」,total<2 或全 done 返回空;上限 6 行截断;内容截 80 字符;无褒贬评语(守 #6)。
   - 装配:`prompt/pre_llm_inject.py` repeat_guard 块内并入——只在 repeat/stall advice 非空(引擎检出卡住/重复证据)时渲染,**常态零注入**;与停滞监测同块消费先例一致,**不新增 T_NOW_BLOCK_REGISTRY 条目、不动 T_NOW_BLOCK_HARD_CAP=23**(消融随 XEYO_T_NOW_SKIP=repeat_guard)。
   - 测试:`tests/test_todo_hint.py` 15 例全绿(纯函数 9 + run_pre_llm_inject 全管线装配 6:advice 空不注、total<2/全 done 不注、有 open 注入且渲染 X/Y、stall 触发、消融生效)。
   - 装配面回归:todo_hint+registry+stagnation+pre_llm_inject+env_channel+wrap_gap+goal_state **全绿**;沙箱内 test_repeat_guard 3 个 async 用例缺 pytest-asyncio 属既有环境失败,与本改动无关(已用控制实验隔离确认)。
   - 测试教训:测试内**勿换 sys.modules** stub(会污染同进程后续惰性 import 的用例);改对真模块 monkeypatch 属性,fixture 自动还原。
