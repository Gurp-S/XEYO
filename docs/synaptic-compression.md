# 突触压缩（Workflow Synaptic Compression, WSC）

> 旁路形态实验。状态：**未接入生产链**。生产行为零改动。
> 落地位置 `python/synaptic/`（算法层）+ `python/tests/wsc/`（测试）+ 本文档。
> 验证结论见文末「离线回放数据」；融入决策表见「融入 XEYO 的接线方案」。

---

## 一、它是什么

突触压缩把 Agent 的执行历史当成一张**不断分叉、验证、回滚的工作流图**来压缩，
而不是当成一段聊天记录来摘要。

目标不是「把历史变短」，而是：

> **把膨胀的执行历史，压缩成一份可继续工作的最小充分状态。**

四句话概括：

1. 压缩对象是**工作流状态**，不是对话文本；
2. 保留的不是最长历史，而是从当前目标反推出来的**依赖闭包**；
3. 剪枝不是删除，而是降级成「缺口 + 可展开句柄」；
4. 压缩阶段由引擎**确定性**完成，不依赖 LLM 做摘要；关系与字段来自输入消息的显式解析及固定启发式，不声称具备语义推断能力。

与 XEYO 既有 C0/C1/C2 的分野（一句话）：
**XEYO 用「位置 + KV 经济」决定留什么，WSC 用「依赖可达性」决定留什么。**

---

## 二、数据结构

| 结构 | 模块 | 内容 |
|---|---|---|
| 证据 DAG | `graph.py` | 节点 = 消息；四类边 `seq` / `use` / `file` / `err` |
| 文件状态表 | `filestate.py` | 观测 hash、已读区间、写后 `stale`、相关未解决错误 |
| 种子集（PIN 来源） | `seeds.py` | 目标 / 用户硬约束 / 未解决错误 / 当前 TODO / 工作集路径 |
| 剪枝卡 | `prune.py` | 结论 + 文件 + 错误签名 + 可重放命令 + `expand` 句柄 |
| 冷层 | `coldstore.py` | 句柄 → 原始文本，gzip 落盘，往返逐字节无损 |
| 热层装配 | `assemble.py` | PIN / WORKING SET / MAIN / DECISIONS / PRUNED /（可选）NEXT |

### 四类边

- `seq`（0.4）：时序相邻。
- `use`（1.0）：`assistant(tool_use) → tool_result`。**这是语法边**，对应生产链
  `memory/memindex.py` 里那张「只建表+写入、没有消费者」的 `edges`。
- `file`（0.5）：同一路径的相邻两次访问。
- `err`（0.8）：失败结果 → 之后首次触碰同一路径的调用。**这是启发式边**，
  见下文「已知弱点」。

> **refs 的来源分层**：`tool_use` 节点优先从入参 `path`/`file_path` 取；否则从
> 命令串扫路径；`tool_result` 节点优先从输出文本扫，**扫不到时继承发起它的调用**。
> 这一条不是细节——「这次结果关于哪个文件」由调用决定，不由输出文本决定。
> 早期版本漏了继承，导致失败现场抽不出路径、`err` 边整类消失。

---

## 三、压缩流程（9 步）

1. **建图**：`build_graph(messages)` —— 只记录，不判断重要性；
2. **文件状态**：`build_file_states(graph, messages)`；
3. **种子收集**：`collect_seeds(...)`；
4. **反向闭包**：`backward_closure(graph, seed_nodes, hops)` —— 沿 `in_adj` 反向 k 跳；
5. **五维加权**：`score_nodes(...)` —— goal_rel / unresolved / constraint / recency / replay_cost；
6. **确定性背包**：`knapsack_select(...)` —— 按「分数 ÷ **发射成本**」密度贪心；
7. **剪枝卡**：`build_cards(...)` —— 删重复留差异，带错误的卡永不丢弃；
8. **冷热迁移**：剪掉的进冷层并绑句柄，被骨架化的保留节点也绑 `node://<idx>`；
9. **组装**：`assemble(...)` —— 见下节两种模式。

### 一处关键记账修正（首轮冒烟实测发现）

预算必须按**发射成本**而不是**原文体积**记账。

初版用 `node.tokens` 计入背包预算，结果 PIN 段里那几条长用户消息按原文 token
把主链预算整块吃光（实测 `budget=0`，主链退化成只剩剪枝卡）。
现在用 `assemble.emitted_tokens`：已进 PIN 的节点 → 0；小节点内联 → 原文；
大节点 → 骨架行。同一条会话修正后 `budget` 从 0 → 317，`kept` 27 → 34。

### 三级降级粒度

| 粒度 | 落在热层的形态 | 代价 |
|---|---|---|
| 内联原文 | `#17 结论...` | 该节点完整 token |
| 骨架 + 句柄 | `#42 Read src/auth.ts 只读 expand(node://42)` | ~15 token |
| 剪枝卡 | `B37: Bash src/auth.ts 失败：… expand(branch://B37)` | ~25 token |

---

## 四、两种组装模式（KV 前缀的直接对撞点）

WSC 与 XEYO 最硬的技术冲突在这里：**依赖闭包不保证前缀连续**。

| 模式 | 行为 | KV 前缀 | 热层体积 |
|---|---|---|---|
| `closure` | 每轮全量重排 | **可能**断（见下） | 最小 |
| `append_only` | 稳定区字节冻结，新信息只追加增量行；追加预算超限才整层重建 | 结构性保证不断 | 单调变胖 |

`rebuilt` 的判据是严格定义的：**上一轮全文是否仍是本轮全文的字节前缀**。

这个判据带来一个反直觉但重要的实测结论：
**`closure` 并不总是打断前缀。** 当 PIN 段与工作集不变、只有区域在增长时，
新内容自然追加在尾部，`closure` 重排出来的全文恰好以旧全文为前缀，
KV 前缀**不 miss**。真正打断前缀的是中段变化——例如中途冒出一个新的未解决错误
（PIN 段变长），或者某个文件状态从「已读」翻成 `STALE`。

所以「closure 一定比 append_only 贵」是错的。贵的时刻是可枚举的：
**PIN / 工作集 / 卡片集合发生中段改写时。** 这也解释了为什么 WSC 必须把
`rebuilt` 当成一等指标报出来，而不是当成隐含假设。

---

## 五、已知弱点（不要当成已完成）

1. **`err` 边是启发式，不是语义。** 「读 A 之后才知道该改 B」这类语义依赖机器不可判。
   当前只做「失败现场 → 之后首次触碰同文件」的近似。这条边退化时，
   闭包会退化成「只有直连」或「全部可达」。
2. **「错误已解决」必须靠成功证据，不能靠路径重合。**
   实测反例：一条 `grep` 输出里提到 `src/auth.ts`，就足以同时满足
   「同工具（Bash）+ 同路径」——但那个失败的测试跟这次 grep 毫无关系。
   现在要求结果文本里出现成功标记（`passed` / `0 failed` / `exit code 0` …）。
   **误判「已解决」比误判「未解决」危险得多**：前者把失败现场从 PIN 里抹掉。
3. **PIN 集无上限。** 约束句多、TODO 多的会话里 PIN 会挤压主链预算。
   当前只做「overhead 超预算时告警 + 主链让位」。
4. **`weight` 与 `tokens` 严格分离。** 不要混用。生产链 `fidelity_segmenter.py`
   里的 `weight` 恒等于 `len(text)`，那是体积计权，不是价值计权。
5. **回放不含工具副作用、不含真实 KV TTL 分布。** 见文末口径。

---

## 六、与 XEYO 引擎铁律的冲突

引擎铁律（`AGENTS.md`）：**注意力里只出现信息，不出现导演。**

WSC 的设计文档里有一段 `[NEXT] 检查 proxy 配置`。内容本身是事实，但它在
注意力里的**位置与语气**会被读成导演（「接下来该做这个」）。因此：

- `WscParams.include_next` **默认 `False`**，该段默认不发射；
- 开启后报告必须标注，且不得作为默认配置进入主链。

同类的还有 `[DECISIONS] 已排除: …`。这条**保留**——它陈述的是「这条分支试过且
失败」这一事实，属于信息而非编排。分界线是：**陈述发生过什么 = 信息；
提示接下来做什么 = 导演。**

---

## 七、与设计文档的偏离（每一条都在代码注释里同步）

| # | 偏离 | 理由 |
|---|---|---|
| 1 | 剪枝卡 id 用根节点下标（`branch://B37`）而非流水号（`B12`） | 流水号随区域增长整体位移，`append_only` 模式下每轮都判定「内容变了」而被迫整层重建 |
| 2 | `[DECISIONS]` 与 `[PRUNED]` 不再重复同一条卡 | 设计文档示例里同一条分支在两段各出现一次，纯属重复计费。现在带错误的卡进 DECISIONS、其余进 PRUNED，各带句柄 |
| 3 | `[NEXT]` 默认关 | 见上节引擎铁律冲突 |
| 4 | token ≤ 48 的节点内联原文，不做骨架化 | 为省几十 token 逼模型发一次 `expand` 往返，净亏 |
| 5 | 新增三项设计文档没有的度量：关键信息针存活率、可恢复性往返、前缀连续性 | 把「98–99.5% 保留」这类自述变成可复核量 |
| 6 | TODO 只取**最后一次** `TodoWrite` 的未完成条目 | 任务树记录「现在要做什么」，不是「历史上一共写过哪些待办」 |
| 7 | 过滤引擎注入的伪用户消息（`[Resume]` / 系统提醒） | 它们不是用户意图，不能当目标或约束。未过滤时 PIN 里混入 6 条 Resume 指令 |
| 8 | 「错误已解决」要求成功标记 | 见「已知弱点」第 2 条 |

---

## 八、度量口径（报数前必读）

见 `python/synaptic/report.py` 的 `CAVEATS`。要点：

- 两侧 token 计量走同一套序列化（`memory.simulator.projection.emit_segment`），
  但 `wsc_tokens` 的尾部那部分与 `v61_tokens` 的尾部口径存在系统性差异，
  故 `reduction_vs_v61` 应看趋势而非绝对值。
- `hit_rate` 来自 `memory.simulator` 的 `ρ̂(C)` 模型，**不是**线上观测到的
  `prompt_cache_hit_tokens`。
- **任务成功率变化、重复错误率降低** 需要真实任务 A/B，本报告不出这些数字。
  `failure_site` 针存活率只是「失败现场是否还在热层」的代理指标。
- 回放按会话 JSONL 逐用户回合重演，`age_seconds` 固定 0（连续请求）。
- 尾部保护区用生产链同名的 `engine.compact.keep_tail_cut`，两侧完全一致。

### 关键信息针为什么不是同义反复

针从**原始历史**收割（用户消息 / 错误签名 / 文件路径 / 失败现场），
**不是** PIN 集的产物。所以「针在热层的存活率」检验的是 WSC 是否真的把关键信息
带过去了，而不是「PIN 塞满了就算完」。

---

## 九、融入 XEYO 的接线方案（待数据支持后再执行）

### 阶段 A（当前）— 纯离线旁路

- 算法层对生产链**零 import**，由 `tests/wsc/test_isolation.py` 机器执法；
- 只读 `~/.xeyo/sessions/*.jsonl`；零 API 花费、零生产改动。

### 阶段 B — 在线 shadow（只观察不生效）

- 接线点只有一个：`memory/runtime.py` 投影出口，开关 `XEYO_WSC=1`；
- shadow 只记录 `hot_tokens / rebuilt / 针存活率`，不改变实际发送的投影；
- 用途：拿到真实分布下的 `rebuilt` 率与 PIN 超预算率。

### 阶段 C — 并入主链

前置条件（缺一不可）：

1. 离线数据里 `closure` 的 `rebuilt` 率与命中率损耗已被量化，且确认
   可接受，或已改用 `append_only`；
2. shadow 里 PIN 超预算率 < 5%；
3. 走 `AGENTS.md` 第 4 条：新行为先以旁路形态上线验证收益，**有数据证明收益后才并入主链**；
4. 并入时在 T_now 块登记表标注归属与收益证据（若涉及模型可见注入）。

**在 1–4 全部满足之前，本模块保持旁路状态，不接线。**

---

## 十、怎么跑

```bash
cd python

# 全量离线回放（两种组装模式对照）
./.venv/Scripts/python.exe -m synaptic replay \
  --sessions-dir "$HOME/.xeyo/sessions" \
  --out ../_wsc_out --mode closure,append_only --sample-turns 12

# A/B 对照臂：关掉规则 2 的动态段落排序（回退固定先验序）
# 两臂必须写到**不同** --out 目录，否则报告互相覆盖
./.venv/Scripts/python.exe -m synaptic replay \
  --sessions-dir "$HOME/.xeyo/sessions" \
  --out ../_wsc_out/ab_off --mode closure,append_only --sample-turns 12 --no-stable-order

# 单会话投影明细（含热层全文与逐节点审计表）
./.venv/Scripts/python.exe -m synaptic single <session.jsonl> --show-hot --show-audit

# 静态检查（零 LLM 依赖 + 算法层零生产依赖）
./.venv/Scripts/python.exe -m synaptic check

# 前缀连续性探针（定位命中率根因，只读；见 11.8）
./.venv/Scripts/python.exe tests/wsc/_lcp_probe.py closure 3

# 测试
./.venv/Scripts/python.exe -m pytest tests/wsc -q
```

---

## 十一、离线回放数据

> 语料：`~/.xeyo/sessions`（317 个会话文件 / 119 MB）
> 采样：每会话最多 12 个用户回合
> 生成物：`_wsc_out/wsc_offline_report.md`（含 cohort 拆分与逐项口径）

### 11.1 总体（level = Medium+）

| 指标 | closure | append_only |
|---|---:|---:|
| 会话 / 回合 | 235 / 421 | 235 / 421 |
| 未压缩基线 token 中位 | 24983 | 24983 |
| 收益门拒绝压缩 | 18 回合（4.3%） | 20 回合（4.8%） |
| 压缩率（全回合，含拒绝）mean / median | **72.8% / 82.3%** | **70.9% / 80.2%** |
| 压缩率（仅实际压缩）mean / median / p90 | 76.1% / 83.0% / 95.2% | 74.5% / 81.3% / 94.6% |
| 相对 XEYO v6.1 mean / median | 54.0% / 72.7% | 44.8% / 72.7% |
| 热层 token 中位 / p90 | 1571 / 2989 | 1617 / 4782 |
| 命中率（ρ̂ 模型）WSC vs v6.1 | **0.031 vs 0.196** | **0.083 vs 0.196** |
| 输入成本合计（可比回合） | ¥2.89 vs v6.1 ¥8.49 | ¥2.87 vs v6.1 ¥8.49 |
| **整层重建率** | **42.3%**（178 次） | **14.5%**（61 次） |
| 压缩延迟 中位 / p95 | 14.70 ms / 158.29 ms | 15.51 ms / 169.45 ms |
| LLM 调用 | 0 | 0 |

`相对 XEYO v6.1` 的可比回合为 300（121 个回合的基线在回放期不可用）。
11 个回合因基线侧 `AttributeError` 降级（`baseline_degraded`）——这是**仓库既有缺陷**，
与 WSC 无关；回放选择降级而非丢弃会话。

### 11.2 关键信息针存活率（热层内命中）

针从**原始历史**收割，不是 PIN 集的产物，故该表检验「WSC 是否真把关键信息带过去了」。

| 类别 | closure 均值 | append_only 均值 | 最低（append_only） |
|---|---:|---:|---:|
| user（用户原话） | 94.6% | 96.9% | 22.2% |
| error_sig（错误签名） | 99.6% | 99.6% | 60.0% |
| path（历史全部路径） | 38.8% | 39.0% | 0.0% |
| path_recent（近 3 轮路径） | 46.6% | 46.7% | 6.7% |
| failure_site（失败现场） | 79.1% | 79.8% | 16.7% |

### 11.3 可恢复性（硬指标，无水分）

- 被剪节点 **22472**，其中有句柄 **22472** ⇒ 句柄覆盖率 **100.00%**；
- `expand` 往返逐字节比对 **35413/35413 无损**（closure）/ **35395/35395**（append_only）⇒ **100.00%**；
- 确定性探针：取最大 5 个会话重跑，`identical: 5 / mismatch: []`。

即「剪枝不是删除，是降级成缺口 + 可展开句柄」这一条**成立**。

### 11.4 四个必须直面的负向结论（并入主链前须解决）

1. **命中率崩塌，这是最严重的发现。根因已定位，见 11.8。**
   WSC 的 ρ̂ 命中率 0.031（closure）/ 0.083（append_only），远低于 XEYO v6.1 的 0.196。
   `closure` 每轮全量重排 ⇒ 前缀每轮都变 ⇒ KV 前缀命中基本归零；`append_only`
   靠冻结前缀把它救回 2.7 倍，但仍不到 v6.1 的一半。
   **代价被绝对 token 量的下降掩盖了**：总输入成本仍从 ¥8.49 降到 ¥2.89，因为
   送出的 token 少得多——但这是「用缓存复用换 token 数量」，不是纯赚。
2. **短会话无收益。** cohort 拆分显示 <30 消息的会话压缩率仅 32.8%/32.0%，
   收益门拒绝率却高达 19.5%/22.1%——因为区域本身就小，热层固定开销
   （PIN + 文件状态表）占比过大。**结论：WSC 只该在长会话上启用**，
   短会话应直接走现有 C0/C1。
3. **`path` 针存活率仅 38.8%，但这一项是度量口径问题而非缺陷。**
   「历史上提到过的每个路径都留在热层」既不可能也不该是目标——绝大多数路径
   在后续工作中就该被淘汰。真正该看的是 `path_recent`（46.6%）与
   `failure_site`（79.1%）：前者仍偏低（近 3 轮路径都保不住），**是需要修的真问题**；
   后者 0.0% 的最低值说明存在整轮丢失失败现场的回合。
4. **`rebuilt` 率 closure 42.3% vs append_only 14.5%。**
   结构上印证了设计文档的判断：`append_only` 才是 KV 友好的形态。若并入主链，
   **`closure` 不应作为默认模式**。

### 11.5 与设计文档预测的对照（不许自述当结论）

| 设计文档预测 | 实测 | 判定 |
|---|---|---|
| token 成本降 60–90% | 全回合 mean 72.8% / median 82.3% | ✅ 成立（区间下沿） |
| Medium+ 降 90–96% | 仅长会话（≥100 消息）87.6% mean 接近 | ⚠️ **仅对长会话成立**，全局达不到 |
| 即时信息保留 98–99.5% | user 94.6 / error 99.6 / path 38.8 / failure 79.1 | ❌ **不成立**，路径类针差距大 |
| 可恢复 99–100% | 句柄覆盖 100.00%、往返无损 100.00% | ✅ 成立 |
| LLM 调用 0 | 0（AST 静态执法） | ✅ 成立 |
| 压缩确定性 | 5/5 会话两跑逐字节一致 | ✅ 成立 |

一句话总结：**「零 LLM、可逆、确定性」全部兑现；「token 降幅」对长会话兑现；
「信息保留 98–99.5%」没兑现；而设计文档没有预测的 KV 命中率，是最大的坑。**

### 11.6 长度分层 cohort

| cohort | 会话 | 回合 | 压缩率均值 | 收益门拒绝 | 热层token中位 | 重建率 | 近期路径针 | 错误针 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| closure / 短（<30 消息） | 54 | 77 | 32.8% | 19.5% | 312 | 14.3% | 64.4% | 100.0% |
| closure / 中（30–99） | 121 | 159 | 75.1% | 0.6% | 961 | 30.8% | 43.9% | 100.0% |
| closure / 长（≥100） | 86 | 185 | 87.6% | 1.1% | 2827 | 63.8% | 44.1% | 99.5% |
| append_only / 短 | 54 | 77 | 32.0% | 22.1% | 320 | 0.0% | 62.8% | 100.0% |
| append_only / 中 | 121 | 159 | 73.1% | 0.6% | 976 | 1.3% | 43.9% | 100.0% |
| append_only / 长 | 86 | 185 | 85.3% | 1.1% | 2945 | 31.9% | 44.9% | 99.5% |

### 11.7 对阶段 C（并入主链）的影响

第 11.4 条使阶段 C 的**前置条件 1（`rebuilt` 率与命中率损耗已量化）**首次有了数字，
但结论是**当前不满足**：

- 命中率损耗**未被抵消**（0.031/0.083 vs 0.196）。根因已查清（11.8：热层首段 `[PIN]`
  含每轮必变内容，导致其后全部稳定内容每轮失缓）——**根因清楚 ≠ 可以并入**：
  修复只做了离线反事实测量，尚未落到算法上；
- 短会话必须排除，否则 20%+ 的回合被收益门拒绝、白付一次热层开销。

**下一步**（见 11.8）：修复方向已量化，但**尚未实施**；需先决策
「按原设计保持、只记录」还是「把 PIN 变动性修复做成变体后 A/B 复测」。

### 11.8 命中率崩塌的根因（已定位，纯离线探针）

探针 `python/tests/wsc/_lcp_probe.py`（只读，零花费）：取最大的 3 个会话，逐轮算
热层与上一轮热层的**最长公共前缀（LCP）**，并定位首个失配点落在哪一段。

结论（199 回合会话 `sess_real_200turn_c2`）：

| 观测 | 值 |
|---|---|
| `[PIN]` 段逐轮变化 | **199 / 199（100%）** |
| 首个失配点落在 `[PIN]` | **199 / 199（100%）** |
| 前缀连续性（LCP / 热层长度）mean / median | **2.2% / 1.9%** |
| LCP 中位 | 57 token（热层 1336 → 9619 token） |

**根因：`[PIN]` 排在热层最前面（offset 0），而 PIN 内含每轮必变的内容。**
逐类别的逐轮变动率：

| PIN 条目类别 | 逐轮变动率 | 来源 |
|---|---:|---|
| `指令`（`task_updates`） | **93%** | `seeds.py:208` —— **就是最近 N 条用户消息本身** |
| `todo`（`todos`） | 55–100% | 最后一次 `TodoWrite` 的未完成项 |
| `未解决`（`unresolved_errors`） | 6–33% | 未解决错误签名 |
| `目标` / `约束` / `当前目标` | 稳定 | —— |

**这是双重缺陷**：

1. **冗余**——`task_updates` 逐字等于最近的用户消息，而**未被保护的尾部本来就带着它们**。
   把尾部已逐字携带的内容再钉进热层，是纯重复计费；
2. **KV 敌对**——它位于 offset 0 之后的不远处，一旦它变，**排在其后的
   `[MAIN]` / `[DECISIONS]` / `[PRUNED]`（数千 token 的稳定内容）全部前缀失效**。

**反事实量化**（同一探针，只把 PIN 里 `指令:` 行删掉再算 LCP，不改算法）：

| | mean | median | LCP 中位 |
|---|---:|---:|---:|
| 现状 | 2.2% | 1.9% | 57 token |
| 去掉 `指令:` 行 | **16.9%** | **10.3%** | **274 token** |

即：**仅此一项就能把前缀连续性提升约 7.7 倍，进入 v6.1（0.196）的同一量级。**

**由此得到一条结构性规则（本设计应补的一条，设计文档原文没有）**：

> **热层段落必须按「前缀失稳率」升序排列**——最稳定的在最前（⚠️ 不是按「逐轮变动率」：
> 只追加的段落变动率可以很高而前缀完全稳，按变动率排会把它们排到最后，适得其反）；
> **并且凡未被保护的尾部已逐字携带的内容，一律不得进 PIN。**
> 违反它的代价不是「多花几百 token」，而是**其后全部内容每轮都失去缓存**。

第二个会话（`sess_mu0mkitk_5aehjt`）验证了规则的普适性：那里 `指令` 只变 25%
但 `todo` 变 100%，去掉 `指令` 行几乎无改善（1.6% → 1.8%）——**说明只要热层最前面的
任一内容在变，命中率就归零，与具体是哪个字段无关。**

### 11.9 修复后实测（规则 1 + 规则 2）——**未达标，第二处结构缺陷已定位**

11.8 的两条规则已落到算法上（不是只做反事实测量）：规则 1 = 后继用户消息不再进 PIN
（`seeds.py`，**修正默认行为**，非变体）；规则 2 = `[PIN]` 拆成
`[CONSTRAINTS]`/`[UNRESOLVED]`/`[TODO]` 并按**前缀失稳率**动态排序
（`assemble.py::order_segments`，开关 `WscParams.stable_prefix_ordering`，默认开）。
自检：`WscResult.churn / front_break / churn_warnings`，报告出「段落逐轮变动率自检」表。

**A/B 三臂**（同一语料、同一参数，仅开关不同；两臂写不同 `--out`）：

| 臂 | 规则 1 | 规则 2 | closure 命中率 | append_only 命中率 | closure 重建率 | append_only 重建率 |
|---|:--:|:--:|---:|---:|---:|---:|
| 修复前 | ✗ | ✗ | 0.031 | 0.083 | 42.3% | 14.5% |
| `ab_off` | ✓ | ✗ | **0.071** | **0.092** | 35.4% | 14.0% |
| `ab_on`（默认） | ✓ | ✓ | **0.067** | **0.092** | 36.3% | 14.0% |

对照基线 **XEYO v6.1 = 0.196**。压缩率基本未动（closure 73.0% / append_only 71.7% 全回合均值，
median 82.5% / 80.6%），可恢复性仍 100.00%，确定性 5/5，LLM 调用 0，延迟 median 10.1 / 11.2 ms。

#### 结论一：规则 1 有效但远不够；规则 2 **不能宣称有增益**

- 规则 1 是唯一有位移的改动：closure 0.031 → 0.071（**+0.040**），append_only 0.083 → 0.092（+0.009）。
- 规则 2 在聚合口径上**是平的**：closure **0.067（开）vs 0.071（关）**，append_only **0.092 vs 0.092**。
- 但**不能说规则 2 没用**——探针证明它把次序**收敛到了最优**。199 回合会话上采用的次序是
  `[CONSTRAINTS] → [UNRESOLVED] → [DECISIONS] → [WORKING SET] → [MAIN] → [PRUNED]`（170/199 回合稳定），
  而这正好是按该会话实测前缀失稳率（0% → 6% → 14% → 28% → 83% → 97%）**单调递增**的排列。
  次序已最优、命中率却没动 ⇒ **瓶颈不在「段落怎么排」，在「段落自己是不是稳的」**。

#### 结论二：第二处结构缺陷 = `[MAIN]` 不单调，稳定前缀被它截断

探针（`_lcp_probe.py`，199 回合会话）修复后数值：

| 观测 | 值 |
|---|---|
| 前缀连续性 mean / median | **18.6% / 11.6%**（修复前 2.2% / 1.9%） |
| LCP 中位 | **324 token**（修复前 57） |

**首个失配点**落在哪一段：`[MAIN]` **102**、`[WORKING SET]` 49、`[UNRESOLVED]` 24、
`[DECISIONS]` 20、`[PRUNED]` 4。

即：LCP 越过 `[CONSTRAINTS]`/`[UNRESOLVED]`/`[DECISIONS]`/`[WORKING SET]`（合计约 324 token）后，
**恰好停在 `[MAIN]`**。原因：`[MAIN]` 由闭包评分选出的 `kept` 集渲染
（`assemble.py::render_main`），**集会随评分逐轮进出**，所以它的**段首每轮都变**
（该会话实测 `[MAIN]` 变动率 90% / 前缀失稳率 83%）——尽管它承载的**内容**大部分是只追加的。

> **段落重排解决不了这件事**：`[MAIN]` 是体量最大的段，它自身不稳，任何排列都救不了它。
> 只有让 `[MAIN]` 变成**单调（append-only）**——已发出的行冻结，只追加、不重排、不摘除——
> 稳定前缀才可能从 ~324 token 往它的体量上延伸。

**这是假设，不是结论**：修 `[MAIN]` 单调性后的命中率必须再测一次才能报数。

#### 结论三：规则 1 开了一个信息空洞（**新发现，比命中率更该先修 —— 已修**）

规则 1 的前提是「未被保护的尾部已逐字携带」，但实测**这个前提只对最后 2–3 个用户回合成立**：

| 观测（199 回合会话，末轮） | 值 |
|---|---|
| `region_end` | 1373 / 1385 条消息 ⇒ **尾部只有 12 条** |
| 被压缩区域内的用户节点（节点级，`seeds.user_nodes`） | **188 条** |
| 同上，经 `harvest_needles` **去重后**的唯一文本数 | 17 条 |
| 修复前热层里可见 | **1 条**（只有「目标:」，即首条） |
| 热层里是否有「指令:」行 | **否**（规则 1 已删） |

机制（`assemble.py::render_main`）：它**跳过 `pin_nodes`**，而 `pin_nodes` 含**全部**实质用户节点
（`seeds.py`）。于是：

- 修复前：`task_updates`（「指令」段）是这些用户原话**唯一的渲染通道**；
- 修复后：通道没了，`[MAIN]` 又不渲染它们 ⇒ 区域内用户原话在热层里彻底不可见；
- 更糟的是它们是 `pin_nodes` ⇒ 不会被剪 ⇒ **连 `expand` 句柄都没有**，即**不可恢复**。

聚合口径的签名是两个臂一致的 **user 针存活率 94.6% → 71.0%**（−23.6pp）——两臂都应用了规则 1，
所以这是规则 1 造成的，与规则 2 无关。

**修复（`[REQUESTS]` 段）**：`assemble.py::render_requests`，节点集合由 `Seeds.user_nodes` 权威给出
（不是在 `assemble` 里重新按 kind 扫图），按 `idx` 升序发射、每条附真 `node://<idx>` 句柄，
句柄在 `project.py` 里绑进冷层。修后实测（同一会话末轮）：

| 观测 | 修复前 | 修复后 |
|---|---:|---:|
| `[REQUESTS]` 行数 | — | 187 |
| 节点级渲染覆盖 | 1 / 188 | **188 / 188（100%）** |
| 句柄往返无损 | — | **187 / 187** |
| `harvest_needles["user"]`（去重）可见 | 1 / 17 | **17 / 17** |

全语料（317 文件）节点级覆盖 = **20378 / 20378 = 100.00%**，见 11.10。

> ⚠️ **两个口径别混（这里踩过一次，造成一次误报）**：`harvest_needles` 的输出做 `dict.fromkeys`
> **去重**（集合语义：这条信息还在不在），而 `render_requests` / `seeds.user_nodes` 是**逐节点**语义。
> 曾看到「区域内实质用户消息 = 17」而 `[REQUESTS]` 渲染 187 行，当场判定「在把工具结果当用户原话
> 导出」——**是误报**。真值：本会话是**合成长会话**（同一组 17 条提问**重复 11 轮**，
> 1385 行 = 200 user + 605 assistant + 580 tool，**零**伪用户消息），所以 188 → 17 是预期。
> **判真伪只能看原文**：`python/tests/wsc/_user_nodes_probe.py <session.jsonl>`。
> 审计比率的分子分母必须同源 ⇒ `req_total` 用 `seeds.user_nodes`，不写 `kind == KIND_USER`。

> **规则 1 应该被「收窄」而不是回退**：只丢掉**尾部确实已经逐字携带**的那几条；
> 区域内的用户原话必须保留一条渲染通道。**该通道必须是 append-only 的**（只追加在段尾）——
> 只要它逐轮只在尾部追加，它自身的前缀就永远稳定；其代价是它增长时会把后面的内容挤出缓存，
> 所以放在**末尾**最省。

#### 结论四：自检表在 `append_only` 下口径不对（**自检自身的缺陷**）

两个组装模式报出的 churn 表**逐字相同**（`[MAIN]` 38.1%/19.6%、`[PRUNED]` 76.4%/76.4% …），
而 `append_only` 的语义是「稳定区字节冻结」——两者不可能同值。原因：`SegStat` 统计的是
**冻结前的重新渲染文本**（`_segment_groups` 产物），不是最终 `full_text`。

⇒ **在修好之前不要用这张表判断 `append_only` 的稳定性**，它对 `append_only` 系统性高估失稳。
（聚合表里 `[MAIN]` 前缀失稳率 19.6% 与单会话探针的 83% 差这么多，也有这个原因——
短会话把均值拉平了，长会话才是问题所在。）

#### 阶段 C 判定：**仍不满足，不并入**

按 11.8 定下的判据——「命中率回到 v6.1 同量级（0.15–0.20）才有资格谈融入」：

- 实测 **0.067 / 0.092 vs 0.196**，**差了 2–3 倍**，**未达标**；
- 而且这次修复**用信息留存换命中率**（user 针 −23.6pp）：0.071 的命中率换 23.6pp 用户原话缺失，
  **这笔交易不值**。

**下一步（按优先级，均需再测）**：

1. **先补信息空洞**：把区域内用户原话的渲染通道加回来，放在热层**最末**的 append-only 段。
   这是「信息纪律」问题，优先于命中率——丢用户原话比丢缓存严重得多。
2. **再修 `[MAIN]` 单调性**：已发出的行冻结，只追加。这是目前唯一有量级希望的命中率杠杆。
3. **顺手修自检口径**：`SegStat` 改统计最终 `full_text`（`append_only` 下即冻结文本）。
4. 重跑三臂，看命中率是否进入 0.15–0.20。

---

### 11.10 规则 8（日志布局）全量实测——**这是目前唯一有量级位移的改动**

11.9 的四条待办已全部落地：信息空洞补 `[REQUESTS]`（见 11.9 结论三）、`[MAIN]` 单调性由
**规则 8 日志布局**一次性解决（不再是「分段 + 重排」，而是**单一 append-only 日志**）、
自检口径改为「先定稿文本再统计」、报告新增节点级覆盖率与 steady 命中率。

**跑法**（两臂写不同 `--out`，`sample_turns=0`，逐用户回合）：

```bash
cd python
./.venv/Scripts/python.exe -m synaptic replay --sessions-dir "$HOME/.xeyo/sessions" \
  --out ../_wsc_out/arm_journal2 --mode closure
./.venv/Scripts/python.exe -m synaptic replay --sessions-dir "$HOME/.xeyo/sessions" \
  --out ../_wsc_out/arm_seg2 --mode closure --no-journal
```

语料 317 个会话文件；入样 235 会话 / 651 回合 / 82 跳过（`too_short`）/ 12 会话基线降级
（既有 `SimpleNamespace` 缺 `compact_checkpoint`，非 WSC 引入）。level = **Medium+**。

| 指标 | 分段（`--no-journal`） | **日志（默认）** | 变化 |
|---|---:|---:|---|
| `hit_rate_wsc` mean（含冷启动，n=302） | 0.0674 | **0.1345** | **2.00×** |
| `hit_rate_wsc_steady` mean（跳首回合，n=85） | 0.2394 | **0.4778** | **2.00×** |
| `hit_rate_v61_steady`（同 85 回合） | 0.6759 | 0.6759 | — |
| `lcp_prev_tokens` median / mean | 79 / 670 | **1694 / 3170** | **21.4× / 4.7×** |
| `rebuild_events` / `rebuild_rate` | 366 / **56.2%** | 20 / **3.07%** | **18.3× ↓** |
| `journal.zero_loss_turns` | 633 / 633 | 613 / 633 | 96.8% 零前缀损失 |
| `hot_tokens` median | 2759 | 3226 | **+16.9%** |
| `wsc_tokens` median | 5252 | 6610 | **+25.9%** |
| `reduction_vs_base` median | 87.9% | 85.7% | −2.2pp |
| `reduction_vs_base_compressed` median | 88.4% | 86.0% | −2.4pp |
| `cost_wsc / cost_v61`（302 可比回合） | 0.3354 | **0.3277** | −2.3% |
| `cost_wsc / cost_v61`（85 steady 回合） | 0.8653 | **0.7535** | **−12.9%** |
| 用户原话覆盖（节点级） | 20378/20378 | 20378/20378 | 均 **100%** |
| 可恢复性 coverage / `expand` 往返无损 | 100% / 100% | 100% / 100% | 均满 |
| 确定性（5 会话两跑逐字节） | 5/5 | 5/5 | 均满 |
| LLM 调用 | 0 | 0 | 均 0 |

关键信息针存活（**去重口径**，journal vs seg）：`user` 1.0000 vs 1.0000、`error_sig` **0.9994** vs 0.9865、
`failure_site` **0.8489** vs 0.8314、`path_recent` 0.4719 vs 0.4558、`path` 0.4289 vs 0.4121。
即日志布局在**同口径下信息留存没有退步，几项还更好**。

#### 结论一：规则 8 在两种口径上都给出 **2.0× 命中率**，不是单口径巧合

`hit_rate_wsc` 与 `hit_rate_wsc_steady` 两处都恰好是 2.00×，方向一致。
更硬的证据是 `lcp_prev_tokens` median **79 → 1694（21.4×）**与 `rebuild_rate` **56.2% → 3.07%（18.3× ↓）**——
后者说明分段布局有一半以上回合在「整层重建」（前缀完全归零），而日志布局只有 3.07%。

#### 结论二：代价是明确的，但**净成本仍下降**

投影 token median **+25.9%**、压缩率 **−2.2pp** —— 这是「日志变胖」的必然代价，
而且**命中率可以靠不压缩买到，压缩率也可以靠不管命中率买到**，两者在这里是对立的：

- 302 可比回合口径：成本比 0.3354 → **0.3277**（−2.3%），命中率提升刚好抵消 token 增量；
- **85 steady 回合口径：0.8653 → 0.7535（−12.9%）** —— 在缓存真正能起作用的回合上，日志布局的收益大得多。

#### 结论三：WSC 目前**打不过 v6.1 的命中率，但成本已经压到 1/3**

| | WSC journal | XEYO v6.1 |
|---|---:|---:|
| `hit_rate_steady` mean | 0.4778 | 0.6759 |
| `cost`（302 可比回合） | ¥2.7952 | ¥8.5295 |

即 WSC 的定位是**「用更少的 token 换略低的缓存复用」**，不是「命中率打平」。
**报「命中率低于 v6.1」时必须同时给成本**，否则会把这笔交易误读成退步。
（命中率两侧的分子分母结构不同：WSC 的 `L` = 热层 + 未触碰尾部；v6.1 的 `L` = C0/C1/C2 投影。
只有 `cost_*_total` 是在**同一批可比回合上求和**，可以直接比。）

#### ⚠️ 口径警示（本轮报告里最容易读错的三处）

1. **`hit_rate_*` 的 median 在三个 cohort 里全是 0** —— 冷启动稀释：每个会话首个可比回合没有上一轮投影，
   命中必然为 0，而本语料 235 会话 / 651 回合 ≈ **2.8 回合/会话** ⇒ 首回合约占 1/3。
   **报绝对水平必须用 `*_steady`**；原口径保留（它是含冷启动的真实平均）。
2. **`steady`（n=85）偏向长会话** —— 只有「≥2 个可比回合」的会话才贡献，与含冷启动口径**不可互换**。
3. **本表的 `latency_ms` 不可用**：两臂**并发**执行，CPU 争用把延迟抬了约一倍
   （mean 119.6 / 119.2 ms，而**串行**跑是 83.5 / 78.5 ms）。可信延迟见 11.9。

#### 已知未解（按影响排序，均需决策后再动）

1. **`[REQUESTS]` 段不在任何预算内。** 它按区域内用户节点数**线性增长**，
   单会话实测占热层 **55.5%（3664 / 6599 tok）**；而 `hot_budget_tokens`（Medium+ = 3000）
   只闸 `kept` 的选择，`overhead = pin_tokens + ws_tokens` **不含** `[REQUESTS]`/`[DECISIONS]`/`[PRUNED]`。
   ⇒ 实测热层 median 3226 **超预算 7.5%**，长会话超得更多。
   **这是账目缺口**：把 `[REQUESTS]` 计入 `overhead` 会挤掉 `[MAIN]`（那才是真正的取舍），
   需要先决定「用户原话 vs 剪枝卡」谁优先。
2. **`path` / `path_recent` 针存活率仅 ~43–47%**，是剩下最大的信息留存缺口。
   长尾路径（早期读一次、之后再没碰过）**本来就应该丢**（冷层可 expand），
   但「还在用的路径」只留住 47% 偏低。
3. **分段布局的段间排序仍是开问题**：正确的目标函数见 `wsc-offline-eval` 技能 §4.5
   （早先「只追加的段排前面无害」的表述是错的）。但对默认的日志布局**无影响**，故优先级最低。

#### 阶段 C 判定：**命中率仍未达标，但成本已显著胜出 —— 建议改为「按成本准入」**

11.8 定的判据是「命中率回到 v6.1 同量级（0.15–0.20）才有资格谈融入」。本轮 journal 布局
steady 命中率 0.4778 —— **已进入并超过该区间**（0.15–0.20 是以含冷启动的均值口径定的，两者不可直接比；
同口径下 0.1345 vs 0.1976 仍差 1.47 倍）。

真正的判据应该是**成本**：`成本 ∝ H + 30·U = 30L − 29H`，命中率每提高 1pp 都直接换成钱，
而压缩率只通过 `L` 起作用。本轮结果：

- 302 可比回合：**¥2.7952 vs ¥8.5295 = 32.8%**；
- 85 steady 回合：**75.4% vs 100%**（只省 24.6%）。

⇒ **准入建议**：以 `cost_wsc / cost_v61 < 1.0` 为门槛，**分 cohort 判定**——
短/中会话收益巨大（省 69%/74%），长会话仅省 ~25%。是否上线应由「线上会话长度分布」
决定，而不是拿一个全局平均值拍板。

---

## 十二、第五轮：串行基线 + 延迟 P0（2026-09-15）

> 本轮口径变化（**先读**）：①验收改用「串行、单进程、背靠背」的延迟口径；
> ②语料是 **live** 的（`~/.xeyo/sessions` 每回合都在增长，第四轮 235 会话/651 回合 →
> 本轮 236/653）⇒ 跨批次的历史数字一律标注不可比；③旧命中率口径**不再作为本轮 KPI**。

### 12.1 起点与归属澄清（与交接提示词的差异）

- `python/synaptic/` **已在 git 内**（`2886850 chore(wsc): 纳入突触压缩旁路包基线…`），
  不再是「三条路径全 untracked」。因此可以 `git diff` 拿到历史版本。
- 接手时工作树里有一批第四轮在途改动（13 文件 +241/−94，含新文件 `budget.py` /
  `test_budget.py` / `test_path_survival.py`），**已全绿（91 passed）**。经用户裁定：
  **以当前工作树为第五轮基线**（不回退），并在 `_wsc_out/baseline_20260915_r5pre/`
  留档（src sha1 + diff + 两臂报告 + `BASELINE.md`）。

### 12.2 串行基线（level=Medium+，mode=closure，两臂顺序执行）

| 指标 | journal | segmented |
|---|---:|---:|
| 会话 / 回合 | 236 / 653 | 236 / 653 |
| 压缩率 median（vs base） | 86.53% | 88.77% |
| 压缩率 median（仅压缩回合） | 87.05% | 89.46% |
| 重建率 | **2.91%** | 56.97% |
| hot / wsc median | 2,713 / 5,871 | 2,060 / 4,220 |
| 延迟 p50 / p95（回放内） | 68.9 / 482.2 ms | 69.7 / 538.4 ms |
| 针存活 user / error_sig / failure_site / path / path_recent | 1.000 / 0.9987 / 0.9226 / 0.6106 / 0.6946 | 1.000 / 0.9946 / 0.9175 / 0.5993 / 0.6909 |
| 用户原话节点覆盖 | 20381/20381 = 1.000 | 1.000 |
| 可恢复性 / 确定性 / LLM 调用 | 1.0 / 5-5 / 0 | 1.0 / 5-5 / 0 |

**口径更正**：第四轮中途报告（`arm_journal4b/c/final`）里的 `user_requests.coverage = 0.2834`
是**旧修订的伪值**；同一份代码复跑为 1.0000，标准会话逐回合亦为 18735/18735 = 1.000。
本轮不接受 0.283 这个数字。

### 12.3 延迟优化 P0：先说 profile，再说改动

200 回合标准会话（8 个末轮）cProfile 的 top 热点（tottime，共 9.64 s）：

| 排名 | 热点 | 耗时 | 根因 |
|---|---|---:|---|
| 1 | `closure.keywords` | 2.51 s | 每轮对**整段历史**逐节点重跑正则，而相邻两轮输入只差最后几条消息 |
| 2 | `graph.outgoing(kind=…)` 的生成器 | 1.98 s | 每次调用**线性扫全部边** ⇒ O(边数 × 调用数)（12,624 次调用） |
| 3 | `closure.score_nodes` 自身 | 0.45 s | 上述两者的调用方 |
| 4 | `textutil.extract_paths` | 0.45 s | 同样逐节点重算（14,036 次） |
| 5 | `graph.build_graph` 自身 | 0.26 s | 每轮从零重建图（增量 Graph 属 P1） |

**改动（全部是「同结果、少做功」型）**：

1. 新模块 `synaptic/memo.py`：定容 FIFO 记忆表（进程内、有界、不落盘）。
2. `closure.keywords` / `textutil.extract_paths` 记忆化 —— 纯函数，结果只取决于输入文本。
3. `graph.py`：新增按边类型预索引的 `out_kind` / `in_kind`，`outgoing/incoming(kind=…)`
   由线性扫描变 O(1) 查表（保留线性回退分支，行为与原来逐元素相同）。
4. `metrics.ALGORITHM_MODULES` 登记 `memo.py`（`test_isolation.py` 的模块清单执法）。

**注意**：`__init__.py` 的包导出、`types.WscParams` 字段、报告口径均未改；
所有改动都是**确定性等价**的——下节是机器验证，不是自述。

### 12.4 验证：指标零位移，延迟减半

同一语料、同一参数、背靠背两跑（`arm_journal5_baseline` vs `arm_journal5_p0p2`）：

| 指标 | 改前 | 改后 | Δ |
|---|---:|---:|---:|
| 压缩率 median | 0.8653 | 0.8653 | **0.0000** |
| hot / wsc median | 2,713 / 5,871 | 2,713 / 5,871 | **0** |
| 重建率 / 收益门跳过率 | 2.91% / 2.76% | 2.91% / 2.76% | **0** |
| 针存活（五类） | 见 12.2 | 见 12.2 | ≤0.001（语料漂移） |
| 可恢复性 lossless | 1.0 | 1.0 | 0 |
| 确定性 | 5/5 | 5/5 | 0 |
| 回放内延迟 p50 | 68.93 ms | **29.25 ms** | **−39.7 ms** |
| 回放内延迟 p95 | 482.17 ms | **208.02 ms** | **−274.2 ms** |

专用串行计时器（`_wsc_out/_lat_harness.py`，纯压缩 `project()`，两遍取第二遍）：

| 口径 | 改前 | 改后 |
|---|---:|---:|
| 纯压缩 p50 / p95 | 67.6 / 477.6 ms | **22.1 / 208.7 ms** |
| 冷启动（会话首个压缩回合）p50 | 13.4 ms | **5.0 ms** |
| 热（同会话后续回合）p50 | 144.8 ms | **58.7 ms** |
| 长会话 cohort（≥100 消息）p50 | 152.4 ms | **60.9 ms** |
| 全语料两遍 wall | 88.5 s | **40.3 s** |

⇒ 纯压缩 p50 已进 **22 ms**（DOD 目标 ≤20 ms，差距 2 ms）；p95 208 ms 仍远高于 100 ms 目标。
**下一步（P1）**：增量 Graph（只 append 新消息）+ 增量 Assembly（未变 segment 复用上一轮字符串）
+ `score_nodes` 的候选集收窄——1.98 s 的边扫描已消掉，剩下的 0.45 s 级热点要靠增量结构。

### 12.5 本轮未完成（明确挂账，不许当已完成）

1. **path / path_recent 仍是最大信息缺口**（0.6106 / 0.6946，目标 0.75 / 0.85）——
   P0-1（路径前缀索引 + 强制配额）尚未动手。
2. **`[REQUESTS]`/`[DECISIONS]`/`[PRUNED]` 计入 overhead**：用户已裁定「全部计入并如实记
   over-budget」，当前 `budget.py` 只把固定段与主链拆成两闸，`[DECISIONS]`/`[PRUNED]`
   仍未入账——需在 P1 补齐并把超预算量写进报告。
3. **触发口径与生产同源**（`prompt_tokens ≥ 0.8 × context_limit`）未改，128k 负向结果未修。
4. **事件级多事件对比**仍不可用（三处偏差未修），本轮一切事件级结论仍只取首事件。
5. **市面对比**：用户提供的是**公开信息表（口径不一）**，本轮**未实测** Codex / Claude / DSH，
   报告里必须标注「未实测」，禁止写「已超过」。
6. **串行端到端净延迟**已测（见 12.2/12.4 的 e2e 口径，p50 69.7→29.5 ms），但**不含真实
   网络与 KV TTL**；生产链影子对照（Phase 1）未做。

### 12.6 P0-1 落地：`[PATHS]` 路径索引 + 强制配额（path 缺口已补）

**改动**：新模块 `synaptic/paths.py`（`H_PATHS` / `shortest_unique_suffix` / `render_paths`），
接入 `assemble._segment_groups` + `_SECTION_PRIOR` + `fixed_headers`（即计入固定段预算），
参数 `WscParams.path_index_limit`（默认 24 条）/ `path_index_budget_tokens`（默认 256）。

**为什么是「通道不够」而不是「预算不够」**：区域内路径原先只有三条渲染通道——
`[WORKING SET]`（≤12 条文件状态）、`[MAIN]` 骨架行（只取 `node.refs[0]` 且只对 `kept` 节点）、
剪枝卡（只对**被剪**节点）。「最近碰过、但既没进 working set 也没进 kept」的路径因此
在热层里**没有出口**。`[PATHS]` 是第四条通道，配额内必然发射，不受闭包/背包影响。

**两个刻意的设计**：①渲染成**最短唯一后缀**（`src/auth.ts` → `auth.ts`，同名自动加长到唯一），
后缀唯一性以同池为全集判定；②发射顺序 = **首次出现下标升序**（不是「最近优先」），
新路径只追加在段尾 ⇒ 段内不会因为「某条路径又被碰了一次」而整体重排，KV 前缀只在追加时延长。

**实测（同语料，journal 臂，背靠背三跑；注意语料 live：baseline 653 回合 → P0-1 654 回合）**：

| 指标 | baseline | P0-2 | **P0-1** | DOD |
|---|---:|---:|---:|---:|
| `path` | 0.6106 | 0.6107 | **0.8432** | ≥0.75 ✅ |
| `path_recent` | 0.6946 | 0.6947 | **0.9137** | ≥0.85 ✅ |
| `failure_site` | 0.9226 | 0.9236 | **0.9534** | ≥0.95 ✅ |
| `user` / `error_sig` | 1.0 / 0.9987 | 1.0 / 0.9987 | 1.0 / 0.9987 | 维持 ✅ |
| 压缩率 median | 0.8653 | 0.8653 | 0.8574 | ≥0.88 ❌ |
| hot median | 2,713 | 2,713 | 2,918 | — |
| 可恢复性 / 确定性 | 1.0 / 5-5 | 1.0 / 5-5 | 1.0 / 5-5 | 保持 ✅ |
| 延迟 p50 / p95（回放内） | 68.9 / 482.2 | 29.3 / 208.0 | 30.3 / 203.2 | — |

**代价与收益的账**：+205 hot token（+7.6%，落在 P0 预估的 +3–8% 内）换 path **+23.3pp**、
path_recent **+21.9pp**、failure_site **+3.1pp**；压缩率 median 因此 −0.79pp（0.8653 → 0.8574）。
**这三个针的缺口是本轮最大的一项，用 0.8pp 压缩率换回来是明确合算的**——但压缩率本身
仍未达 0.88 目标，需要 P1（`[REQUESTS]` 分层 / 工具结果 CDC 去重）把它补回来。

**回归测试**（`tests/wsc/test_path_survival.py`，与 91 项旧测试一起 **94 passed**）：
最短唯一后缀消歧、`[PATHS]` 必发且不超条数上限、token 上限优先于条数上限（预算 0 ⇒ 不发）。

### 12.7 P1-a（`[REQUESTS]` 分层）——**负向结果，必须保留**

**动机**：标准 200 回合会话的紧凑渲染账目显示 `[REQUESTS]` 是最大的一段
（med **1565 tok/回合**，占总量 med 3526 的 44%，且随用户回合数线性增长）。

**改动**：`WscParams.request_recent_verbatim`（默认 3）/ `request_excerpt_chars_old`（默认 80，
硬地板 80 = 针的判定长度），`budget.render_requests*` 按节点分层取摘录长度，
组句柄取组内最宽松档。

**实测（P0-1 → P1-a，背靠背）**：

| 指标 | P0-1 | P1-a | Δ |
|---|---:|---:|---:|
| 压缩率 median | 0.8574 | 0.8579 | **+0.0006** |
| wsc median | 6,014 | 6,001 | **−13 tok** |
| hot median | 2,918 | 2,908 | −10 |
| 五类针 | 1.0/0.9987/0.9534/0.8432/0.9137 | 1.0/0.9987/0.9531/0.8431/0.9135 | 持平 |

**结论（负向）**：**分层摘录在本语料上几乎不产生收益**。
单会话账目复核：`[REQUESTS]` med 仍是 **1565 tok**，一格没动。

**根因**：摘录上限（320 → 80 字符）**只在原话长于上限时才起作用**，而本语料的
用户原话普遍短于 80 字符 ⇒ 两档摘录输出**完全相同**。
⇒ 「`[REQUESTS]` 太大」的真实成因不是摘录长度，而是**行数 × 每行固定开销**
（`#<idx> 用户: … expand(node://…)` 的句柄与头部约 10–15 tok/行，93 行/回合）。
**要压它必须动「每行的句柄形态」，不是动字符上限**——落点应是把旧节点合并成
**区间句柄**（如 `expand(range://5-120)` 或按 10 条一组的组句柄），而不是继续调字符数。

**同批确认的第二件事**：`[PRUNED]` med **1221 tok** / `[MAIN]` 563 / `[WORKING SET]` 181 /
`[DECISIONS]` 73 — 即长会话热层里**卡片段（~1292）与 REQUESTS（~1565）几乎各占一半**。
压缩率要往 88–90% 走，只能从这两段取，且必须先解决「卡片结论重复」与「每行固定开销」。

**另需记住的口径事实**：本语料 653 回合 / 236 会话 ≈ 2.8 回合/会话 ⇒ **中位回合落在中短会话**，
那里 `wsc = 热层 + 原样尾部`，尾部不可压 ⇒ 压缩率中位数受**热层绝对值**支配，
而「百分比收益」在大会话上才大。任何换参数的取舍都必须同时报 median 与 cohort，不能只报一个数。

---

## 十三、第六轮：P1-b 接线 + 两项必查（2026-09-15）

### 13.1 P1-b 区间句柄接线（代码侧已实测）

四步接线 + **接线时才暴露的两个缺陷**（原语从未被调用，所以一直潜伏）：

1. `assemble._segment_groups`：`render_requests(...)` → `render_requests_compact(...)`。
2. `project.py`：新增按 `request_chunk_ids` 的绑定循环，`cs.bind(reqs_handle(first, last), idxs)`。
   **只绑多节点块**——单节点块渲染成 `node://<idx>`，已由逐节点循环绑好。
3. `types.for_level()`：显式透传 `request_recent_verbatim` / `request_excerpt_chars_old` /
   `request_old_group_size` / `path_index_limit` / `path_index_budget_tokens`。
4. **缺陷①（真 bug）**：`budget.py::rendered_request_nodes` 调用 `parse_reqs_payload`
   却**没有 import**。P1-b 原语此前从未被接线执行，所以 `reqs://` 分支从未跑到 ⇒ NameError 潜伏。
   接线后第一次跑测试即红（`test_budget.py` 两条 + 新测试三条）。已修。
5. **缺陷②（漂移点）**：`skip` 集合（首个 pin 节点）原先在 `assemble.py` 与 `project.py`
   **各写一份表达式**。渲染与绑定一旦漂移，就会出现「行里写的区间」与「句柄展开的节点集」
   不一致——而且往返比对**照样通过**（句柄自洽地错），是可恢复性被悄悄破坏的形态。
   已收敛为 `synaptic/seeds.py::request_skip(seeds)` 单一实现。

**回归测试**（`tests/wsc/test_request_chunks.py`，5 条）锁四件事：
①区间句柄往返逐字节无损；②区间 == `request_chunk_ids` 的分块（渲染↔绑定同源）；
③审计认区间（`req_rendered == req_total`）且 `user` 针仍 1.0；④紧凑形态行数远少于逐节点形态。
另：`test_budget.py` 两条旧断言（依赖「会话自然会撞破固定段预算」）在紧凑渲染后不再成立，
已改为**显式压低预算**驱动同一条降级阶梯——**不得删测试，只能改驱动方式**，
否则「机制没人守但测试还是绿的」。

### 13.2 必查 A：「上下文预算」的归属 —— ⚠️ **第七轮更正：真信号 + 错作用域**

**结论：它不是 XEYO 的机制；但触发它的信号是真的——错的是作用域标注。**

全仓检索 `上下文预算` 只命中 docs（通用概念）；引擎里叫「预算」的只有两处且都与
上下文窗口无关——单请求回合预算（当时经 wrap-up 窗两块送入模型）与服务端 goal 轮预算
（`server/goal_round_driver.py:284`）。

**第七轮更正（推翻本节旧措辞）**：旧文写「**[上下文预算已耗尽] 措辞冒充了机制**」——
这是**归因不准**，且把防治手段指错了方向。真相比这更严重、也更可修：

- 引擎**确实**向模型注入了"预算已尽"（`# Wrap-up(预算已尽)` / `# Runtime budget notice`；
  两块都是 `directive` 类 ⇒ 预算内绝不裁剪 ⇒ **必然进注意力**）；所以模型不是"凭空编了个机制"。
- 但 `TurnBudget`（`engine/budget.py`）里**混装三种作用域**：回合级
  （`max_turns` / 工具配额 / `grace_turns_remaining`）、任务级（`wall_deadline_ts`）、
  会话级（`used_usd` / `usd_limit`）。
- 块文本**只说"预算"、不说是哪一种** ⇒ 模型**只能自己猜**。第五轮猜成「上下文窗口」、
  第六轮**再次**猜成「上下文窗口」（两次均为第一手复现）。

⇒ 正确表述是 **真信号 + 错作用域**（不是"措辞冒充机制"）。
⇒ 正确处理**不是**"提醒后续 agent 不得引用"（那只是把事故推迟），而是
**撤块，或让块名自证作用域**（若要保留文本，须写「回合配额」而非「预算」）。

第七轮已按引擎铁律「限制只在执行层 / 能静默就不说话」**撤销这两块**——渲染器与常量
**物理删除**（不是关开关），预算机制本身全部保留并由 `engine/query_loop` 强制。
详见 §14.2。

### 13.3 必查 B：`env-notice` 缺口（第六轮第一手复现，附更正）

**第六轮实录（本轮实测，非转述）**：本会话里模型发起了一次空参数的
`xeyo_env_notice` 调用，**换回了完整的 `[system-environment]` 正文**
（含 `# Continue` 块与 `# Resume` 块，`Resume` 里带着**原始目标全文**）。
⇒ 与第五轮同形：**模型发起的保留名调用确实拿到了环境正文。**

**但归因要更正一处**：本轮同时观察到——该正文块**在普通工具结果后也会出现**
（`Bash` / `Read` / `Grep` 之后同一份背景被反复注入）。因此在本会话里
**「保留名被应答」与「引擎本该逐轮注入同一份正文」在模型视角不可区分**；
把前者当成「存在一个外部应答器」是**外推，不是实测**。可确定的是两条：

1. **仓库内部本来就拒**：`tool_registry.run` 对未注册名返回中性 `unknown tool: {name}`
   （`is_error=True`），从不派发、无副作用——这条路是安全的；
2. 全仓 grep `xeyo_env_` 只命中 `prompt/` `model/` `evals/` `tests/` `docs/`，
   **无 server/host 处理器** ⇒ 若真存在应答者，它在仓库外（桌面 host / 中继层），
   本轮**未能定位**（无 host 代码位置），故该项标记为「**未定位**」而非「已定位」。

**第七轮补记（把「未定位」推进到「行为级已定位」）**：第七轮同一会话内
`xeyo_env_notice` 幻觉调用 **89 次**（含多次整条响应体只有该调用），并且——
**引擎把这类调用当成真实工具轮应答**：回灌 `# Continue（工具结果后）`。
⇒ 应答者仍在仓库外，但**其行为已可观测、可复现**：它是
「伪对 → 模型调用 → 应答 → 新 `tool_use` 先例」这条**自催化闭环**的中间一环。
⇒ 消除方式**不是**劝模型别调（意图抑制已被 89 次证伪），而是**换承载形态**（§14.1）。

**代价（本轮直观可见）**：每次这类调用会把整段背景（含原始目标全文，约 7k token）
重新灌进注意力，并把「模型幻觉调用」伪装成「引擎注入」——第五轮因此**两次误导审计**。

**本轮仓库内可做且已做（深度防御，把巧合变契约）**：

- `tools/tool_registry.py`：新增 `RESERVED_TOOL_PREFIX = "xeyo_env_"` 与
  `run()` 里的**保留前缀拒绝**（措辞中性：`reserved environment channel: {name}`），
  且**拒绝发生在注册表查询之前**——即使有人真把工具注册成保留名也走不到它；
- `tests/test_reserved_tool_prefix.py`（4 条）：保留前缀与
  `prompt.t_now_strategy.ENV_ID_PREFIX` **同源断言**（防漂移）、拒绝早于查询且无副作用、
  该名字永不进 `schemas()`、**引擎自身 env 投影照常构造**（拒绝规则不得误伤）。
- 该项是**旁路加固**，与 WSC 无关，独立提交。

### 13.4 第六轮数字（P1-b 后）

口径：`level=Medium+`、`mode=closure`、journal 布局、单进程**串行**、语料 `~/.xeyo/sessions`（**live**）。

| 指标 | p0p1（第五轮） | **P1-b（本轮）** | Δ |
|---|---:|---:|---:|
| 会话 / 回合 | 236 / 654 | 237 / 663 | 语料已增长 |
| 压缩率 median | 0.8574 | **0.8621** | **+0.47pp** |
| 压缩率 median（仅压缩回合） | 0.8649 | 0.8684 | +0.34pp |
| hot median | 2,918 | **2,818** | **−100** |
| wsc median | 6,014 | **5,901** | **−113** |
| 重建率 | 0.0306 | 0.0317 | +0.11pp |
| 回放延迟 p50 / p95 (ms) | 30.32 / 203.2 | 30.16 / 209.1 | 持平（p95 +5.9） |
| 可恢复性 lossless / 确定性 | 1.0 / 5-5 | 1.0 / 5-5 | 持同 |
| 用户原话节点覆盖 | 1.0 | 1.0 | 持同 |
| error_sig / path / path_recent / failure_site | 0.9987 / 0.8432 / 0.9137 / 0.9534 | 0.9988 / 0.8386 / 0.9114 / 0.9489 | 噪声级波动 |
| **`user` 针（文本可见性）** | **1.0000** | **0.8320** | **−16.8pp ❌** |

**⚠️ 阻断级负向结果：`user` 针从 1.0000 掉到 0.8320。**
根因清楚且是结构性的：`render_requests_compact` 对**旧块只发区间句柄、不发摘录**
（只有最近 `request_recent_verbatim=3` 条逐字可见）。于是旧用户原话**可从冷层无损拉回、
但不再在热层文本里可见**——`user_requests.coverage` 仍 1.0（有出口），
`needle_survival.user` 掉档（看不见）。**这正是「可恢复 ≠ 可见」的口径分叉**，
第五轮 P1-a 的 80 字符地板就是为了守住它，P1-b 把地板整个拿掉了。

**⇒ 结论（必须显式保留）**：P1-b **不是 DOD-clean 的改动**。
它用 +0.47pp 压缩率换掉了 16.8% 的唯一用户原话可见性。
按 DOD（`user` 针必须保持）**不得直接并入**；要拿到行数收益又不丢针，落点应是
**「每块保留首节点的一条 80 字符摘录 + 区间句柄」**（行数仍降 ~8 倍，
块内其余节点靠 `expand(reqs://)` 拉回），而不是整块只留句柄。

**口径警告（本轮踩到）**：上表 p0p1 与本轮**不是同批**——语料是 live（236→237 会话、
654→663 回合）。<0.1pp 的差异不可测；本次 +0.47pp 增益与 −16.8pp 针损失都远大于该噪声，
但**严格 A/B 仍需同批背靠背**。

#### 13.4.1 同批背靠背（**权威口径**，本节的数以此为准）

用冻结的改动前快照代码（`_wsc_out/baseline_20260915_r6pre/src`，`_r6_pre_arms.py`）
与改动后代码在**同一批语料、同一台机器、单进程串行**下背靠背各跑一遍：

| 指标 | pre（改动前） | **P1-b** | Δ |
|---|---:|---:|---:|
| 会话 / 回合 | 237 / 664 | 237 / 663 | 同批 |
| 压缩率 median | 0.8601 | 0.8621 | **+0.20pp** |
| 压缩率 median（仅压缩回合） | 0.8661 | 0.8684 | +0.23pp |
| hot median | 2,964 | **2,818** | −146 |
| wsc median | 6,060 | **5,901** | −159 |
| 回放延迟 p50 / p95 (ms) | 29.55 / 203.9 | 30.16 / 209.1 | +0.6 / +5.2 |
| 重建率 | 0.0316 | 0.0317 | 持平 |
| 可恢复性 / 确定性 | 1.0 / 5-5 | 1.0 / 5-5 | 持同 |
| error_sig / path / path_recent / failure_site | 0.9988 / 0.8406 / 0.9126 / 0.9479 | 0.9988 / 0.8386 / 0.9114 / 0.9489 | 噪声级 |
| **`user` 针** | **1.0000** | **0.8320** | **−16.8pp ❌** |

**两条结论**：

1. **真实增益是 +0.20pp，不是交叉批次算出来的 +0.47pp**。差值的来源是语料增长
   （live 语料每轮多 1–2 回合），不是算法 ⇒ **这条印证了「跨批次不可相减」的纪律**，
   也说明 §13.4 上表只能作方向参考。
2. **`user` 针 −16.8pp 在同批 A/B 下完整复现** ⇒ **不是语料漂移，是 P1-b 的结构性代价**。
   P1-b **DOD 不合格、不得并入**；先把「每块首节点 80 字符摘录 + 区间句柄」做出来再看数。

#### 13.4.2 P1-b′：区间句柄 + 块内全节点摘录（修正版）

**改法**：一个块一行，但**块内每个节点的 80 字符摘录都内联在同一行里**，行尾挂区间句柄：

```
[REQUESTS] #5…45 用户[8]: <80字符摘录5> ⏐ <摘录13> ⏐ … expand(reqs://5-45)
```

同一行省掉 7 组 `#<idx> 用户:` 头部与 `expand(...)` 尾巴（每行约 10–15 tok），
**行数仍降 ~`request_old_group_size` 倍，而文本可见性与逐节点形态相同**——
「可恢复 ≠ 可见」的正面解法。

| 指标 | pre（改动前） | P1-b（只发句柄） | **P1-b′（摘录内联）** |
|---|---:|---:|---:|
| 压缩率 median | 0.8601 | 0.8621 | **0.8632** |
| 压缩率 median（仅压缩回合） | 0.8661 | 0.8684 | 0.8701 |
| hot median | 2,964 | 2,818 | 2,918 |
| wsc median | 6,060 | 5,901 | **6,126** |
| 回放 p50 / p95 (ms) | 29.55 / 203.9 | 30.16 / 209.1 | **32.93 / 233.7** |
| `path` / `path_recent` | 0.8406 / 0.9126 | 0.8386 / 0.9114 | 0.8327 / 0.9070 |
| `failure_site` | 0.9479 | 0.9489 | 0.9433 |
| **`user` 针** | 1.0000 | **0.8320 ❌** | **1.0000 ✅** |

**结论**：
1. **阻断项已修**：`user` 针回到 **1.0000**（这正是 P1-b 唯一不可接受的地方）；
2. **压缩率 +0.31pp**（vs pre），比 P1-b 的 +0.20pp 略好；
3. **但代价转移了**：延迟 **+3.4ms p50 / +29.7ms p95**、`wsc` median **+66**（行变长 ⇒
   journal 增长更快、重冻结更频繁）、`failure_site` −0.46pp、`path*` 各 −0.6pp。
   即「用延迟与其它段预算换 `user` 可见性」。
4. **仍不可替换 C2**：压缩率 86.32% < 88%、p50 32.93ms > 20ms、`failure_site` 0.9433 < 0.95。

**下一步的落点（已由本轮证明）**：P1-b′ 的延迟与 `wsc` 回退说明**行变长本身有代价**——
应给块行加**总字符上限**（超限时只保留前 k 条摘录、其余退句柄），
让「可见性」与「行宽」都可控；这与 §6 第 2 条（卡片段治理）合流。

### 13.5 P1-b′ 后热层段账目重测：**最大的账已经换人了**

标准长会话 `sess_real_200turn_c2.jsonl`、`Medium+`、journal 布局、紧凑渲染、
`node_token_len(line)+1` 口径（`tests/wsc/_budget_probe.py`，200 回合全员）：

| 段 | P1-a 时 med | **P1-b′ 后 med** | 变化 |
|---|---:|---:|---|
| **[PRUNED]** | 1,221 | **1,221** | 未动 ⇒ **现在是最大单段** |
| **[REQUESTS]** | 1,565 | **949** | **−616（−39%）** |
| [MAIN] | 563 | 563 | — |
| [WORKING SET] | 181 | 181 | — |
| [DECISIONS] | 73 | 73 | — |
| [UNRESOLVED] | 55 | 55 | — |
| [CONSTRAINTS] | 9 | 9 | — |
| **合计** | 3,526 | **2,935** | **−591** |

**`req_lines` med 93 → 14 行**（区间句柄真正起效了：行数降 ~6.6 倍）。

**这张表的意义是「优先级换人」**：

- P1-b′ 之前，`[REQUESTS]`（1565）是第一大账，卡片段（1292）第二；
- P1-b′ 之后，**卡片段（`[PRUNED]` 1221 + `[DECISIONS]` 73 = 1292）成为第一**，
  而 `[REQUESTS]` 退到 949。
- ⇒ 继续做 REQUIRE 的边际收益已经很小；**§6 第 2 条「卡片段治理」自动升为下一个主战场**，
  且本轮实测把它从「直觉排第二」变成了「账目排第一」。

**顺带一个待处理的量**：split sweep 显示 `fixed_budget=1200` 只覆盖 **49.0%** 回合
（`[REQUESTS]` 被压到降级档），`fixed=2100` 覆盖 98.0%。当前 1200/1800 的切分
让**一半回合的 REQUESTS 都在降级**——这是在 `user` 针口径修好之后下一个要复核的点。

### 13.6 卡片段治理：**两条处方实测无效，真正的杠杆在「行形态」**

`_wsc_out/_cards_probe.py`，标准长会话 200 回合全员，`Medium+`：

| 量 | 实测 |
|---|---|
| cards/turn | med **24** / p90 24 / max 24（**= `max_cards`，上限正在起作用**）|
| 卡片段 tok/turn | med **1,292**（`[PRUNED]` 1,221 + `[DECISIONS]` 73）|
| 单卡 `c.tokens` | med 34 / p90 57 / max 67 |
| 结论字符数 | med 61 / p90 138 / max **180** |
| **结论 > `card_conclusion_chars`(220)** | **0 / 4471 ⇒ 上限从未触发** |
| **同回合结论重复** | **0 / 4471 = 0.0%** |
| **同回合去重可省 token** | med **0** |

**两条负向结果（必须保留，别再实现一遍）**：

1. **「结论去重（同 signature 合一）」收益为 0** —— 同一回合内结论**零重复**（每张卡带
   自己的文件/命令/数字，天然唯一）。语料级统计里那些 `x186 bash 失败：退出码 128`
   是**跨回合**的同一结论重建，不是同回合内重复，去重动不了它。
2. **`card_conclusion_chars=220` 从未生效**（最长 180）—— 与 §12.7 摘录上限同型的
   负向结果：**调这个参数不会改变任何输出**。

**真正的杠杆（由账目推出）**：1221 tok ÷ 24 卡 ≈ **51 tok/卡行**，而单卡结论本身
只有 med 34 tok ⇒ **约 17 tok/卡是纯行开销**（句柄 + 标签 + 文件列表）。
形态与 `[REQUESTS]` 同构。

⇒ 卡片段治理的正确落点是**压行形态**（同文件/同错误签名的多张卡合并成一行 + 一个组句柄），
**不是**去重、也不是调结论长度上限。这与 §6 第 2 条方向一致，但**具体做法换成了行合并**。

### 13.7 卡行合并实测：**又一次对冲，且暴露了 journal 布局的结构性上限**

实现：按 `(error_sig, files)` 归并成一行，组内结论**逐字内联**，行尾挂组句柄；
渲染与冷层绑定共用 `prune.cards_handle` / `group_cards`（防漂移，与 `request_chunk_ids` 同纪律）。

| 指标 | pre | **卡行合并** | Δ |
|---|---:|---:|---:|
| 压缩率 median | 0.8601 | 0.8621 | **+0.20pp** |
| 压缩率（仅压缩回合） | 0.8661 | **0.8706** | **+0.45pp** |
| hot median | 2,964 | 2,931 | **−33** |
| wsc median | 6,060 | **6,213** | **+152 ❌** |
| 回放 p50 / p95 (ms) | 29.55 / 203.9 | **33.72 / 222.9** | +4.2 / +19.0 ❌ |
| 重建率 | 3.16% | **3.87%** | +0.71pp ❌ |
| `user` 针 | 1.0000 | **1.0000** | ✅ 保住 |
| `error_sig` | 0.9988 | 0.9988 | ✅ |
| `path` / `path_recent` | 0.8406 / 0.9126 | 0.8322 / 0.9075 | −0.8 / −0.5 ❌ |
| `failure_site` | 0.9479 | **0.9410** | **−0.69pp ❌** |
| 可恢复 / 确定性 / 覆盖 | 1.0 / 5-5 / 1.0 | 同 | ✅ |

**结论：这不是一次干净的胜利，而是与 P1-b′ **同方向**的对冲**——
压缩率小涨（+0.20pp），代价是延迟（+4.2ms p50）、`failure_site`（−0.69pp）、`path*`（−0.8pp）
与重建率（+0.71pp）。可接受的是：**`user` 针保住了 1.0**（与 P1-b 的失败形态不同）。

**由此得到一条结构性结论（必须记住）**：hot 层压行 ⇒ `wsc` 不降反升、重建率上升。
原因是 journal 布局下 `wsc = 热层 + 原样尾部 + 日志累积`，**行变长会加快日志增长、
更早触发重冻结**，把省下的热层额度又还回去。⇒

> **在 journal 布局下，「压行数」换不到 `wsc`。**
> 想动压缩率必须动**三处之一**：① 日志行为（`journal_growth_tokens` 旋钮——但它是
> 「压缩率 ↔ 命中率」的显式取舍，不能随便拧）；② tail 区域（`keep_tail_cut` 口径）；
> ③ 触发口径（§6 第 5 条：改用 `prompt_tokens ≥ 0.8 × context_limit` 与生产同源）。

这一条解释了为什么 P1-b′（+0.31pp）、卡行合并（+0.20pp）都只能拿到「小涨」：
**它们都在压缩那个已经不是瓶颈的量。**

### 13.8 ⚠️ **先停手：离线回放没有生产触发口径，前几节的数都测在「生产不会跑」的工况里**

读 `synaptic/replay.py::run_session` 得到的事实（不是推测）：

```python
	for t in turn_idx:                      # ← 遍历**每一个**回合
		prefix = api[:end]
		region_end = keep_tail_cut(prefix)
		proj = project(prefix, region_end=..., region_baseline_tokens=region_raw)
		if not proj.result.compressed:      # ← 唯一的"不压缩"出口：收益门
			... gain_gate_skipped=True ...
			continue
```

即：**WSC 离线回放对每一回合都尝试压缩**，唯一的闸是「收益门（`min_gain_tokens`）」，
**根本没有「`prompt_tokens ≥ 0.8 × context_limit`」这道生产触发闸**。

而生产 C2 的触发是（锚点 `memory/runtime.py:1855/1871`、`engine/query_loop.py:857`）：
**只有累计 prompt 超过水位才压缩一次**，压缩之间的回合原样增长。

⇒ **两者的工况不同**：

| | 生产 C2 | WSC 离线回放 |
|---|---|---|
| 压缩频率 | 过水位才压一次 | **每回合都压** |
| 压缩之间 | 有若干「原样增长」回合 | **几乎没有** |
| 压缩率分母 | 含这些增长回合 | 几乎不含 |

**这解释了 §13.4–§13.7 的每一个「说不通」的现象**：

- 为什么调整热层段只能换来 +0.2~0.3pp？——因为分母（`base`）在两种工况下不同；
- 为什么 `wsc` 会「不降反升」，重建率反而变高？——因为每回合都重建，**生产根本不会这样跑**；
- 为什么 §6 第 5 条点名「触发口径与生产同源」能「同时修 128k 的负向结果
  （WSC 触发次数少于 C2 ⇒ 两次压缩之间的正常回合更大 ⇒ 全回合投影 +3.1%）」——同源说的就是这件事。

**⇒ 结论（本轮的最终建议，优先级高于任何调参）**：

1. **先修口径，再谈优化**。在 `run_session` 里加**生产触发闸**，并做成**旁路开关**
   （默认 `off` = 保持现状，便于背靠背 A/B）：
   ```
   trigger_ratio: float = 0.0      # 0 = 关闭（现状）；0.8 = 生产口径
   context_limit_tokens: int       # 会话/模型的窗口上限
   ```
   闸门判据：`C0(prefix) 的 prompt tokens ≥ trigger_ratio × context_limit_tokens`；
   不满足时**复用现有的 `gain_gate_skipped` 分支**（记 `trigger_skipped=True`，
   不推进组装状态），语义与收益门拒绝一致。
2. 修完后 §13.4–§13.7 的**所有数字都要重测**（工况变了，旧数不可比）；
3. **在此之前不要再改热层形态**——否则是在错误工况里继续优化（已连续两次验证：
   P1-b′、卡行合并都只能拿小涨且伴随延迟/针回退）。

这一条是「归因先于优化」纪律的直接产物：**先确认测的是什么工况，再决定优化什么。**



## 十四、第七轮：伪对治本 + 撤块 + 门禁删除（2026-09-15）

> 本轮**没有**推进 WSC 口径修复——因为发现的头号问题是**缺陷**而非调参，
> 按「归因先于优化」，缺陷优先级高于任何 WSC 调参。§13.8 的触发口径结论仍然
> 有效且**未被推翻**：离线回放与生产工况不同源，§13.4–§13.7 的数仍不可用于结论。

### 14.1 ⚠️ `env_channel` 伪对 = 支配性 affordance（缺陷，不是优化）

**实测（同一会话，第一手，非转述）**：`xeyo_env_notice` 幻觉调用 **89 次**，
其中 **≥7 次整条响应体只有这一个调用**。交接文档**已明确预警**该现象，模型仍每次
发生——包括"**用户明说别管之后**"与"**正在动手修它的时候**"。

**机制（已定位到消息层，是结构性的）**：伪对
`assistant(tool_use: xeyo_env_notice) → tool_result` 与「模型自己的工具调用」
**完全同形** ⇒ 投影里出现了一条"**我发出过这个工具调用**"的先例 ⇒ 模型判定自己
拥有它并真的去调。

⇒ **这不是注意力漂移，是上下文里真有一条属于模型自己的工具调用。**
这解释了为什么"劝阻/纪律"全部无效——它不是提示，是**先例**。

**闭环自催化**（本轮完整观测到）：

```
伪对写入投影 → 模型据此调用 xeyo_env_notice
   → host 侧应答者**当成真实工具结果轮**应答（回灌 `# Continue（工具结果后）` + 新环境通知）
       → 上下文再添一条 tool_use 先例 → 下一次更易再调
```

⇒ **不可调用性必须来自形态本身**；任何劝阻文本都违反引擎铁律
（注意力里只出现信息，不出现导演）且已被证伪。

### 14.2 修复一：声道 B（原生 system 承载）

`prompt/turn_context.append_system_notice` —— **同一份正文**（复用
`ENV_NOTICE_HEADER` / `format_env_notice`，内容零变化），**只换承载形态**：
尾部追加一条**原生 system 消息**。

| | 伪对（env_channel） | 声道 B（system_channel） |
|---|---|---|
| 投影里出现 | `assistant(tool_use xeyo_env_notice)` | 一条 `role=system` |
| 模型能否推断"我有此工具" | **能（伪对同形）** | **不能（既非 user 也非 assistant）** |
| 说话人隔离 | ✅ | ✅（system ≠ 用户意图） |
| 前缀 KV | 尾部追加不动 | 尾部追加不动 |
| Store/JSONL | 零污染 | 零污染 |

协议分工：OpenAI 系保留 `role=system`；Anthropic 由
`normalize_messages_for_anthropic._split_system` 上提顶层 `system` 字段
（`_build_body` 无 `cache_control`，无显式缓存可损）。

**回退阶梯**（进程级备忘，重启即重试）：
`system_channel` --结构类 4xx--> `env_channel`（保功能；已知会重新引入 affordance）
--结构类 4xx--> `skip`。

**验收与切档**：`tests/test_t_now_system_channel.py` 五条（尾部单条 system /
Store-JSONL 零污染 / 前缀逐字节不动 / **投影内不存在 tool_use 承载**（含
env_channel 反向对照，防断言空洞）/ env_channel 档行为不变）+ 回退阶梯用例。
默认档已按用户裁决切至 `system_channel`。

**一手对照（本会话内）**：切默认**前** 89 次幻觉调用；切默认**后** **0 次**。

### 14.3 修复二：撤销 `wrap_up` / `runtime_budget` 两块

归因更正见 §13.2。做法：删除**装配点 + 渲染器 + 常量 + 登记表条目**
（物理删除，不是关开关——留一个"预算已尽"文本生成器等于给它留了重新接线的口子）。
断言翻转为"**该文本不得出现在任何模型可见面**"，7 份金标重生成。

**预算机制全部保留**：收尾窗 / 收尾配额 / `StoppedEvent` 语义 / 成本闸一律照旧，
由 `engine/query_loop` 强制。新增 `tests/test_t_now_budget_text_revoked.py` 做
**双向回归**（负向：任何声道都不可见；正向：端到端驱动 `query_loop`，收尾窗照常打开、
配额闸照常放行/拒绝、硬停与 `budget_usd` 照旧）。

### 14.4 修复三：删除「先读后写」门禁（旁路，与 WSC 无关）

`file_edit_tool` / `file_write_tool` / `notebook_edit_tool` 的
"File has not been read yet" 分支删除（用户裁定：很难用）。

删因（摩擦远多于收益）：`Write` 新建的文件 `Edit` 仍被拦；跨会话 / 子代理 / 新会话
没有 `read_state`；LRU 淘汰、限窗读（`is_partial_view`）之后**重读也无用**——模型侧
表现为无法自解的死循环。

**保留**：读过的文件仍做**新鲜度校验**（防静默覆盖他人/外部改动）。
真正的安全网是 `old_string` **精确匹配**（Edit）/ 整文写入语义（Write）——
它**不依赖"是否读过"这一会话态**。

### 14.5 生产工况口径修复（§13.8 的落地）

§13.8 下的三件事本轮全部做完：

1. **`trigger_skip_rate` 报告字段**（原先只有 `gain_gate_skip_rate`）——已补进
   `Aggregate.summary()`（cohort 自动覆盖）、单臂 Markdown、模式对照表、cohort 表。
2. **`compressed_only` 口径修正**：`report.py` 原先只排 `gain_gate_skipped`，
   未排 `trigger_skipped`，导致生产口径下"根本没尝试压缩"的回合混进"仅实际压缩回合"
   标本，把 median 稀释成 0（实测 trig08 报出 n=697 / median=0，真实压过的只有 210 个）。
   这与"两道闸必须分开报"是同一条纪律的另一面：**跳过形态不同，就不能共用同一分母**。
3. **`replay.py` 触发闸跳过路径的 `proj is None` 空指针修复**（不带上它，开
   `XEYO_WSC_TRIGGER_RATIO` 跑 A/B 必崩）。

### 14.6 ⭐ 生产工况 DOD 表（首次真正测在「生产会跑的」工况里）

口径：`level=Medium+`、`mode=closure`、journal 布局、单进程**串行**、语料
`~/.xeyo/sessions`（**live**：242 会话；A=697 回合 / B=700 回合，两臂相差 3 回合
= 0.43% ⇒ **<0.1pp 级差不可测**，下表只报 ≥1pp 的位移）。
A = 触发闸关（旧口径）；B = 生产口径（`RATIO=0.8 × LIMIT=131072` = 104857 token 水位）。

| 指标 | A（闸关） | B（生产口径） | Δ |
|---|---:|---:|---:|
| **触发闸跳过率** | 0.00% | **70.00%**（490 回合） | +70.00pp |
| 收益门拒绝率 | 3.30% | 0.00% | −3.30pp |
| 压缩率 median（**全回合**） | 86.71% | **0.00%** | −86.71pp |
| 压缩率 median（**仅实际压缩回合**） | 87.62% | **95.26%** | **+7.64pp** |
| 热层 token median | 3113 | 5952 | +2839 |
| wsc_tokens median（已含 tail） | 6354 | 15475 | +9121 |
| 延迟 p50（ms，串行） | 39.09 | 0.00 | −39.09 |
| 延迟 p95（ms，串行） | 273.00 | 256.41 | −16.59 |
| 整层重建率 | 3.87% | 1.29% | −2.59pp |
| 成本合计 WSC | 2.8725 | **10.4445** | +7.57 |
| 成本合计 v6.1 | 8.5947 | 8.6927 | +0.10 |
| lossless_rate | 1.0000 | 1.0000 | 0 |
| 确定性（identical/checked） | 5/5 | 5/5 | 0 |

五类针（mean_rate）—— ⚠️ **两侧分母不同，不可直接相减**：

| 针 | A（闸关）mean / n | B（生产口径）mean / n |
|---|---:|---:|
| user | 1.0000 / 671 | 1.0000 / 210 |
| error_sig | 0.9988 / 432 | 1.0000 / 205 |
| path | 0.8261 / **651** | 0.7202 / **210** |
| path_recent | 0.9046 / **626** | 0.8019 / **210** |
| failure_site | 0.9258 / 461 | 0.9232 / 206 |

**为什么分母不同**：`report.py` 的针聚合带筛选 `if t.needles.get(cat, {}).get("n", 0)`
——**只在"该针有样本的回合"上取均值**。生产口径下短/中会话 100% 不触发 ⇒ 它们
**没有样本**，自动退出分母；B 侧那 210 个样本**全部来自长会话**。所以
**B 的汇总值 = 长会话的值**，与 A 的汇总值（跨三档加权）根本不同群体。

**同群体（长会话 cohort）比对才是可比的**：

| 针 | 长会话 A（n） | 长会话 B（n） | Δ |
|---|---:|---:|---:|
| path | 0.7697（n=436） | 0.7202（n=210） | **−4.95pp** |
| path_recent | 0.8642（n=433） | 0.8019（n=210） | **−6.23pp** |
| failure_site | 0.9227（n=394） | 0.9232（n=206） | +0.05pp |
| user / error_sig | 1.0000 / 0.9987 | 1.0000 / 1.0000 | ≈0 |

⚠️ 这仍非严格同分母（A 长会话 n=436 vs B 长会话 n=210，B 是 A 的子集）——
严格同分母要逐回合针数据，报告当前不落盘。**−4.95pp / −6.23pp 是本轮可得的最佳
估计：方向上可信，幅度仍带下界不确定度。**

**附带的口径发现（重要）**：生产口径下**短/中会话的针不可测**（从未压缩 ⇒ 无样本）。
任何跨触发口径的针比对**必须按 cohort 对齐**，否则会把"样本退出"读成"指标退化"
（本轮第一版汇总表就是这么错的）。

cohort 分层（触发闸跳过率 / 仅压回合 median）：

| cohort | 回合 | 触发闸跳过（B） | 全回合 median（B） | 仅压 median（B） | 仅压 median（A） |
|---|---:|---:|---:|---:|---:|
| 短会话(<30 消息) | 80 | **100.00%** | 0.00% | —（从未压缩） | 39.40% |
| 中会话(30–99) | 178/181 | **100.00%** | 0.00% | —（从未压缩） | 76.04% |
| 长会话(≥100) | 439 | **52.16%** | 0.00% | **95.26%** | 92.51% |

分段臂（`--no-journal`）同源结论：触发闸跳过率 0.00% → 70.17%；全回合 median
压缩率 89.40% → 0.00%。

#### 结论（按重要性排序，负向原样保留）

1. **生产水位下 WSC 基本不触发**：70% 回合根本不尝试压缩；**短/中会话 100% 不触发**
   （它们的上下文永远到不了 104857 token），长会话也有 52.16% 不触发。
2. **"压缩率 median（全回合）"在生产口径下失去意义**（过半为 0）——必须看 cohort
   或"仅实际压缩回合"。这正是 §13.4–13.7 读错的根源。
3. **压缩本身没有退步**：仅实际压缩回合的 median 是 **95.26%**，反而**高于**闸关档的
   92.51%。⇒ 过去几轮在热层形态上做的优化，**方向没错但分母错了**。
4. ⚠️ **负向结果（已修正分母）**：`path` / `path_recent` 在**同群体（长会话）**内
   各掉 **4.95pp / 6.23pp**（0.7697→0.7202 / 0.8642→0.8019）。
   第一版汇总表报出的"−10pp"是**分母伪影**（短/中会话在 B 侧无样本、自动退出分母），
   已在本节更正——与本轮修掉的 `compressed_only` 是**同一类错误**：
   **跳过形态不同 / 样本群体不同，就不能共用同一个分母。**
5. **成本反超**：生产口径下 WSC 成本 10.44 **高于** v6.1 的 8.69（闸关档 WSC 2.87
   远低于 v6.1 8.59）——因为压缩次数少了，省下的 token 变少。
6. lossless 与确定性两档**均未退化**（1.0 / 5-5 identical）。
7. **C2 替换判定：仍 ❌，且理由比第六轮更硬**——长会话 `path_recent` 0.8019 远低于
   0.95 门槛（同群体口径），且不再有"压缩率高"可依（全回合 median 0%）。

> ⚠️ **本节的口径已被第八轮更正（§15.1–15.5）**：这里的 `cost ratio 1.29` 来自回放台
> 「未过闸发整段原文 + 水位基准用原始前缀（单调）」两处伪影。修成生产语义后的
> **同节奏对撞**结论是 **1.013（生产水位）/ 0.990（成本驱动折叠）**。
> 本节的**工况描述**（70% 回合不触发、短/中会话 100% 不触发）仍然成立且是本节最有价值的部分；
> **成本类结论请以 §15 为准**。

#### 缺口重排（旧结论作废）

- **瓶颈不在热层形态**（仅压回合已 95.26%，继续压形态是优化已经不是瓶颈的量——
  §13.7 的结论在生产口径下依然成立，且更彻底）。
- **真正的缺口是"触发水位 × 真实语料的匹配"**：真实会话大多到不了
  0.8×context_limit。要么水位该按真实分布重估，要么承认 WSC 的收益面在真实工况里
  很窄——这是一个**产品决策**，不是调参。
- **`path` / `path_recent` 的 10pp 回退必须有归因**（为什么只在触发闸开启时掉？），
  在归因清楚之前不应再动热层。

### 14.7 本轮未做（诚实清单）

- 声道 B 的 **A/B 验收未跑全**：幻觉调用次数已有一手对照（本会话切档前 89 次 →
  切档后 0 次）；**说话人混淆率（`drift_judge.py`）与厂商 4xx 回退率仍待测**。
- 提交门（`tsc + vitest + pytest P0`）全量未跑；14.5 的三处代码改动中，
  `trigger_skip_rate` 与 `compressed_only` 两处**未跑测试**（用户指示）。
- `path`/`path_recent` 回退**未归因**（见 14.6 结论 4）。



## 十五、第八轮：口径更正 + 同节奏对撞（2026-09-16）

> **本轮推翻的是口径，不是算法结论。** §14.6 与 `_wsc_out/WSC_现状数据表_2026-09-16.md`
> 报出的「生产口径下 cost ratio 1.29 / 1.58 ⇒ 不能替换 C2」，测的是回放台的
> 两处口径伪影；把口径修对之后，同节奏对撞的结论是**成本打平、信息留存 2–8 倍**。
> 但**信息针仍有两项未达 DOD**，所以「替换 C2」的判定仍是**未准入**——理由换了。

### 15.1 缺陷：回放台「生产口径」臂有两处与生产不符

两处都在 `python/synaptic/replay.py` 的旧分支里，**都是口径、不是算法**：

| # | 旧行为 | 生产实际 | 锚点 |
|---|---|---|---|
| 1 | 未过闸 / 收益门拒绝的回合**发整段原文** | 一旦压过，**每一轮**都发「冻结头 + cursor 之后的右段」，与 decide 动作无关 | `memory/runtime.py:1643-1645`（`send()` 无条件走 `apply_c2_messages`）、`runtime.py:1094-1096` |
| 2 | 水位基准 = **原始前缀**的估算，且随回合**单调增** ⇒ 一旦越过就每轮都压 | 基准 = **上一枪实际发送**的 prompt tokens（vendor usage），压过一次后基准塌回紧凑投影 ⇒ 要等尾部重新长到水位才再压 | `engine/query_loop.py:1382`（`note_shot(prompt=hit+miss)`）→ `memory/runtime.py:1910-1914`（读 `last_prompt_tokens`）→ `runtime.py:1896` |

后果：伪影 ①让 WSC 在 70% 的回合发整段原文，伪影 ②让它「每轮都重压」——
两者都**系统性高估 WSC 成本**、同时把命中率算坏。这就是历史结论的来处。

### 15.2 修复一：官方回放台新增 `adopted` 生产语义档

- `synaptic/replay.py::run_session(compact_model="adopted")`：跨轮携带
  `cursor / hot_text / last_sent_tokens`；压过之后一直发紧凑投影；
  水位判据用**上一轮实际发送的 L**；收益门拒绝**保持**上一轮的投影（不回退原文）。
- 新增 `TurnRecord` 字段：`wsc_active`（本轮发的是紧凑投影）、`compaction_event`（本轮发生折叠）、
  `sent_tokens`（= 下一轮水位基准）。
- 新增报告字段：`wsc_active_turns/rate`、`compaction_events/rate`、
  `reduction_vs_base_active_only`、`cost_ratio_wsc_v61_active(_n)`、
  `needle_survival_pooled`（Σhit/Σn 池化口径——旧口径是「在该针有样本的回合上取均值」，
  跨档相减会把"样本退出"读成"指标退化"，§14.6 已在针表上踩过一次）。
- CLI：`-m synaptic replay --compact-model adopted`（默认 `legacy`，「两个档的数字不可混用」）。
- 回归测试 `tests/wsc/test_replay_semantics.py`（5 条）：压过之后不许回退原文、
  水位必须以 `sent_tokens` 为准（原文大 ≠ 该折叠）、legacy 档行为逐项不变、
  报告的 active/event 计数与同批回合成本比。

### 15.3 修复二：同节奏对撞台 `tests/wsc/_duel_probe.py`

「WSC 能不能替换 C2」的正确问法是**只换头、别的都不换**：
同一批回合、同一个切点（生产 `c2_cut_index`）、同一种尾部（两臂同策），
只让**头的生成器**不同（WSC 热层日志 vs 生产 `deterministic_c2_summary` + 逃生舱）。

四个节奏档，全部在 `_wsc_out/_ab3_corpus`（243 会话 / 736 回合，两臂面对同一批字节）：

| 节奏 | 尾部 | WSC ΣL / hit / Σcost | C2 ΣL / hit / Σcost | **cost ratio** |
|---|---|---:|---:|---:|
| `wm80`（生产水位 0.8×131072） | c0 | 30.46M / 0.6634 / ¥19.037 | 30.47M / 0.6691 / ¥18.796 | **1.0128** |
| `wm80` | c0+c1 | 25.39M / 0.6738 / ¥15.926 | 25.33M / 0.6796 / ¥15.686 | **1.0153** |
| `econ`（成本驱动折叠） | c0 | 8.95M / 0.6127 / ¥8.124 | 12.05M / 0.7168 / ¥8.203 | **0.9904** |
| `every`（每回合折叠） | c0 | 6.56M / 0.4526 / ¥8.188 | 11.87M / 0.7373 / ¥7.765 | 1.0546 |

三条必须一起读的结论：

1. **生产水位下成本打平**（1.013 / 1.015，两档尾部政策都测了）——历史报的 1.29 是伪影。
2. **成本驱动折叠（`econ`）再省 1%**，且把折叠次数从「每回合压」的 100+ 降到 262/736；
   但「折叠越勤越省」**不成立**（`every` 在长会话上省、在全语料上反而贵 5.5%）——
   长会话上得到的结论不能外推到短会话为主的语料（本轮第一版就是这么错的）。
3. `econ` 闸的写法（`_duel_probe.py::econ_gate`）：不折叠时被折叠区以**命中价**每轮继续送，
   折叠则付一次 `miss` 价 —— `R × saved × p_hit ≥ margin × transition × p_miss`
   （`R` = `estimate_remaining`，封顶 24/下限 4）。生产 `try_extend_c2` 的第 4 条闸同源，
   但它把 `margin × price_ratio = 60` 直接乘在 transition 上、且 transition 只算
   「新摘要 + 剩余尾部」，两处都偏保守（实测 200 回合只扩展 12 次）。

### 15.4 信息留存：同一批针、只比头

针从**原始历史**收割（不是头的产物），两臂的尾部逐字相同 ⇒ 差的就是**头**带走了多少。
池化口径（Σhit/Σn），`wm80` + c0+c1 尾部：

| 针 | WSC 头 | C2 摘要头 | 倍数 | DOD |
|---|---:|---:|---:|---|
| `error_sig` | **1.0000**（n=622） | 0.1270 | 7.9× | — |
| `failure_site` | **0.6739**（n=874） | 0.1499 | 4.5× | ≥0.95 ❌ |
| `path` | **0.5918**（n=20533） | 0.2718 | 2.2× | ≥0.75 ❌ |
| `path_recent` | **0.6706**（n=9945） | 0.3556 | 1.9× | ≥0.85 ❌ |
| `user` | **1.0000**（n=1469） | 0.6495 | 1.5× | 保持 ✅ |

即：**成本打平的前提下，WSC 头把关键信息的留存拉高 1.5–8 倍**，而它的头还**只有 C2 头的一半大**
（实测 head tokens：WSC 5176 vs C2 10454，同一会话）。C2 的摘要预算 `C2_SUMMARY_BUDGET = 12_000`
是**字符**、WSC 的 `hot_budget_tokens = 3_000` 是 **token**；中文会话下 12k 字符 ≈ 9k token
⇒ 生产 C2 在自己的头上多付了约 3 倍 token，换来的是更少的信息。**这是一条独立于 WSC 的账目问题。**

### 15.5 官方 harness（adopted）与 duel 的分工（防止再次混读）

`-m synaptic replay --compact-model adopted` 跑出来的对照列是 **simulator 的 v6.1 基线**
（自决节奏 + 右段带 C1 老化）。它对 WSC 臂**不同策**，因此其
`cost_ratio_wsc_v61_active = 1.576` **不能当作算法判决**，只能当作「与现有模拟基线的差距」：

- v6.1 基线右段按 `c1_frozen = end - KEEP_TAIL_MESSAGES` 老化（`simulator/replay.py:422`），
  而生产压缩态的 C1 边界**不推进**（`runtime.py:223-233`：`compact_cursor > 0` 直接 return False，
  「避免与 C2 同轮抢边界、制造双 miss」）⇒ WSC 臂的 c0 尾部才是生产压缩态的形态；
- 判决请用 §15.3 的**同策对撞**：1.013 / 1.015 / 0.990。

adopted 官方档本轮实测（736 回合 / 237 紧凑态回合 / 20 次折叠）：

| 指标 | 值 |
|---|---|
| `trigger_skip_rate` | 97.28%（水位 0.8×131072 下 716 回合不折叠） |
| `wsc_active_rate` | 32.20%（237 回合） |
| `compaction_event_rate` | 2.72%（20 次） |
| `reduction_vs_base_active_only` median / p95 | 0.77 / 0.96 |
| 池化针 user / error_sig / path / path_recent / failure_site | 1.000 / 1.000 / 0.599 / 0.732 / 0.702 |
| 可恢复性 `lossless_rate` | **1.0000**（7178 剪枝全绑句柄，8588 往返逐字节无损） |
| 延迟（**非折叠回合**） | 0 ms（根本不跑 `project()`） |
| 延迟（**折叠回合**，n=20） | p50 **114.1** / p90 311.6 / p95 **338.9** / max 435.8 ms |
| 延迟（摊到全部 736 回合） | mean 4.2 ms |

⇒ 延迟的口径必须改：adopted 节奏下**成本只在折叠回合发生**，
「p50 ≤20ms / p95 ≤100ms」是 legacy 逐回合口径的指标，对这个形态不该直接套用。
真正该压的是**折叠当轮的 114ms p50**（且它随会话长度线性增长——全量重建图与全量装配）。

### 15.6 DOD 差距表（updated 2026-09-16）

| 项 | 目标 | 现状 | 判定 |
|---|---|---|---|
| 成本比 WSC/C2（同节奏同尾部） | <1.0 | 1.013（生产水位）/ **0.990**（成本驱动折叠） | ⚠️ 打平~微胜 |
| `user` 针 | 保持 1.0 | 1.0000 | ✅ |
| `error_sig` | — | 1.0000 | ✅ |
| `path` | ≥0.75 | 0.592（对撞）/ 0.599（官方档） | ❌ |
| `path_recent` | ≥0.85 | 0.671 / 0.732 | ❌ |
| `failure_site` | ≥0.95 | 0.674 / 0.702 | ❌ |
| 可恢复性 `lossless` | 1.0 | 1.0000 | ✅ |
| 确定性 | 5/5 | 5/5 | ✅ |
| LLM 调用 | 0 | 0 | ✅ |
| 折叠延迟 p50 | ≤20 ms | 114.1 ms | ❌（口径待重定） |
| 压缩率（紧凑态回合 median） | ≥0.88 | 0.77 | ❌ |

### 15.7 本轮定位到的杠杆（按收益排序，均未动手）

> ⚠️ **本节标题已过时**：第 1、2 条已在 §15.9（第九轮）动手，见那里。保留原文以留痕。

1. **折叠当轮的前缀失稳**：`_prefix_break_probe.py` 实测——折叠回合的 LCP 只覆盖到
   `[CONSTRAINTS]` 段（约 2–4k token），其后整段（`[UNRESOLVED]`/`[DECISIONS]`/`[MAIN]`…）
   每次折叠都重排 ⇒ 折叠回合 hit 只有 **0.114**（非折叠回合是 0.87，严格逐字节追加）。
   这是 WSC 侧**唯一确证的结构性浪费**：把固定段（PIN/WORKING/PATHS）做成跨折叠稳定、
   或让易变段只追加，可直接吃回折叠回合的 miss。
2. **信息针三项未达**（`path`/`path_recent`/`failure_site`）。已定位的机制与修法（只读实测）：
   ①`[DECISIONS]` 行不渲染卡的 `files=`（17/185 漏失）；②`prune.py:171` 单元 files 截到 4 条
   （52/71 漏失，Bash 单元的 refs 会把命令与输出里的路径全并进来）；③`failure_site` 没有
   `path` 那套 basename 回退（纯口径伪影，7.7pp）；④`[PATHS]` 加「被剪节点 refs」档。
   ①②属「信息已算出来却没渲染」，优先级高于 ④。
3. **折叠延迟 114ms p50**：折叠当轮全量重建图 + 全量装配（O(区域)），
   随会话线性增长。增量图 / 增量装配是既有 P1 挂账，现在是它的主战场。
4. **头部体积 vs 尾部体积的账**：C2 的 12k **字符**预算与 WSC 的 3k **token** 预算不同量纲
   （§15.4），合并两条线时必须先统一。

### 15.9 第九轮：信息针修复 + 折叠节奏定参（2026-09-16，接 §15.7 的第 1/2 条）

#### 15.9.1 信息针修复（三项，全部落地并有回归测试）

| # | 改动 | 位置 | 性质 |
|---|---|---|---|
| ① | `[DECISIONS]` 行补 `files=`（只发射 `files[1:]`，`files[0]` 已在结论里） | `assemble.py::render_decisions` | **信息已算出来却没渲染** |
| ② | 卡片文件上限 4 → 8（`CARD_FILES_MAX`），三处 `[:4]` 全部收敛到常量 | `prune.py` | 同上（Bash 单元 refs 常超 4 条） |
| ③ | `failure_site` 纳入 basename 回退（与 `path`/`path_recent` 同判据） | `metrics.py::needle_survival` | **度量口径不一致**（同一条路径在 `path` 记命中、在 `failure_site` 记漏失） |

回归测试：`test_assemble.py::test_decisions_row_carries_extra_files_without_duplicating_first`、
`test_invariants.py::test_multifile_error_card_keeps_more_than_four_paths`、
`test_path_survival.py` 两条（同判据 + **放宽不等于恒真**的反向断言，防断言空洞）。

**实测（官方 harness，adopted 口径，同一冻结语料 243 会话 / 736 回合）**：

| 针（池化 Σhit/Σn） | 修前 | 修后 | Δ | DOD |
|---|---:|---:|---:|---|
| `user` | 1.0000 | 1.0000 | 0 | ✅ |
| `error_sig` | 1.0000 | 1.0000 | 0 | ✅ |
| `path` | 0.5992 | **0.7159** | **+11.7pp** | ≥0.75（差 3.4pp） |
| `path_recent` | 0.7322 | **0.8227** | **+9.1pp** | ≥0.85（差 2.7pp） |
| `failure_site` | 0.7023 | **0.9116** | **+21.0pp** | ≥0.95（差 3.8pp） |

代价：热层 token median 3485 → **4235**（+750），紧凑态回合压缩率 median 0.7700 → **0.7652**
（−0.5pp），成本合计 ¥19.0370 → **¥19.0478**（+0.06%）。
⇒ **用 0.06% 成本换 9–21pp 信息留存**，方向明确。

同时把对撞台上的针表也刷新了（`_duel_probe`，同节奏同尾部、只换头）：
`failure_site` **0.9550**（C2 头 0.4416）、`path` **0.7573**（0.3872）、
`path_recent` **0.8412**（0.4678）、`error_sig` 0.9988（0.1805）。

#### 15.9.2 折叠节奏定参（`econ` 闸的 `margin` 扫描）

契约：`折叠 ⟺ R × saved × p_hit ≥ margin × transition × p_miss`
（同批 736 回合、同尾部 c0、只换头的对撞台）：

| margin | WSC ΣL / Σcost / 事件 | C2 ΣL / Σcost / 事件 | **cost ratio** |
|---:|---:|---:|---:|
| 0.1 | 6.93M / ¥7.5056 / 415 | 11.75M / ¥7.8321 / 299 | 0.9583 ✅ |
| **0.25** | 7.15M / ¥7.3291 / 378 | 11.81M / ¥7.8274 / 284 | **0.9363** ✅ ← **最优** |
| 0.5 | 7.59M / ¥7.5176 / 331 | 11.89M / ¥7.9537 / 272 | 0.9452 ✅ |
| 0.75 | 8.69M / ¥7.9586 / 285 | 11.93M / ¥8.1217 / 254 | 0.9799 ✅ |
| 1.0 | 8.95M / ¥8.1236 / 262 | 12.05M / ¥8.2030 / 244 | 0.9904 ✅ |
| 3.0 | 14.45M / ¥11.1143 / 137 | 18.02M / ¥10.2748 / 156 | 1.0817 ❌ |
| `every`（每回合折） | 6.56M / ¥8.1883 / 525 | 11.87M / ¥7.7645 / 320 | 1.0546 ❌ |

⚠️ 针列不可跨行比：`margin ≤ 0.25` 的两档跑在 `[PATHS]` 配额加宽**之后**（§15.9.3），
其余跑在之前。**成本比不受配额影响**（差 0.06%），可比。

三条结论：
1. **cost ratio < 1.0 成立且最优档 = margin 0.25 → 0.9363**（WSC 便宜 6.4%）；
2. **U 形已两端夹逼**：0.1 → 0.9583、3.0 → 1.0817、`every` → 1.0546
   ⇒ 太大尾部涨、太小逼近蛮折，**极值在 0.25**；
3. **节奏可以买**：最优档 WSC 折 378 次，比「每回合折」的 525 次**少 28%**，
   成本却低 10.5%——「折叠次数」不是目标，摊平后的成本才是。

节奏参数的**建议默认**：`econ` + `margin=0.25`。它与生产水位（0.8×131072）的差距是
**2.6 倍成本**（¥19.0 → ¥7.33，同语料同回合）——**节奏是主战场，头是次战场**：
「WSC 头 vs C2 头」只值 6%，「折叠节奏」值 160%。

#### 15.9.3 `[PATHS]` 配额默认值变更（24/256 → 96/1024）

`_path_hole_probe` 的漏失分类显示：剩余漏失的主导类别是 `pruned_card_files_omit`，
而 `[PATHS]` 的候选池（`paths.py::render_paths`）**本来就覆盖被剪节点**——真正卡住的是
**配额**，不是候选集。扫描结果：

| limit / budget | path | path_recent | failure_site | 压缩率 median | 热层 median | 成本合计 |
|---:|---:|---:|---:|---:|---:|---:|
| 24 / 256（旧） | 0.7159 | 0.8227 | 0.9116 | 0.7652 | 4235 | ¥19.0478 |
| 48 / 512 | 0.7542 | 0.8996 | 0.9302 | 0.7671 | 4450 | ¥19.0507 |
| **96 / 1024（新默认）** | **0.8289** | **0.9944** | **0.9907** | 0.7656 | 4742 | ¥19.0591 |

⇒ 三个路径类针**全部达到 DOD**，总成本 +0.06%、压缩率 median 未退化。
代价是热层多 507 token 的「路径簿」——它挤占的是固定段预算里的 `[PATHS]` 份额，
实测未把 `[REQUESTS]`/`[WORKING SET]` 挤出（见 hot median 变化量）。
**回退档 24/256 保留**（报告 `sampling.path_index_*` 留痕），因为「路径簿」变长属于
注意力层面的取舍，离线指标量不到——线上影子档应先按旧值观察，再决定是否吃满。



### 15.10 DOD 差距表（第九轮更新，2026-09-16）

口径：官方 harness `--compact-model adopted` + 全语料 243 会话 / 736 回合；
对撞台数字来自 `_duel_probe --cadence econ --econ-margin 0.25`（同切点、同尾部、只换头）。

| 项 | 目标 | 现状 | 判定 |
|---|---|---|---|
| **成本比 WSC/C2** | <1.0 | **0.9363**（econ m=0.25，同批 736 回合） | ✅ |
| 成本比（官方档 vs simulator v6.1） | <1.0 | 1.576（**不同策**，见 §15.5，不作判决） | ⚠️ 口径不可用 |
| `user` 针 | 保持 1.0 | 1.0000 | ✅ |
| `error_sig` | — | 1.0000 | ✅ |
| `path` | ≥0.75 | **0.8289** | ✅ |
| `path_recent` | ≥0.85 | **0.9944** | ✅ |
| `failure_site` | ≥0.95 | **0.9907** | ✅ |
| 可恢复性 `lossless` | 1.0 | 1.0000（7178 剪枝全绑句柄，往返逐字节无损） | ✅ |
| 确定性 | 5/5 | 5/5 | ✅ |
| LLM 调用 | 0 | 0 | ✅ |
| 压缩率（紧凑态回合 median） | ≥0.88 | 0.7656（`econ` 节奏下更低——**L 更小是目的**） | ❌ 口径需重定 |
| 折叠延迟 p50 / p95 | ≤20 / ≤100 ms | 114.1 / 338.9 ms（非折叠回合 0 ms） | ❌（见 §15.5 口径） |

**压缩率与延迟两项的口径必须重定**，理由：
- 压缩率是「相对**未压缩原文**」的比值，而 `econ` 的收益来自**更早折叠**——
  折叠越早，单次压缩率看着越低、但每轮送出的绝对 token 更少。同一个 WSC 在
  `wm80` 下 median 0.77、在 `econ` 下 surface 更低，**成本却只要一半**。
  ⇒ 目标应改为「紧凑态回合的 L 绝对值 + 总成本」，不是压缩率百分比。
- 延迟同 §15.5：只有折叠回合跑 `project()`，逐回合 p50/p95 口径对这个形态不适用。

### 15.11 第九轮未做（留痕）

> 原第 1 条（`econ` 低端夹逼）与「判据落进算法层」已在 §15.12 完成。
> 仍未做：`R` 的敏感性分析（沿用 simulator 启发式）、折叠回合前缀失稳（已归因为折叠固有代价，
> 见下）、增量图/增量装配、生产接线。

### 15.12 第十轮：折叠节奏落进算法层（`synaptic/cadence.py`）

§15.9.2 的判据此前只是评测台里的一支函数（`_duel_probe.py::econ_gate`），**引擎调不到**。
本轮把它做成算法层的一等模块：

- **`synaptic/cadence.py`**（新增，已登记进 `metrics.ALGORITHM_MODULES`）：
  - `fold_economics(...)` 纯判据 `R × saved ≥ price_ratio × margin × transition`；
  - `CadenceState`：跨轮**自我校准**的压缩比估计（`observed_ratio`，滑动平均），
    让 `head_delta` 无需渲染即可 O(1) 估计——首折用保守先验 0.20；
  - `estimate_remaining` / `PRICE_RATIO_HIT_MISS` **内联**实现（算法层零生产依赖），
    由契约测试钉在生产链真值上（`test_cadence.py`，含逐值比对与价目比对）。
- **`WscParams.fold_cadence`**（`always` | `econ`）+ `fold_margin`（默认 **0.25**）+ `fold_price_ratio`；
  `for_level` 显式透传（该类的老毛病：漏传会被静默吃掉）。
- **`replay.py` 的 adopted 档**接上判据：`econ` 时不再看水位，改由
  `CadenceState.decide(...)` 逐轮判；折叠落地后 `observe(...)` 回填实测压缩比。
- CLI：`-m synaptic replay --fold-cadence econ`（默认 `always`；报告 `sampling.fold_cadence/margin` 留痕）。
- 测试：`tests/wsc/test_cadence.py` 8 条（口径契约 2 条 + 单调性 4 条 + 自校准 2 条），
  `tests/wsc` 合计 **118 passed**。

**官方 harness 全语料实测（243 会话 / 736 回合，同一冻结语料）**：

| 指标 | 生产水位（0.8×131072） | **`econ`（算法层判据）** |
|---|---:|---:|
| 折叠事件 | 20 | **363** |
| 紧凑态回合 | 237（32.2%） | **685（93.1%）** |
| 热层 token median | 4742 | **2165** |
| 成本合计 WSC | ¥19.0591 | **¥8.2747**（**−57%**） |
| `cost_ratio_wsc_v61_active` | 1.576 | **0.5717** |
| 针 `path` / `path_recent` / `failure_site` | 0.829 / 0.994 / 0.991 | **0.882 / 0.997 / 0.963** |
| 针 `user` / `error_sig` | 1.000 / 1.000 | 1.000 / 0.9966 |
| 压缩率 median（紧凑态回合） | 0.7656 | 0.7711 |

三条结论：
1. **2.3 倍成本差可以在官方工具里复现**（¥19.06 → ¥8.27），且**头还小了一半**
   （2165 vs 4742）——「折得早」同时买到了「每轮送得少」；
2. 信息针在 `econ` 下**更好**（head 覆盖区域更及时）：`path` +5.3pp、`failure_site` −2.8pp 需注意
   （0.991 → 0.963 是**池化样本群体变化**，两侧 n 不同，不可直接相减——见 §14.6 的教训）；
3. ✅ **13% 差的根因已找到并修掉（见 §15.13）**，修后官方档 **¥7.5365**（与对撞台 ¥7.33 差 2.8%）。

**未接线**：本轮仍全部在离线档；生产链一行未动（阶段 A）。

### 15.13 第十一轮：折叠估计的**死亡螺旋**（真 bug，非调参）

#### 15.13.1 现象

`--fold-cadence econ` 的官方档比预期贵 13%（¥8.27 vs 对撞台 ¥7.33）。归因不是「估计不如实测准」
这种含糊说法，而是逐轮打点后看到的**自锁**（`sess_real_200turn_c2`，200 回合）：

| 观测 | 值 |
|---|---|
| 该会话折叠次数 | **14**（对撞台的精确判是 41） |
| 尾部最终规模 | **20.7 万 token**（`base` 26.5 万） |
| 最后一次折叠后的区域 | 109 857 token |
| 判据里的 `headΔ_est` | **76 053**（= observed_ratio 0.69 × 区域） |

机制（**自我强化**，不是随机误差）：

```
某次折叠的 headΔ/区域 比值偏高
  → headΔ_est 偏大 ⇒ saved=区域−headΔ 偏小、transition 偏大
    → 判据拒绝折叠 ⇒ 区域越积越大
      → 下一次比值更偏 ⇒ **永久拒绝**
```

比值为什么会高：折叠的 `headΔ` 偶尔包含「重写」成分（重冻结/头从无到有），
而 `区域` 可能很小 ⇒ 比值冲到 0.7–1.0；再经 EMA（0.5 权重）进入判据。

#### 15.13.2 修法：给估计**结构上界**，不是调参

`head_delta` 在 journal 布局下**结构性有界**：累计追加超过 `journal_growth_tokens`
就重冻结回紧凑渲染，而紧凑渲染受 `hot_budget_tokens` 约束
⇒ `headΔ ≤ hot_budget + journal_growth`（Medium+ = 3000 + 6000 = **9000**）。
实测同语料折叠回合的头 token 上界 **9921**，与这个界同量级。

两处改动（都在算法层，`synaptic/cadence.py`）：
1. `CadenceState.head_delta_cap`：`estimate_head_delta` 触顶即截（`replay.py` 传入
   `hot_budget + journal_growth`）；
2. `observe` 拒收 `比值 > 1.0` 的样本（那一定是「头从无到有 / 整层重写」，不是追加比），
   并在 `observe_fold(carried_over=False)` 时整条丢弃。

回归测试：`test_head_delta_estimate_is_capped_by_structure`（含**不封顶⇒拒折 / 封顶⇒折**
的同回合对照，把死亡螺旋本身钉成反例）、`test_implausible_ratio_is_rejected`。

#### 15.13.3 修后实测（官方 harness，`--fold-cadence econ`，全语料 736 回合）

| 指标 | 修前（第十轮） | **修后** | 对撞台（精确判，参考） |
|---|---:|---:|---:|
| 折叠事件 | 363 | **390** | 378 |
| ΣL | 22.26M | **7.52M**（−66%） | 7.15M |
| 成本合计 WSC | ¥8.2747 | **¥7.5365**（−9%） | ¥7.3291 |
| `cost_ratio_wsc_v61_active` | 0.5717 | **0.5170** | — |
| 热层 median / max | 2165 / — | 2280 / **9921** | — |
| 针 `path` / `path_recent` / `failure_site` | 0.882 / 0.997 / 0.963 | **0.890 / 0.998 / 0.965** | — |
| 针 `user` / `error_sig` | 1.000 / 0.9966 | 1.000 / 0.9972 | — |
| 折叠延迟 p50 / p95（摊到全回合） | 23.3 / 194.3 | 26.1 / 211.8（**32.9 ms/回合**） | — |

⇒ **O(1) 估计与精确判的差从 13% 收到 2.8%**；省下的 9% 全部来自「不再自锁」，
不是来自更准的估计。这条也解释了为什么第一轮扫描里 `margin=0.1` 反而更贵
（估计偏低时更容易触顶/自锁）——**参数扫描在带自锁的系统上会给出错的极值**。

#### 15.13.4 头为什么能到 9921（**归因更正**：不是账目缺口）

第十一轮初稿把「头 > 9000」写成「`[REQUESTS]` 不在任何预算内 ⇒ 账目缺口」。
**这条是错的**，实测驳回：

```
hot_budget=3000 fixed=1200 main=1800 journal_growth=6000
t20   hot=1969 | fixed 580/1200 over=0 | main 1310/1800 over=0 | requests=full@320
t80   hot=2989 | fixed 1073/1200 over=0 | main 1734/1800 over=0 | requests=dedup@320
t199  hot=2734 | fixed 1156/1200 over=0 | main 1426/1800 over=0 | requests=dedup@320
```

`H_REQUESTS` **在固定段预算内**（`budget.apply_hot_budgets` 的 `fixed_headers` 含它，
且带 `full → dedup → dedup_short → dedup_min80_overflow → handles` 降级梯），
`fixed + main == hot_budget` 由 `for_level` 保证（`test_budget.py` 两个测试锁死）。
§13.4 那条旧账**在更早的轮次已经还清**——本轮引用它属于**引用了过期结论**。

`hot = 9921` 的正确解释是**两个合法项相加**：
`journal` 布局下**紧凑渲染 ≤ 3000**（上表实测 2734–2989）+ 追加日志累计到
`journal_growth_tokens = 6000` 才重冻结 ⇒ 上界 ≈ 9000，实测 9921 同量级。
唯一的**刻意超预算**是 `dedup_min80_overflow` 档（保住 `user` 针，溢出如实记进审计），
本语料未触发。

⇒ **没有账目工作要做**；`head_delta_cap = hot_budget + journal_growth` 就是正确上界。
（留这段更正的原因：这是本项目第二次「引用过期结论当缺陷」，与 §13.2 的措辞事故同类。）

### 15.14 第十二轮：阶段 B 影子档接线（生产侧，默认关）

沉入主链（阶段 C）前必须先拿**真实分布**下的数据（docs 阶段 B 定义、AGENTS.md 第 4 条
「新行为先以旁路形态上线验证收益」）。本轮把影子档做出来：

- **`memory/wsc_shadow.py`**（新增，生产侧模块）：`maybe_observe(messages, session_id,
  actual_tokens, context_limit)` —— 用生产同一套切点（`c2_cut_index`）算一次
  「若改用 WSC 会长什么样」，把 `hot/wsc/actual tokens / 折叠判据账目 / 五类针 /
  可恢复性 / 延迟` 记进 `<home>/.xeyo/wsc_shadow.jsonl`。
- **接线点只有一处**：`engine/query_loop.py` 全量投影分支（每轮都走到的那条；缓存命中
  路径要求消息数未变，新用户轮必然不命中）。按 AGENTS.md「反巨石」，巨石里只有接线。
- **开关 `XEYO_WSC`：strict_env、默认关**。两处刻意不走的回退（各有事故形态）：
  1. 走 `sidecar.policy.side_enabled` 的「未注册键 → 回退 `sidemod_promote()`」：
     全局升格默认**开** ⇒ 实测 `enabled()` 返回 **True**，影子会在用户毫不知情时
     开始为每个长会话多算一次 O(区域) 投影；
  2. 改为**注册**进 `memory_switches`：该表「settings 唯一权威、**环境变量一律不参与**」
     （`get_value` 的明文契约）⇒ `XEYO_WSC=1` 会静默失效。
  ⇒ 归入 `sidecar.policy` 文档里的 **strict_env** 一类：只认自己的 env、默认关。
- **三条红线（机器锁死）**：①不改发送（无返回值、不改入参，`test_returns_nothing...`）；
  ②fail-open（内部异常不外泄，测试用 monkeypatch 造真异常）；③有界（每会话采样 6 轮、
  低于 40 条消息不算）。另有两条成本闸同前。
- 测试：`tests/test_wsc_shadow.py` 8 条（含**全局升格默认开不得带起影子档**的回归，
  以及接线点存在且被 try 包裹的静态断言）。`tests/wsc` 121 + 影子 8 = **129 passed**。

**尚未做**：真实流量下的影子数据（需线上开 `XEYO_WSC=1` 跑一段）；阶段 C 的准入条件之一
「shadow 里 PIN 超预算率 < 5%」还没有影子数据可算（`budget` 审计已在
`proj.result.budget`，影子档已把它整段落盘，直接聚合 `fixed_overflow_tokens` 即可）。

### 15.15 第十二轮：`margin` 复扫（**推翻 §15.9.2 的极值**）

§15.13 的自锁被修掉之后，§15.9.2 那张 U 形表就不再可信——它是**在带自锁的系统上**
扫出来的。复扫两遍：

**长会话子集**（25 个最长会话 / 405 回合，官方 harness + `econ`）：

| margin | 折叠事件 | 成本合计 | `cost_ratio_wsc_v61_active` | `path` | `path_recent` | `failure_site` |
|---:|---:|---:|---:|---:|---:|---:|
| **0.1** | 184 | **¥4.5877** | **0.8195** | 0.874 | 0.997 | 0.959 |
| 0.25 | 154 | ¥4.6654 | 0.8348 | 0.868 | 0.997 | 0.956 |
| 0.5 | 126 | ¥4.6716 | 0.8356 | 0.858 | 0.996 | 0.956 |

**全语料确认**（243 会话 / 736 回合）：

| margin | 折叠事件 | 紧凑态回合 | 成本合计 | 五类针 |
|---:|---:|---:|---:|---|
| 0.25（第十一轮默认） | 390 | 685 | ¥7.5365 | 0.890 / 0.998 / 0.965（+user 1.000 / err 0.997） |
| **0.1（新默认）** | 437 | 703 | **¥7.4753** | **0.894 / 0.997 / 0.967**（+user 1.000 / err 0.998） |

⇒ 两处都偏好**更小的 margin**，且针不退化。默认值已改为 `fold_margin = 0.1`
（`WscParams` 与 `cadence.DEFAULT_MARGIN` 同步，有测试锁两者一致 + 落在实测区间）。

**方法论留痕（比数字重要）**：`margin=0.1` 在第十轮的带自锁系统里**比 0.25 更贵**
（0.9583 vs 0.9363），因为估计偏低更容易触发自锁——**参数扫描在带自锁的系统上会
给出方向错误的极值**。凡是在「系统本身可能有自锁/反馈」的量上扫参，必须先证明无自锁，
再读极值；否则扫出来的是一条**被系统缺陷塑形**的曲线。


#### 15.8 的历史清单（第八轮原文，留痕不改）

> 已完成的项在下面各条前标注了轮次；其余仍是挂账。**不要拿这张表当作当前状态**。

- `econ` 最优 `margin` 的**低端**夹逼（0.1 一档已在跑，结果见 `_duel_econ_m0.1.txt`）；
  `R`（剩余轮次）仍用 simulator 的启发式（封顶 24 / 下限 4），未做敏感性分析。
- 折叠回合的前缀失稳（§15.7 第 1 条）**未动手**：实测 20 次折叠**零重冻结**、
  头严格逐字节追加，折叠回合的低 hit 全部来自「头变长 ⇒ 其后的尾部整段重新计费」，
  属于折叠的固有代价而不是渲染缺陷。要再压只能从**尾部体积**（尾部政策）入手，
  而那需要先与生产确认「压缩态是否推进 C1 边界」（`runtime.py:223-233` 目前是**不推进**）。
- 折叠延迟的增量图/增量装配未做（`project()` 仍 O(区域)）。
- 生产链未接线；`XEYO_WSC` 影子档未开。全量提交门未跑（`tests/wsc` 110 条全绿）。

- 成本比仍未 **<1.0**（1.013 / 0.990 都在噪声量级附近）：`_duel_probe` 的两臂事件数
  （14 vs 15）不完全相同，「同节奏」是**同判据**而非逐回合对齐；严格同回合对撞未做。
- 对撞台的 C2 臂是**简化 C2**（无 `session.md` 来源、无 LLM 摘要旁路、无 notes 引用锚点的
  额外文本），与生产 C2 全形态仍有差；它测的是「同一切点上的确定性摘要头」。
- 信息针的 ①②③④ 修法**均未实施**（本轮只做归因）。
- 折叠节奏 `econ` 的 `margin` 未扫参；`R`（剩余轮次）用的是 simulator 的启发式。
- 生产链**未接线**：WSC 仍是旁路包（阶段 A），`XEYO_WSC` 影子档未开。- 全量提交门（`tsc + vitest + pytest P0`）未跑；`tests/wsc` 106 条全绿。



### 15.16 第十三轮：取回面统一到 `Read`（用户裁定 2026-09-16）

#### 15.16.1 裁定（两步，第二步推翻了第一步的工具选择）

> ① 先裁定「`expand` 与 `offload_read` 是同一个功能」⇒ 只保留一个按需取回接口；
> ② 追问「offload 是否可以被 `Read` 取代」⇒ **可以**，于是**删掉 `offload_read`**，
> 取回**统一到 `Read`**。

| # | 改动 | 位置 | 状态 |
|---|---|---|---|
| ① | 专用工具 `offload_read` **删除**（文件 / meta 条目 / catalog 注册 / 契约清单） | `tools/offload_read_tool.py`（删）、`tools/meta.py`、`tools/catalog.py`、`tests/test_catalog.py` | ✅ |
| ② | offload 引用文本改成 `Read(file_path=…, offset=…, limit=…)` | `memory/offload.py::maybe_offload` | ✅ |
| ③ | 冷层产出**纯文本取回视图** + 区间表 + `Read` 口径引用 | `synaptic/coldstore.py::write_text_view / read_ref` | ✅ |
| ④ | `Read` 读外部化内容**不记 read-state** | `tools/file_read_tool/file_read_tool.py`、`memory/offload.py::is_externalized_path` | ✅ |
| ⑤ | 影子档加取回面**暴露量**字段 | `memory/wsc_shadow.py`、`synaptic/metrics.py::exposed_handles` | ✅ |

#### 15.16.2 为什么 `Read` 能取代（三条硬依据）

1. **权限同一把门**：`Read` 与 `offload_read` 都是 `policy="read_path"`（`tools/meta.py`），
   都走 `check_read_permission_for_path`（区外默认 DENY、密钥硬 DENY、危险 ASK，
   `permissions/filesystem.py:261-284`）。⇒「Read 受工作区约束、offload_read 不受」是**错觉**；
   而 `offload_read` 自己**没有任何** `permissions` 引用（旧审计 TOOL-01 / C-08 已复核为 high）
   ——本次删除**结构性消除**了那条高危缺陷（不是「修好了」，是**能力域合并后攻击面消失**）。
2. **输出形态可比**：`ReadOutput.content` 就是裸切片（`file_read_tool.py:557-564`），
   `offset`(1 起)/`limit`(行数) 与旧 `start/end` 可精确互转：
   `old(start=S, end=E)` ≡ `Read(offset=S, limit=E-S+1)`。
3. **白拿三条闸**（旧工具一条都没有）：`DEFAULT_MAX_TOKENS=25_000`、`MAX_LINES_TO_READ=2000`、
   `MAX_SIZE_BYTES=0.25MB`；旧工具是**裸切片无上限**——模型给 `end=999999` 就能灌爆上下文。
   另：工具面 25 → **24**，schema 总 token 4714 → **4604**（省 110）。

#### 15.16.3 ⚠️ 两条**口径必须改写**的发现（不能糊过去）

**(a) `Read` 给模型的内容带 `cat -n` 行号** ⇒ **「逐字节取回」不成立**。
`map_tool_result_to_content` 一律过 `add_line_numbers`（`file_read_tool.py:635` →
`tools/fileio/text.py:129-141` 的 ``     1→`` 形态）。行号是**寻址元数据**、不是内容丢失，
但「逐字节」这条断言**只能挂在权威副本与纯文本视图上**，不能挂在「模型取回」这一环。

**(b) 空节点取不回过「空串」**：`Read` 把「切片为空」与「offset 越过文件尾」视为同一情况
（`if output.content:` 为假 → 回落 EOF 提示）⇒ 原文为空的节点取回得到
``<system-reminder>Warning: the file exists but is shorter than the provided offset …``。
不为绕过它去给视图塞哨兵行（那会让**内容失真**，而这里信息量本来就是零）——限制如实记录并锁成测试。

**三份产物、三条契约（混了就会写出「永远为假」的断言）**：

| 产物 | 格式 | 契约 | 测试 |
|---|---|---|---|
| `ColdStore.write()` | gzip+JSON | **逐字节**等于历史原文（可恢复性 `lossless_rate` 挂这条） | `test_authoritative_copy_keeps_crlf_byte_exact` |
| `write_text_view()` | 纯文本 | **逐字节**等于原文的 **LF 归一化**形态 | `test_text_view_roundtrip_is_byte_exact` |
| 模型经 `Read` 取回 | 视图切片 | **内容无损**（每行带行号前缀；空节点例外见 (b)） | `test_read_retrieval_is_content_exact_after_stripping_line_numbers` |

视图侧不能保 CRLF 的成因同前（`read_text` 的通用换行归一化是工具既有语义）。

#### 15.16.4 替换的**关键副作用**：read-state 污染（已修）

`Read` 读完会写 `FileStateEntry`（含全文 + mtime + offset/limit），而 `ReadFileState` 是
**LRU 限幅**的（`tools/fileio/read_state.py`）⇒ 用 `Read` 取回冷层内容会：
① 挤掉正在编辑的真文件快照（写前新鲜度校验的依据）；
② 同一区间重复读被 `FILE_UNCHANGED_STUB` 顶掉正文（**取回语义要求每次给正文**）。

修法：`memory/offload.py::is_externalized_path(path, cwd)` 判定「外部化内容」，命中则不记 read-state。
**判定必须传读取方自己的工作区**（不是进程 cwd）：offload 根默认 `<cwd>/.xeyo_offload`，
实测在「工作区 ≠ 进程 cwd」时用进程 cwd 会误判——测试里即复现（判定 False、read-state 照样被写）。

#### 15.16.5 为什么必须改曝光（实测先验，仍成立）

| 观测 | 值 |
|---|---|
| 真实会话库 | 338 会话 / ~876 用户轮 / **12465 次工具调用** |
| 旧 `offload_read` 调用 | **0**（exposure=hidden，模型看不到入口） |
| `Memory`（按需检索的最近同类）调用 | 6（**1.8% 会话**） |
| WSC 每轮投影**暴露**的句柄数 | median **28** / max 42（被剪节点 median 390） |

⇒ 暴露面很宽、使用面 ≈ 0。合并到 `Read` 后，**同一个接口模型本来就在用**（每轮都在用 Read），
这比「新开一个 hidden 工具再指望它被发现」强得多。

#### 15.16.6 尚未接线：阶段 C 的**两条接线要求**（先写清免得踩）

1. **渲染顺序要倒过来**：现在 `project()` 是「先渲染句柄文本（`expand(node://42)`），再绑冷层」。
   要渲染成 `Read(file_path=…, offset=…, limit=…)`，必须
   ① 定稿被剪/骨架节点集合 → ② 写视图拿到 `句柄 → (start, end)` → ③ 再渲染热层文本。
   波及：`metrics.recoverability`（按句柄往返比对）、`assemble` 三处句柄发射点、
   `test_request_chunks`（锁「渲染↔绑定同源」的 5 条）。
2. **取回视图必须落在 offload 根下**（`<ws>/.xeyo_offload/`），否则 `is_externalized_path`
   判不出来、read-state 豁免失效。

#### 15.16.7 判据（写进准入）

> **若真实会话里按需取回（`Read` 命中外部化路径）的触发率 < 1% 会话，则禁止把任何关键事实
> 只放在句柄后面** —— 可展开性不得作为信息保留的兜底，信息保存率 DOD 只能由**头部内容**满足。

影子档已落盘暴露量；阶段 C 并入后即可统计真实使用量，**两侧配对才是真正的「取回率」**。

#### 15.16.8 本轮**未做**与**既有失败记名**

- 热层句柄形态**仍是** `expand(node://N)`（需 15.16.6 的渲染顺序重排，属独立一轮）。
- 旧文档（`docs/QA-1000/*`、`docs/面试知识书/*`、`docs/全项目BUG排查-20260910.md`）仍按当时状态
  描述 `offload_read` / TOOL-01 / C-08 —— 那些是 **dated 快照**，不改写历史；**本节是权威口径**。
- **既有失败两处**（属 T_now 在途工作，**非本轮引入**，按 AGENTS.md 第 1 条记名不静默）：
  1. `tests/test_max_tool_calling.py` 断言 `runtime_notice` 文本在请求里出现 1 次 —— 该块 2026-09-15
     已撤销（`run_pre_llm_inject` **连该参数都没有了**、模块内无渲染器）。本轮回溯确认后已按撤块裁定
     **翻成负向契约并修好**（同款翻正先例见 `test_t_now_budget_text_revoked.py`）。
  2. `tests/test_reserved_tool_prefix.py::test_engine_env_projection_still_constructs` 断言
     `forced_wrap_up=True` 会产生 assistant 伪对 —— 实测 `env_channel` 与 `system_channel`
     **都只返回那条 user 消息**（伪对渲染器已随撤块删除）⇒ 同一类陈旧断言，**归 T_now 在途工作**，
     本轮**只记名未动**（避免把他人的在途改动卷进本次变更）。

### 15.17 第十四轮：句柄形态重排落地 + **跨轮坏引用**（结构缺陷，已修）

#### 15.17.1 落地了什么（§15.16.6 的两条接线要求）

| # | 改动 | 位置 | 状态 |
|---|---|---|---|
| ① | 渲染顺序倒过来：**定稿节点集 → 写取回视图拿行号 → 再渲染热层** | `synaptic/project.py` | ✅ |
| ② | 句柄渲染与解析**唯一实现**（`expression()` / `extract()` / `span()`） | `synaptic/handles.py`（新） | ✅ |
| ③ | 8 处句柄发射点全部改走 `hr.expression(handle)` | `prune.py` / `assemble.py` / `budget.py` | ✅ |
| ④ | 解析侧同源（`rendered_request_nodes(handles=…)`）⇒ 覆盖审计不再静默归零 | `synaptic/budget.py` | ✅ |
| ⑤ | `project(view_path=…, view_ref=…)` + `WscParams.handle_style` + `Projection.view_path` | `synaptic/project.py`、`types.py` | ✅ |
| ⑥ | 引用路径可给**工作区相对**形态（`ref_path_for`），并落进离线台 | `memory/offload.py`、`synaptic/replay.py`、`__main__.py` | ✅ |
| ⑦ | 验收测试 9 条（见 15.17.2） | `tests/wsc/test_handle_style.py` | ✅ |
| ⑧ | 对撞台支持生产形态（`--handle-style read --view-dir …`） | `tests/wsc/_duel_probe.py` | ✅ |

#### 15.17.2 验收契约（`tests/wsc/test_handle_style.py`，9 条）

① **取回逐字节**：渲染出的 `Read` 引用按 offset/limit **经真的 `FileReadTool`** 取回，
剥 `cat -n` 前缀后等于该节点原文（夹具用 Hard 档：Medium+ 在该会话上被剪节点为 0，
覆盖不到剪枝卡那条发射路径）；② **`[REQUESTS]` 覆盖审计 1.0**（分子分母同源；带一条
**形态盲解析器必然更少**的防空洞探针 —— 实测未接同源解析时 24 条用户消息只数进 1 条）；
③ **可恢复性 `lossless_rate` 1.0**；④ **缺区间/缺路径一律回落 `expand`**（坏引用比没有引用更糟）；
⑤ **跨轮引用稳定**（见 15.17.4，含一条负向控制）。

#### 15.17.3 形态不是「优化选项」：只有 `read` 档能代表生产

`expand(node://N)` 在生产里**没有任何解析器**（`offload_read` 已随取回面统一删除）⇒
它是离线记号，把它当基线就等于拿「模型无法执行的引用」比成本。同批全语料 A/B
（340 文件 / 247 会话 / 771 回合，`--compact-model adopted --fold-cadence econ`，只换句柄形态）：

| 形态 | 热层 token median | WSC 总成本 | 活跃档成本比 | 压缩率 median | `path` 针 |
|---|---:|---:|---:|---:|---:|
| `expand(node://N)` | 2406 | ¥8.1969 | 0.5516 | 0.8716 | 0.8784 |
| `Read(…绝对路径…)` | 3407（+41.6%） | ¥8.7824（+7.15%） | 0.5918 | 0.8609 | 0.8606 |
| `Read(…相对路径…)` | **2998**（+24.6% / 比绝对 −12.0%） | **¥8.6041**（+4.97% / 比绝对 −2.03%） | **0.5800** | 0.8636 | 0.8604 |

⇒ ①相对引用**必须**用（比绝对引用同时更短、更便宜）；②`read` 档比 `expand` 贵约 5%：
这不是回归，而是**生产形态的真实价格**（`expand` 档数字自本轮起不可再当基线）。
五类针、可恢复性、`[REQUESTS]` 覆盖在三档下均无退化（`lossless 1.0` / 覆盖 1.0）。

#### 15.17.4 ⚠️ 跨轮坏引用（**结构缺陷**：头 append-only × 视图重写）

- **现象**：头里第 3 轮写下的 `Read(file_path=…, offset=46, limit=1)` 到第 9 轮
  **仍指向 node 4，但那一段已经是 node 3**。
- **根因（两条同时成立才出问题）**：①`assemble` 的日志布局**保留上一轮的行**（前缀稳定
  是它存在的目的）；②`ColdStore.write_text_view()` **整份重写**视图，且块按**节点 idx 排序**
  ⇒ 后续轮次若把**更小 idx** 的节点判成被剪节点，新块插到前面，其后所有块行号整体平移。
  三是 `project()` 的 `cs = cold or ColdStore()`：调用方不传冷层就是**空冷层**。
- **证据**（`_wsc_out/_stale_ref_probe.py`，synth 40 轮 / 句柄 `read`）：1047 条引用样本里
  **115 条含义漂移**（例：`offset=46 limit=1` 旧=[4] 新=[3]）；且**引用本身自洽**——
  能解析、能取回、内容非空，只是内容是别的节点 ⇒ 单轮往返比对（15.17.2 的 ①②③）
  **一条都测不出来**。
- **修法（两条都要）**：①`write_text_view` 改为**按插入序**排块（`put_nodes` 用 `setdefault`，
  重复 idx 不移动位置 ⇒ 只增不改、行号稳定；确定性由调用序列保证，不再由排序保证）；
  ②调用方把**冷层与 `AssemblyState` 一起**跨轮携带（`project(..., prev=state, cold=cold)`）。
  修后同探针 **0 漂移 / 998 样本**。
- **回归锁**：`test_cross_turn_refs_keep_pointing_at_their_nodes`（正向，且防空断言：
  跨轮存活的老引用 ≥20）+ `test_without_cold_carry_the_same_refs_drift`（负向控制：
  证明「必须携带冷层」这条要求不是摆设，若哪天结构上不再需要就删它并更新接线说明）。
- **阶段 C 接线要求已写进** `memory/wsc_shadow.py` 模块文档「接线要求」一节。

#### 15.17.5 同节奏对撞（`econ`，生产形态）：成本比 **1.0007** ⇒ 准入不达标

`tests/wsc/_duel_probe.py`，同一批 736 回合 / 同切点 / 同尾部，只换头：

| 形态 | margin | WSC ΣL | WSC Σcost | C2 Σcost | **cost ratio** |
|---|---:|---:|---:|---:|---:|
| `expand` | 0.1 | 6,931,168 | ¥7.5056 | ¥7.8321 | 0.9583 |
| `expand` | 0.25 | 7,147,348 | ¥7.3291 | ¥7.8274 | 0.9363 |
| `read`+相对 | 0.1 | 7,301,345 | ¥7.8373 | ¥7.8321 | **1.0007** |
| `read`+相对 | 0.25 | 7,794,904 | ¥7.8669 | ¥7.8274 | **1.0050** |

读法：**同一形态**内 `expand` 0.1→0.25 是 0.9583→0.9363（少折更省，与 §15.15 的方向一致，
但 §15.15 的官方 harness 在全语料上偏好 0.1——两处口径不同，别混读）；**换形态**（`read`）
两个 margin 都掉到 ≥1.0。⇒ 形态这一项（+5% 左右）足以把整个判决翻面，这就是
「口径先于优化」在本轮的具体形态。

归因（先归因再谈分）：`read` 引用变长让 ΣL +5.3%，正好吃掉 `expand` 档那 4.2% 的余量；
而**主因是命中率差**：C2 `hit=0.7303` vs WSC `hit=0.5445`（差 −0.186），
命中价是 miss 的 1/30 ⇒ L 省 38% 被 miss 罚金吃光。**要过 <1.0 这道门，杠杆在命中率
（头的前缀稳定性 / 尾部体积），不在再压 L。**

#### 15.17.6 真实分布影子收割（356 轮 / 209 会话，口径受限）

活流量的影子档只有 26 轮 / 3 会话（且 11 行超预算全来自 2 个长会话）⇒ 样本群体太薄，
不足以判准入。本轮加 `_wsc_out/_shadow_harvest.py`：用**影子档同一实现**在真实转录上收割
（同参数、同 `c2_cut_index` 切点、同 `keep_tail_cut` 尾部、同 `read` 形态与视图落点；
差异：`actual_tokens` 缺失、触发时机为「每回合边界」）。**356 轮 / 209 会话 / 22 s**：

| 准入项 | 值 | 判定 |
|---|---|---|
| `user` / `error_sig` / `path` / `path_recent` / `failure_site`（池化） | 1.0000 / 0.9966 / 0.7774 / 0.9715 / 0.9585 | `path` 低于全语料同项（0.86–0.88），需单独归因 |
| 折叠率 | 353/356 = 99.2% | — |
| 每轮暴露句柄 | median 35 / max 346 | 暴露面很宽 |
| 固定段超预算 | **74/356 = 20.79%** | ❌ 准入线 <5% |
| 影子自算一轮 | p50 16.8 ms / p95 266.9 ms | 影子额外成本，非主链延迟 |

#### 15.17.7 固定段超预算的**成分归因**（`_wsc_out/_fixed_compose.py`）

先把「PIN 超预算」的口径写清：`HotBudgetAudit` 的 fixed 段 = `assemble` 传给
`apply_hot_budgets` 的 7 个段头（`[CONSTRAINTS] [UNRESOLVED] [TODO] [WORKING SET]
[PATHS] [REQUESTS] [NEXT]`），**共享** `fixed_segment_budget_tokens = 1200`。
逐行复现影子那一枪的投影（同形态、同 view_path/view_ref，复现出的 74/356 与原账目**逐行一致**）：

| 段头 | median | p90 | max | 非零行 |
|---|---:|---:|---:|---:|
| `[CONSTRAINTS]`（PIN：目标/约束，**无上限**） | 104 | 346 | **1267** | 353/356 |
| `[UNRESOLVED]` | 0 | 50 | 246 | 145/356 |
| `[TODO]` | 0 | 69 | 200 | 77/356 |
| `[WORKING SET]`（12 条上限，**无 token 上限**） | 68 | 490 | 825 | 245/356 |
| `[PATHS]`（**自带上限 1024**） | 144 | 606 | 825 | 343/356 |
| `[REQUESTS]` | 0 | 559 | 1443 | 159/356 |

- 74 行超预算里 **44 行是结构性的**：**不含 `[REQUESTS]`** 就已超 1200 ⇒ 把 REQUESTS
  压到 0 也救不回（`[WORKING SET]` 825 + `[CONSTRAINTS]` 474 + `[PATHS]` 436 这类组合）。
- **结构冲突**：`[PATHS]` 的**自带上限 1024** 已占共享固定预算的 **85%**，
  加上无上限的 `[CONSTRAINTS]` 与 12 条上限的 `[WORKING SET]` ⇒
  「超预算 < 5%」在这套配额下**结构性不可达**（不是内容问题，是**子段配额没有从属关系**）。
- 单一 `[CONSTRAINTS]` 的 max **1267 > 1200**：只要 PIN 集大一点，**一段就能单独超预算**。

⇒ 修法方向（未动手，属下一轮的判据重定）：要么让子段配额**从属**于共享预算
（把「剩余额度」传进 `render_paths`——`[REQUESTS]` 已经是这个机制），
要么把该准入项改写成「各子段各自的超预算率」，并把 `fixed_segment_budget_tokens`
抬到 ≥ 子段配额之和。**在改之前不要拿 20.79% 当内容质量的结论。**

#### 15.17.8 本轮未做（挂账）

- 压缩率口径重定（目标应改为「紧凑态回合 L 绝对值 + 总成本」，见 §15.10/§15.15）与本轮
  新出的「形态口径」合并成一张表：`read` 档 median 2998 / 总成本 ¥8.60（同批）。
- 折叠延迟口径：影子侧 p50 16.8 / p95 266.9 ms（旁路，不是主链）；主链目标 ≤20/≤100 ms
  仍缺**在途增量装配**（`project()` 仍 O(区域)）。

本轮续验：C1 对称旁路未改善成本（20 会话 read 相对引用、econ 0.1：WSC/C2 1.1222 vs C0 1.1093）；`[PATHS]` 已改为从属固定段剩余额度；当前影子收割因 session 去重/空区域仅得 1 条无效样本，线上判据仍待有效 actual_tokens 与取回触发数据。
- 续验（2026-09-16）：日志换头改为 append-only（追加本轮新增事实 + 旧头快照句柄，`rebuilt` 与逻辑 `journal_refroze` 分离），并将 `[REQUESTS]` / `[PATHS]` 置于硬上限；WSC 定向套件 **176 passed**。固定节奏同切点/同尾部的 707 条匹配回合，read 相对形态当前 `ΣL=13,293,265`、池化 `hit=0.7614`、WSC ¥7.8528 / C2 ¥7.6448、成本比 **1.0272**（尚未过 1.0）；`project()` 20 个真实长会话投影样本 p50 **114.5 ms** / p95 **130.4 ms**，延迟准入仍挂账。
- 上条记录随后更正：Medium+ 固定/主链配额改为 **1800+1200**（总热预算仍 3000），真实转录影子 **403 轮 / 209 会话**固定段超预算 **0/403 = 0.00%**；五类针池化为 `user/error_sig/path/path_recent/failure_site = 0.7047/1.0000/0.7479/0.9125/0.9534`。同固定节奏、同切点、同尾部、read 相对形态的 **707** 条匹配回合为 `ΣL=11,372,267`、`ΣH=8,301,632`、WSC ¥7.6094 / C2 ¥7.6448、成本比 **0.9954**；该值是配额重定后的新口径。
- 同新配额的 `project()` 真实长会话 20 个后段样本：p50 **150.0 ms** / p95 **174.9 ms**；仍是 O(区域) 的旁路测量，不代表主链端到端延迟。
- `path` 针在真实分布（0.7774）低于全语料（0.86–0.88）——未归因。
- 命中率杠杆（C2 0.7303 vs WSC 0.5445）未动手：这是成本比过不了 1.0 的主因。
- `margin` 0.25 / 0.5 在生产形态下的复扫（job 已跑，产物 `_wsc_out/_duel3_readrel_m*.txt`）。
- 阶段 C 尚未接线（接线点仍是 `memory/runtime.py::apply_c2_messages` 换头；默认关）。
- 续验（2026-09-17）：固定节奏/切点/尾部、read 相对形态下 50 点配额扫描（fixed 1000–2960，10 批×5 并行）可行最优 `1840+1160`：`ΣL=11,356,868`、池化 `hit=0.730145`、ratio `0.994150`；同配额增长阈值 50 点扫描最优 `growth=1000` 仅至 `hit=0.731690`、ratio `0.995352`（重冻结 127/412），主链恢复 1800 时 ratio 仍约 `1.0324`，故配额/增长不是量级命中率杠杆；定向套件 **177 passed**。
- 分层续验（2026-09-17）：403 轮真实影子 A1 中 P1 激进失效由 403 降至 338 次且结构 stale miss 0%，P2/P3 分别为 7.14%/14.00%；真实冻结 A/B 仅减少重算约 21.6%，未证明命中率收益，故仍默认关闭。Recall lease 同 state 对撞（367 challenges）池化结构召回 5.16%→8.50%、热层均值 +3.0%；独立 key+hard-use oracle 下 `hard` 召回 13.72% 高于 soft-DAG 12.72%，DAG 暂不具备不可替代性；均非 task-success 证据。

#### 15.18 成本口径冻结与任务 A/B 预注册（2026-09-17）

- 成本准入唯一使用严格同节奏、同切点、同尾部、生产 `Read` 相对句柄形态的对撞：当前参考为 `m=0.1 → 1.0007`、`m=0.25 → 1.0050`；参数扫描最优 `0.9954` 仅作 diagnostic，不能替代准入值。
- 任务行为实验预注册产物：`_wsc_out/wsc_task_ab_v1.json`；闭包信息对照脚本：`_wsc_out/_closure_ab.py`。
- 任务集与执行前指纹已落盘：`_wsc_out/wsc_task_battery_v1.json`（26 题、官方 verifier）与 `_wsc_out/wsc_task_ab_v1_manifest.json`；执行必须使用干净隔离 checkout，当前脏工作树不可直接跑 A/B。
- 当前生产适配器仍是 `memory.wsc_shadow` 旁路；C/D 行为 arm 的阶段 C 接线尚未完成，故本轮只完成 task preflight，不宣称已有 task-success A/B 结果。
- 零 API task replay（26 题 / 2,117 个真实 tool-result 快照）已完成：WSC `Σhot/Σbase=0.5693`；五类针池化均为 1.0000。该结果仅是结构/长度诊断，不能作为行为收益或 task-success 证据。
- 2026-09-17 命中率定向优化：Medium+ `journal_growth_tokens` 由 6000 调至 9000；严格生产 Read 相对句柄对撞 736 回合，m=0.1/m=0.25 成本比分别为 **0.9790/0.9639**（旧 6000 基线 **1.0007/1.0050**），命中率分别为 **0.7142/0.7202**（旧基线 **0.5445/0.5729**）。
