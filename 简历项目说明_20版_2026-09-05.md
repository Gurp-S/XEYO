# XEYO 秋招简历项目说明 · 20 版（2026-09-05）

> 用法：先看 §一 数据弹药库（所有数字都来自本仓库实测，可复现、可答辩），
> 再按目标岗位选对应版本，粘简历前删掉你讲不动的条目——**每一条 bullet 都要能被
> 追问三层**。标注 [实测] 的数字有脚本/账本可复现；标注 [设计] 的是架构事实。

---

## 〇、数据弹药库（挑进简历前先背熟出处）

| 数字 | 出处 | 一句话口径 |
|---|---|---|
| 7 层 Agent 架构 | `docs/设计/48-52` | 决策调度/循环/提示词/工具/权限/记忆/容错全自研 |
| ~12.7 万行 Python + 7.8 万行 TS | git ls-files | 引擎+服务端+双前端 |
| KV 命中率 **median 93.9%**（4198 次真实请求） | 校准事件账本 | 输入费用省 **75-84%** [实测] |
| 输出精简 **省 93%**（pytest 1126→78 tok） | bench_phase1_benefit.py | Bash/Read 输出治理 [实测] |
| 符号定位读 **省 99.2%**（20615→175 tok） | codeindex 复测脚本 | 411 行文件只读 18 行 [实测] |
| C2 压缩 hit_proxy 0.98-0.985、尾溢出 0 | c2_benefit.json（605 调用重放） | 长会话成本砍 84% [实测] |
| 45k 行测试 / 268 文件 / CI 三 job | python/tests | 契约测试锁死架构不变量 |
| 证据门否决 3 项候选优化 | v61_evidence_gate.py | 数据准入纪律：不过门不合入 [实测] |
| 合成轮三重护栏（32 轮/唤醒 3/人工恢复） | goal_round_driver | 自动推进永不失控 [设计] |
| 沙箱内存护栏 512MiB Job Object + 树终止 | bash_tool/win_job.py | 进程级兜底 [设计] |
| 6 轮架构自审 → 32 项分级缺口 + 路线图 | 审计文档 | 元认知：能给自己的项目做审计 |
| 5094 次请求 / 41.5 元账本 | usage ledger | 每请求级成本核算体系 |

---

## 一、AI Agent / LLM 应用方向

### A1 · 完整旗舰版（适合大模型应用工程师，4-5 bullets）

**XEYO —— 自研桌面 AI Agent 引擎**（个人项目，Python/TypeScript，12.7w+ 行）
- 独立设计并实现七层 Agent 架构（决策调度/推理循环/上下文装配/工具/权限/记忆/容错），引擎 12.7w 行 Python + 双前端 7.8w 行 TS，45k 行测试，CI 全绿门禁 [实测]
- 上下文工程：设计 C0/C1/C2 三级压缩与环境声道注入管线，生产 4198 次请求实测 KV 缓存命中率 median **93.9%**，输入成本降低 **75%+**；长会话成本砍 **84%** 而答对率不降 [实测]
- 工具治理：统一裁决漏斗（权限三态/审计/输出预算）+ 溢写找回机制，符号定位读 token 省 **99.2%**；Bash 输出精简省 **93%** [实测]
- 建立"数据准入"流程：旁路灰度 → 离线重放 → 证据门裁决，实测否决 3 项负收益候选优化 [实测]

### A2 · 上下文工程深挖版（适合 Prompt/Context 工程岗）

**XEYO 上下文工程子系统**（个人项目）
- 针对"KV 缓存 economics"设计三区上下文模型：稳定左段（memo 化）/ 跳变历史（投影缓存签名）/ 易变尾插（T_now 登记表硬顶 22 块），字节级稳定由缓存签名与契约测试双重保证 [设计]
- 发明"环境声道"注入法：易变块装入仅存在于投影的伪造 tool 对，与用户意图在消息结构上物理隔离，根治说话人混淆型注意力漂移；厂商拒绝时自动回退降档 [设计]
- 实测长会话压缩的"锯齿效应"：8 次压缩 transition 命中谷底 9.9%-81.7%，但干净口径下命中率仅差 0.64pp、答对率 +4.2pp——证明压缩损失是钱的代价而非失忆 [实测]
- 产出确定性摘要选型结论（表A 83.3%/零成本），LLM 摘要因不可复现（三次 62/92/83%）被数据否决 [实测]

### A3 · 记忆系统深挖版（适合 Agent 记忆/个性化方向）

**六层记忆体系（L0-L6）设计与实现**（XEYO 子系统）
- 设计 L0 事实底账→L1 压缩投影→L2/L3 会话任务态→L4 长期记忆→L5 任务归档→L6 NightShift 守门的完整记忆流水线，写入过 schema 门禁/冲突消解/墓碑永不复活三道闸 [设计]
- NightShift 夜间蒸馏：候选池 + diff 佐证（置信度 0.85）+ 晋升限频 8 条/轮 + 6h 冷却，模型/收割只能产生候选，唯一晋升通道是守门人 [设计]
- sqlite 派生索引（文件为唯一事实源，可丢库重建）：90 查询热路径墙钟 **-15.2%**，命中数与基线逐位一致 [实测]
- 已知边界：纯词法检索做不了"英问中答"，Embedding 语义检索列为 P0 路线——能讲清楚自己系统的天花板 [设计]

### A4 · 工具调用与执行安全深挖版

**Agent 工具系统与进程级执行安全**（XEYO 子系统）
- 三通道注册（内置工厂/MCP 直挂哈希钉名/网关动态面）汇入单一裁决漏斗：三态权限 + 双层审计 + 输出预算 + γ4 不可信围栏，出口统一收税 [设计]
- Bash 执行器：Windows Job Object 内存护栏（512MiB）+ 整树终止 + 只读白名单 + 渐进透明路由（cat→Read 改道），实测 pytest 输出 1126→78 tok [实测]
- MCP 生命周期：超时分层/指数退避/坏 schema 降级/required 不挡会话；启停经 reconcile 广播，tools 数组会话冻结零重缓存 [设计]
- 输出预算+溢写：50k 字符工具输出压缩为 8k 预览，全文落盘可找回——上下文防火墙 [实测]

### A5 · 评测驱动版（适合注重方法论的团队）

**为 Agent 建立"数据准入"评测体系**（XEYO 工程实践）
- 自建六维评测（函数调用/代码生成/检索/漂移/块消融/综合）+ 离线仿真器（C2 决策参数推演）+ 4 个反作弊 shadow 模块（盲审/污染门/严格环境档）[设计]
- 全部优化走"旁路灰度 → 离线 200 轮重放 → 证据门裁决"：实测否决 3 项候选（Pareto 加权成本 1.51x、驻留分 1.20x、动态 R 零收益）——**门是真的会拦** [实测]
- 生产账本闭环：5094 请求/每请求级成本核算/4198 次缓存命中率校准事件回喂仿真器参数 [实测]
- 评测诚实度自觉：LLM 摘要同配置三次 62/92/83% → 判"不可复现"弃用生产 [实测]

## 二、后端 / 基础设施方向

### B1 · 架构设计版

**XEYO 多端 Agent 系统**（个人全栈，Python FastAPI + React/Tauri + Ink TUI）
- 设计七层解耦架构：装配层作为唯一"翻译官"收敛全部层间通信，主循环/调度/记忆/权限互不感知 [设计]
- 反巨石纪律：新逻辑一律新模块，机器执法（契约测试锁死 T_now 登记表/工具面冻结/DENY 不可穿越等架构不变量）[设计]
- 事件驱动 + 状态机：Goal 5 态 CAS 修订、任务原子落盘、依赖反转（engine 零 import server）[设计]
- 6 轮架构自审产出 32 项分级缺口与 P0-P3 路线图——给自己的系统做 code review 到 file:line 级 [设计]

### B2 · 高可用与容错版

**Agent 长任务的崩溃恢复与回溯体系**
- 内容寻址文件快照（SHA-256）+ 逆操作日志 + 会话转录事件化回溯：恢复前指纹预检，检测工作区漂移即拒绝，绝不静默覆盖用户改动 [设计]
- 断点续跑三件套：未配对 tool_use 修复 + 中断锚 + fail-closed 合成结果（写工具中断标"结果未知"而非假装成功）[设计]
- SSE 断线不杀任务：客户端断连 turn 转 detached 续跑，重连 cursor 补帧对账 [设计]
- 后台作业晋升：前台超时活进程整体接管（原子换槽防丢行），孤儿进程由 Job Object 树终止兜底 [设计]

### B3 · 安全工程版

**本地 Agent 的权限与沙箱体系**
- 三态权限（ALLOW/ASK/DENY）判定序 deny-first：DENY 不可被授权穿越、撤销即 DENY；授权指纹只含工具身份不含参数（注入免疫）[设计]
- 双层纵深：注册表裁决 + 文件系统硬 DENY（密钥/.ssh/symlink 逃逸防护）独立判定取严 [设计]
- 进程沙箱：Windows Job Object 内存护栏 512MiB + 树终止 + 只读白名单 + 子代理写范围双层强制（过宽自动降只读）[设计]
- 6 轮安全自审定位 32 项缺口（含 bash 前缀授权放大、换行绕过白名单等真实可利用面）并分级整改——把攻击者视角用在自己身上 [设计]

### B4 · API 服务与多端接入版

**FastAPI 引擎服务与三端统一接入**
- HTTP/SSE 流式服务：断连转 detached 续跑 + cursor 回看补帧 + 心跳保活；请求级 busy 租约防并发踩踏 [设计]
- 多通道接入：桌面 GUI / 终端 TUI / 微信（长轮询+浏览器自动化）统一走同一引擎，入站消息统一 untrusted 围栏 + 远程会话权限强制收紧 [设计]
- 后台任务体系：job 注册表（容量/环形输出/唤醒预算）+ goal 合成轮三重护栏防自动推进失控 [设计]
- 全链路账本：每请求级 token/成本核算 + C2 压缩事件 + 校准事件三本账物理分离 [设计]

## 三、全栈 / 客户端方向

### C1 · 全栈版

**XEYO 桌面 AI 工作台**（React 78k 行 + Python 引擎 127k 行）
- 前端自研流式渲染管线：SSE 分片 → Markdown 白名单净化（rehype 自研插件，script/iframe 连子树丢弃）→ 代码高亮 Worker 化 [设计]
- 复杂状态治理：per-session 键控 Map + 失序丢弃（seq box）+ 12+ 项会话销毁清理清单，无跨会话串台 [设计]
- 端到端契约：slash 命令 manifest 单源生成双端代码 + CI drift 检测（字节级比对）[设计]
- 自研回溯状态机：幂等键/失败回滚/落盘/仅终态成功五段加固，服务端崩溃可恢复 [设计]

### C2 · 桌面客户端版（Tauri）

**Tauri 桌面壳工程**（Rust + WebView）
- 进程守护：健康探测 + 冷启动宽限 + 僵尸清理（portfile 带 pid），Windows 单实例互斥 [设计]
- 权限收敛：CSP 白名单 + capabilities 最小集；后端端口文件注入，前端零硬编码 [设计]
- 流式 UI：乐观更新 + 晚到结果回填 + waiting 超时孤儿结算——断流"绝不 interrupt"原则贯穿 [设计]
- 安全自审：发现并记录 frame-src 过宽/shell open 无 scope 等注入面（与审计联动整改）[设计]

### C3 · 终端 UI 版（差异化小项目描述）

**Ink 终端 Agent 客户端**（TypeScript，3.6k 行）
- 三模式运行（in-process / HTTP attach / serve），权限 ASK 键盘交互 + fail-closed（无 TTY 默认拒绝）[设计]
- 与 GUI 共享 slash manifest 单源，命令零漂移 [设计]

## 四、工程效能 / 平台方向

### D1 · 工程规范与机器执法版

**让架构不变量被机器执法**（XEYO 工程基建）
- 契约测试锁死架构红线：T_now 块登记表硬顶 + 装配点双向核对、工具面冻结逐字节比对、DENY 压过授权、指纹注入免疫等 6 组不变量 [设计]
- 指纹 v2 授权协议：身份哈希不含参数 → 提示注入无法伪装已授权工具；版本隔离防旧授权复用 [设计]
- 单源生成：slash 命令 registry → 双端 manifest 自动导出 + CI 字节级 drift 检测 [设计]
- 45k 行测试 / 268 文件 / 三 job CI / example 配置三件套（.env/.xeyo-policy/bash_rules）[设计]

### D2 · 质量治理版（体现元认知，适合加班文化反差型团队慎用）

**对 12 万行自研系统做六轮架构审计**
- 方法：按层并行深读源码 + 高危论断逐一 Grep 复核 + 收益基准实测，产出 32 项分级缺口（file:line 级证据）与 P0-P3 路线图 [设计]
- 提炼跨层反模式：双份状态机/已实现未接线/冻结红线被旁路/估算双口径——4 类问题在自己仓库全部找到实例 [设计]
- 审计驱动的整改：门禁矩阵、补偿链覆盖矩阵、供应链信任链修复方案均已落文档 [设计]

## 五、算法 / 评测方向

### E1 · 评测体系版

**Agent 离线仿真与在线 A/B 双轨评测**
- 离线仿真器：冻结状态独立 Apply + Q/H/U 三分量决策 + 经济成本模型，与生产共享单点估参定义（口径零漂移）[设计]
- 真实重放：200 轮/605 调用冻结真录上跑四配置对照（hit_proxy 0.980-0.985）[实测]
- 在线校准：4198 枪 predicted vs observed 命中事件 → ρ̂ 参数回喂仿真器（自校准闭环）[实测]
- LoCoMo-Refined 冒烟定位能力边界：压缩保人物/主题、丢时间锚点——给出修复方向 [实测]

### E2 · 成本优化版（token 经济学）

**Agent 上下文成本优化：从 175k 到 12k**
- 三级压缩（截断/冻结占位/摘要折叠）+ 确定性摘要（零成本 83.3% 质量分），LLM 摘要因不可复现被数据否决 [实测]
- 实测长会话：输入 tokens **-84%**（4.47M→712k）、成本 **-31%**（干净口径）、答对率 +4.2pp 不变笨 [实测]
- 输出侧：Bash 输出精简 93%、Read 符号定位 99.2%、spill 溢写让 50k 工具输出只占 8k 上下文 [实测]
- KV 侧：稳定前缀纪律 + 校准闭环，生产命中率 median 93.9% [实测]

## 六、通用与面试备用

### F1 · 一页精简版（3 bullets，投递海投用）

**XEYO——自研七层 AI Agent 引擎**（Python/TS，20w 行，45k 行测试）
- 上下文工程：三级压缩+环境声道注入，生产 KV 命中率 93.9%、输入费省 75%+ [实测]
- 工具/权限/记忆全自研：统一裁决漏斗、六层记忆流水线、进程级沙箱 [设计]
- 数据准入纪律：旁路灰度→离线重放→证据门，实测否决 3 项负收益优化 [实测]

### F2 · 一句话版（放项目名后那行灰字，三选一）

1. 自研七层 Agent 引擎：上下文成本降 84%、KV 命中 93.9%，45k 行测试与证据门保证架构不变量
2. 从零实现桌面 AI Agent（引擎+双前端+评测体系），并对自己做了 6 轮 file:line 级架构审计
3. 一个把"KV 缓存经济、数据准入、防失控护栏"做成一等公民的 Agent 系统

### F3 · STAR 面试口述版（2 分钟版）

- **S**：LLM 长会话三座大山——上下文爆窗、KV 缓存浪费、输出 token 失控。
- **T**：自研 Agent 引擎验证一套"上下文经济学"方案能否在生产级稳定性下成立。
- **A**：设计三级压缩+环境声道注入；建离线仿真器+冻结真录重放做旁路验证；上生产账本做每请求核算。
- **R**：KV 命中 median 93.9%（4198 枪实测）、长会话输入 -84%/成本 -31%、输出精简 93%；证据门否决 3 项负收益候选——**方案被数据证明，且方法论能拦住我自己想上的坏点子**。

### F4 · 反差钩子版（差异化开场，适合想被记住）

**我给 12 万行的自研 Agent 做了一次红队审计——审出 32 个洞，包括我自己写的**
- 六轮分层审计：决策/记忆/工具/权限/容错逐层深读，每个缺口带 file:line 证据；抓到"授权前缀放大""换行绕过白名单""冻结红线被旁路"等真实可利用面
- 审计结论反哺设计：32 项缺口 → P0-P3 路线图 → 五份子系统全景文档
- 这段经历证明的不是"没 bug"，而是**有系统性找 bug 的方法论**

---

## 附：使用提醒

1. **数字三问**：每个数字被追问时按"口径 → 数据源 → 复现命令"回答（本报告与 47/49 号全景图都有）；
2. **别堆**：一版简历选 3-5 条，删掉讲不动的；"六轮自审"是钩子不是主打，除非岗位明确看重质量文化；
3. **诚实边界**：DeepSeek live 实测受欠费受阻的项不要写成"已完成"；"Scheduler 未接线"这类瑕疵被问到就坦然讲（反而是 D2/F4 版的素材）；
4. **开源联动**：仓库清理开源后，简历可挂 GitHub 链接——面试官能直接看到 45k 行测试与 docs/设计 52 篇文档。
