diff --git a/gui/src/index.css b/gui/src/index.css
index 505cb36..b0d7601 100644
--- a/gui/src/index.css
+++ b/gui/src/index.css
@@ -448,40 +448,45 @@ html[data-theme="dark"] .xy-code-fade {
   -webkit-text-fill-color: currentColor;
 }
 
-/* 编辑态必须遮住 transcript，尤其是 sticky 到顶部时不能透出下面的文字。 */
+/* 编辑态沿用普通气泡表面，sticky 仅改变编辑控件与边框状态。 */
 .xy-editing-bubble {
   z-index: 1;
   isolation: isolate;
   overflow: visible;
-  /* 用与普通气泡相同的纸面/墨色混合，既不透出下层文字，也不突然变成 composer 卡片。 */
-  background: color-mix(in srgb, var(--xy-paper) 96%, var(--xy-ink) 4%) !important;
-  border-color: var(--xy-user-bubble-border);
+  /* 编辑态沿用普通气泡的透明表面；唯一新增的视觉信号是强调色边框。 */
+  background: var(--xy-user-bubble) !important;
+  border-color: var(--xy-accent) !important;
   box-shadow: none;
   -webkit-backdrop-filter: none;
   backdrop-filter: none;
+  transform-origin: top center;
   transition:
     background-color 180ms var(--ease-out-soft),
     border-color 180ms var(--ease-out-soft),
     box-shadow 180ms var(--ease-out-soft),
     transform 180ms var(--ease-out-soft),
     opacity 180ms var(--ease-out-soft);
-  will-change: background-color, border-color, box-shadow, transform, opacity;
-  animation: xy-editing-bubble-in 180ms var(--ease-out-soft) both;
+  will-change: transform, opacity;
+  animation: xy-editing-bubble-in 190ms var(--ease-out-soft) both;
+}
+
+.xy-editing-input {
+  border-radius: 0.6rem;
+  box-shadow: inset 0 0 0 1px var(--xy-accent);
+  transition: box-shadow 160ms var(--ease-out-soft);
 }
 
 @keyframes xy-editing-bubble-in {
   from {
-    opacity: 0.72;
-    transform: translateY(2px) scale(0.995);
+    transform: translate3d(0, 2px, 0) scale(0.985);
   }
   to {
-    opacity: 1;
-    transform: translateY(0) scale(1);
+    transform: translate3d(0, 0, 0) scale(1);
   }
 }
 
 .xy-prompt-sticky .xy-editing-bubble {
-  background: color-mix(in srgb, var(--xy-paper) 96%, var(--xy-ink) 4%) !important;
+  background: var(--xy-user-bubble) !important;
 }
 
 .xy-prompt-sticky:has(.xy-editing-bubble) {
@@ -494,7 +499,8 @@ html[data-theme="dark"] .xy-user-prompt {
 
 @media (prefers-reduced-motion: reduce) {
   .xy-user-prompt,
-  .xy-editing-bubble {
+  .xy-editing-bubble,
+  .xy-editing-input {
     animation: none;
     transition: none;
     will-change: auto;
