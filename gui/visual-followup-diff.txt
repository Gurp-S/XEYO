diff --git a/gui/src/components/MessageList.tsx b/gui/src/components/MessageList.tsx
index 8d84c3f..f2577b6 100644
--- a/gui/src/components/MessageList.tsx
+++ b/gui/src/components/MessageList.tsx
@@ -37,7 +37,7 @@ import {streamSignalsEnabled} from '@/lib/streamSignal';
 import {collectLatestTodosFromItems} from '@/lib/toolActivity';
 import type {ChatMessage} from '@/lib/types';
 import {cn} from '@/lib/utils';
-import {useChatStore} from '@/stores/chatStore';
+import {useChatUiStore} from '@/stores/chatUiStore';
 import {isSmoothnessOn, useSettingsStore} from '@/stores/settingsStore';
 import {AssistantTurn} from './AssistantTurn';
 import {MessageBubble} from './MessageBubble';
@@ -329,8 +329,8 @@ type RoundHostProps = {
 	toggleModel: () => void;
 		closeModel: () => void;
 		editingTextareaRef: MutableRefObject<HTMLTextAreaElement | null>;
-	editModelMenuRef: MutableRefObject<HTMLDivElement | null>;
-	anyStreaming: boolean;
+		editModelMenuRef: MutableRefObject<HTMLDivElement | null>;
+		anyStreaming: boolean;
 	stopGeneration: () => void;
 	resizeEditingTextarea: () => void;
 
@@ -436,7 +436,7 @@ const RoundHost = memo(function RoundHost({
 										}
 									}}
 									rows={1}
-									className="xy-chat-text block min-h-8 max-h-[15rem] w-full resize-none overflow-y-hidden border-0 bg-transparent p-0 font-sans text-[15px] leading-relaxed text-ink outline-none"
+									className="xy-chat-text xy-editing-input block min-h-8 max-h-[15rem] w-full resize-none overflow-y-hidden border-0 bg-transparent p-0 font-sans text-[15px] leading-relaxed text-ink outline-none"
 									disabled={editingSubmitting || anyStreaming}
 									aria-label="缂栬緫鍘嗗彶娑堟伅"
 								/>
@@ -470,13 +470,13 @@ className="relative hidden sm:block"
 											/>
 										</button>
 																	<div onClick={closeModel}>
-									<ModelPicker
-										open={modelOpen}
-										menuId={modelMenuId}
-										portal
-										anchorRef={editModelMenuRef}
-										disabled={anyStreaming || editingSubmitting}
-									/>
+										<ModelPicker
+											open={modelOpen}
+											menuId={modelMenuId}
+											portal
+											anchorRef={editModelMenuRef}
+											disabled={anyStreaming || editingSubmitting}
+										/>
 							</div>
 
 									</div>
@@ -555,7 +555,7 @@ export function MessageList({
 							const smoothness = useSettingsStore(s => isSmoothnessOn(s.smoothness));
 			const model = useSettingsStore(s => s.model);
 
-		const storeMessages = useChatStore(s => {
+		const storeMessages = useChatUiStore(s => {
 		const id = s.activeId;
 		if (!id) {
 			return EMPTY_MESSAGES;
@@ -563,16 +563,16 @@ export function MessageList({
 		return s.messagesById?.[id] ?? EMPTY_MESSAGES;
 	});
 	const messages = messagesProp ?? storeMessages;
-	const viewingStream = useChatStore(s =>
+	const viewingStream = useChatUiStore(s =>
 		Boolean(s.activeId && s.streamingSessionId && s.activeId === s.streamingSessionId),
 	);
 	const signalMode = streamSignalsEnabled && streamingTextProp === undefined;
-	const storeStream = useChatStore(s => (signalMode ? '' : s.streamingText));
-	const storeShown = useChatStore(s => (signalMode ? '' : s.streamingShown));
-	const storeLoading = useChatStore(s => s.isLoading);
-			const storeStatus = useChatStore(s => s.statusText);
-		const stopGeneration = useChatStore(s => s.stopGeneration);
-		const anyStreaming = useChatStore(s => s.isLoading && Boolean(s.streamingSessionId));
+	const storeStream = useChatUiStore(s => (signalMode ? '' : s.streamingText));
+	const storeShown = useChatUiStore(s => (signalMode ? '' : s.streamingShown));
+	const storeLoading = useChatUiStore(s => s.isLoading);
+			const storeStatus = useChatUiStore(s => s.statusText);
+		const stopGeneration = useChatUiStore(s => s.stopGeneration);
+		const anyStreaming = useChatUiStore(s => s.isLoading && Boolean(s.streamingSessionId));
 
 	const isLoading = isLoadingProp ?? (viewingStream && storeLoading);
 	const streamingSignal = signalMode && viewingStream && isLoading;
@@ -1065,7 +1065,7 @@ export function MessageList({
 			};
 	}, [hasRows, streaming]);
 
-			const activeId = useChatStore(s => s.activeId);
+			const activeId = useChatUiStore(s => s.activeId);
 			const sessionSwitchRef = useRef(activeId);
 			const sessionSwitching = sessionSwitchRef.current !== activeId;
 			useLayoutEffect(() => {
@@ -1086,7 +1086,7 @@ export function MessageList({
 				scrollerRef.current?.style.setProperty('--xy-top-fade-h', '0px');
 					scheduleTopFade();
 				}, [activeId, scheduleTopFade]);
-							const resendFromMessage = useChatStore(s => s.resendFromMessage);
+							const resendFromMessage = useChatUiStore(s => s.resendFromMessage);
 
 				const [editingMessageId, setEditingMessageId] = useState<string | null>(null);
 		const [editingText, setEditingText] = useState('');
@@ -1098,9 +1098,9 @@ export function MessageList({
 		const editingTextareaRef = useRef<HTMLTextAreaElement | null>(null);
 		const editModelMenuRef = useRef<HTMLDivElement | null>(null);
 
-	const emptyQuipSeq = useChatStore(s => s.emptyQuipSeq);
-	const activeSpaceId = useChatStore(s => s.activeSpaceId);
-	const spaces = useChatStore(s => s.spaces);
+	const emptyQuipSeq = useChatUiStore(s => s.emptyQuipSeq);
+	const activeSpaceId = useChatUiStore(s => s.activeSpaceId);
+	const spaces = useChatUiStore(s => s.spaces);
 	const workspaceName =
 		spaces.find(s => s.id === activeSpaceId)?.name?.trim() || 'this project';
 	const emptyQuip = useMemo(
@@ -1200,25 +1200,25 @@ const toggleModel = useCallback(() => {
 					if (!(target instanceof Node)) {
 						return;
 					}
-						const portalMenu = modelOpen
-							? document.getElementById(modelMenuId)
-							: null;
-						const insidePortalMenu = Boolean(portalMenu?.contains(target));
-						if (
-							modelOpen &&
-							!editModelMenuRef.current?.contains(target) &&
-							!insidePortalMenu
-						) {
-							setModelOpen(false);
-						}
-						if (!editRootRef.current?.contains(target) && !insidePortalMenu) {
-							cancelEdit();
-						}
+									const portalMenu = modelOpen
+										? document.getElementById(modelMenuId)
+										: null;
+									const insidePortalMenu = Boolean(portalMenu?.contains(target));
+									if (
+										modelOpen &&
+										!editModelMenuRef.current?.contains(target) &&
+										!insidePortalMenu
+									) {
+										setModelOpen(false);
+									}
+									if (!editRootRef.current?.contains(target) && !insidePortalMenu) {
+										cancelEdit();
+									}
 				};
 
 			document.addEventListener('pointerdown', onPointerDown);
 			return () => document.removeEventListener('pointerdown', onPointerDown);
-		}, [cancelEdit, editingMessageId, modelMenuId, modelOpen]);
+			}, [cancelEdit, editingMessageId, modelMenuId, modelOpen]);
 
 
 			const submitEdit = useCallback(async () => {
diff --git a/gui/src/index.css b/gui/src/index.css
index 505cb36..b0d7601 100644
--- a/gui/src/index.css
+++ b/gui/src/index.css
@@ -448,40 +448,45 @@ html[data-theme="dark"] .xy-code-fade {
   -webkit-text-fill-color: currentColor;
 }
 
-/* 编辑态必须遮住 transcript，尤其是 sticky 到顶部时不能透出下面的文字。 */
+/* 编辑态沿用普通气泡表面，sticky 仅改变编辑控件与边框状态。 */
 .xy-editing-bubble {
   z-index: 1;
   isolation: isolate;
   overflow: visible;
-  /* 用与普通气泡相同的纸面/墨色混合，既不透出下层文字，也不突然变成 composer 卡片。 */
-  background: color-mix(in srgb, var(--xy-paper) 96%, var(--xy-ink) 4%) !important;
-  border-color: var(--xy-user-bubble-border);
+  /* 编辑态沿用普通气泡的透明表面；唯一新增的视觉信号是强调色边框。 */
+  background: var(--xy-user-bubble) !important;
+  border-color: var(--xy-accent) !important;
   box-shadow: none;
   -webkit-backdrop-filter: none;
   backdrop-filter: none;
+  transform-origin: top center;
   transition:
     background-color 180ms var(--ease-out-soft),
     border-color 180ms var(--ease-out-soft),
     box-shadow 180ms var(--ease-out-soft),
     transform 180ms var(--ease-out-soft),
     opacity 180ms var(--ease-out-soft);
-  will-change: background-color, border-color, box-shadow, transform, opacity;
-  animation: xy-editing-bubble-in 180ms var(--ease-out-soft) both;
+  will-change: transform, opacity;
+  animation: xy-editing-bubble-in 190ms var(--ease-out-soft) both;
+}
+
+.xy-editing-input {
+  border-radius: 0.6rem;
+  box-shadow: inset 0 0 0 1px var(--xy-accent);
+  transition: box-shadow 160ms var(--ease-out-soft);
 }
 
 @keyframes xy-editing-bubble-in {
   from {
-    opacity: 0.72;
-    transform: translateY(2px) scale(0.995);
+    transform: translate3d(0, 2px, 0) scale(0.985);
   }
   to {
-    opacity: 1;
-    transform: translateY(0) scale(1);
+    transform: translate3d(0, 0, 0) scale(1);
   }
 }
 
 .xy-prompt-sticky .xy-editing-bubble {
-  background: color-mix(in srgb, var(--xy-paper) 96%, var(--xy-ink) 4%) !important;
+  background: var(--xy-user-bubble) !important;
 }
 
 .xy-prompt-sticky:has(.xy-editing-bubble) {
@@ -494,7 +499,8 @@ html[data-theme="dark"] .xy-user-prompt {
 
 @media (prefers-reduced-motion: reduce) {
   .xy-user-prompt,
-  .xy-editing-bubble {
+  .xy-editing-bubble,
+  .xy-editing-input {
     animation: none;
     transition: none;
     will-change: auto;
