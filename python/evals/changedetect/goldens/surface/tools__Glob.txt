{"description":"Find files by glob. Optional path root; paginate head_limit/offset. Prefer over Bash find; use Grep for contents.","input_schema":{"properties":{"detail":{"default":"auto","description":"auto (default): fold by directory when many hits; paths: flat list; folded: always group by parent dir.","enum":["auto","paths","folded"],"type":"string"},"head_limit":{"default":120,"description":"Max files to return. Default 120, hard cap 120 (larger values are clamped). If truncated, narrow the pattern/path; offset pages a specific slice.","maximum":120,"minimum":1,"type":"integer"},"offset":{"default":0,"description":"Skip this many matches before returning (default 0). Use after a truncated page.","minimum":0,"type":"integer"},"path":{"description":"Search root (any allowed directory; default cwd). Set when the subtree is known—does not lock you to src/. Do not pass \"undefined\" or \"null\".","type":"string"},"pattern":{"description":"Glob by name, e.g. \"*Map*.tsx\" or \"src/**/*Button*.ts\". Do NOT use `**/*` or directory-scoped `gui/**/*` to explore — any pattern without a file-name fragment returns a directory summary only. Always include a name/extension fragment + optional path.","type":"string"}},"required":["pattern"],"type":"object"},"name":"Glob"}
