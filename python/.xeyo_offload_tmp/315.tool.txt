The file D:\lea\XenYon code\gui\src\lib\formatUsage.ts has been updated successfully. +7 -7

```diff
--- a/formatUsage.ts
+++ b/formatUsage.ts
@@ -26,20 +26,20 @@
 	return `¥${Math.max(0, cny).toFixed(2)}`;
 }
 
-/** 顶栏用量芯片：累计消耗 / 模型窗口 · 上下文占用%（窗口未知则省略中段）。 */
+/** 顶栏用量芯片：累计消耗 / 输出token · 输出token占窗口%（窗口未知则省略中段）。 */
 export function formatUsageChipPreview(
 	consumedTokens: number | null | undefined,
-	contextLimit: number | null | undefined,
-	contextPercent?: string | null,
+	outputTokens: number | null | undefined,
+	outputPercent?: string | null,
 ): string {
 	const left =
 		consumedTokens != null && Number.isFinite(consumedTokens)
 			? formatTokenCount(consumedTokens)
 			: '—';
-	const hasWindow =
-		contextLimit != null && Number.isFinite(contextLimit) && contextLimit > 0;
-	const mid = hasWindow ? formatTokenCount(contextLimit) : null;
-	const pct = contextPercent == null ? '占用待确认' : `${contextPercent}%`;
+	const hasOut =
+		outputTokens != null && Number.isFinite(outputTokens);
+	const mid = hasOut ? formatTokenCount(outputTokens) : null;
+	const pct = outputPercent == null ? '占用待确认' : `${outputPercent}%`;
 	return mid ? `${left} / ${mid} · ${pct}` : `${left} · ${pct}`;
 }
 
```
Hint: Diagnostics path=D:\lea\XenYon code\gui\src\lib\formatUsage.ts