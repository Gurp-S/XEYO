The file D:\lea\XenYon code\gui\src\components\ChatHeader.tsx has been updated successfully. +4 -5

```diff
--- a/ChatHeader.tsx
+++ b/ChatHeader.tsx
@@ -376,11 +376,10 @@
 							{usage
 								? formatUsageChipPreview(
 										totalTokens,
-										// smoke-test #2：chip 也显示模型窗口（此前硬编码 null）。
-										usage.contextLimit,
-										contextPercent == null
-											? null
-											: contextPercent.toFixed(1),
+										totalOut,
+										totalOut != null && contextLimit != null && contextLimit > 0
+											? Math.min(100, (totalOut / contextLimit) * 100).toFixed(1)
+											: null,
 									)
 								: '用量 · 暂无数据'}
 												<ChevronDown className={cn('h-3 w-3 transition-transform', usagePreviewOpen && 'rotate-180')} strokeWidth={1.8} />
```
Hint: Diagnostics path=D:\lea\XenYon code\gui\src\components\ChatHeader.tsx