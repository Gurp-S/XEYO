1684:/* ── 新功能面板：请求按小时分布（SVG 迷你柱）── */
1687:  var hours=new Array(24).fill(0);
1688:  byTurn.forEach(function(t){var h=hourOf(t.first_ts);if(h>=0&&h<24)hours[h]++});
1689:  var maxH=Math.max.apply(null,hours)||1;
1700:    var x=pad+i*bw,hh=(H-24)*hours[i]/maxH;
1701:    if(hours[i]>peakH){peakH=hours[i];peak=i}
1705:    r.setAttribute('data-hour',i);r.setAttribute('data-count',hours[i]);
1909:  // ── 下方功能面板：请求小时分布 + 分模型对比 ──
1912:  // 面板1：请求按小时分布
1914:  var p1h=mk('header');p1h.innerHTML='<h2>请求 · 小时分布</h2><div class="sub">REQUESTS BY HOUR</div>';