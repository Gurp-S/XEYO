The file python/engine/query_engine.py has been updated successfully. +1 -0

```diff
--- a/query_engine.py
+++ b/query_engine.py
@@ -1218,6 +1218,7 @@
             base_url=url,
             model=resolved_model_name,
             provider=backend if backend in PROVIDER_PRESETS else "openai",
+            session_id=session_id or "",
         )
         resolved_provider = backend if backend in PROVIDER_PRESETS else "openai"
     elif backend == "deepseek":
```
Hint: Diagnostics path=python/engine/query_engine.py