# GUI 动画卡顿排查报告(只查不改)

日期:2026-09-05 · 范围:`gui/src` 全部动画入口(CSS keyframes/transition、RAF、setInterval/setTimeout 泵、backdrop-filter)

---

## 结论概览(按嫌疑度排序)

| # | 嫌疑 | 位置 | 类型 | 症状归属 |
|---|------|------|------|----------|
| 1 | 流式打字机每帧全列表重渲(信号模式默认关闭) | `stores/chat/streamSendSlice.ts` + `hooks/useMessageListStore.ts` + `lib/streamSignal.ts:11` | 架构 | 流式输出时持续掉帧 |
| 2 | ThoughtTicker 泵:innerHTML 全量重建 + 强制同步 layout | `components/activity/MorphVerb.tsx:205-254` | JS 泵 | thinking 期间卡顿 |
| 3 | 布局属性过渡(width/flex-basis/grid-template-rows) | `styles/app-chrome-shell.css:237-252,290`、`workflow-agent.css:254`、`dock-panels.css:120` | CSS reflow | 面板开合瞬间卡 |
| 4 | 持续重绘型无限动画(shimmer / box-shadow / SVG) | `sidebar.css:225-278`、`agent-map.css` | CSS repaint | 运行全程 GPU/重绘负担 |
| 5 | 大面积 backdrop-filter 叠加 | `overlays.css`、`immersive.css:20`、`app-turn-rail.css:139`、`ux-toast.css` | GPU 常驻 | 放大上面所有问题 |
| 6 | 永续 RAF 循环(休眠地雷) | `hooks/useStreamTypewriter.ts:239-291` | JS 泵 | 目前未触发,复用即炸 |

---

## 详述

### 1. 流式打字机:每帧 zustand set → MessageList 每帧整体重渲 ⚠️ 最高嫌疑

- 直播帧路径:`flushFrame`(`streamSendSlice.ts:~700-840`)按 RAF 调度,每帧 `advanceTypewriterShown` 推进 1-6 码点,然后 `set()` 更新 `sessionStreams.streamingShown`;drain 阶段(`streamDrain.ts:87-121`)同样每帧 set,直到全文排空。
- 消费侧:`useMessageListStore.ts:71-99` 用 `useShallow` 订阅 `stream.streamingShown`——**每帧都是新字符串 → MessageList 每帧重渲**。历史消息靠 `roundStreamHostProps` 的 memo 躲过,但列表本体、选中态、滚动锚定逻辑每帧都跑。
- **信号旁路存在但默认关**:`streamSignalsEnabled = import.meta.env.VITE_XY_STREAM_SIGNALS === '1'`(`lib/streamSignal.ts:11`)。gui/ 下**没有任何 .env 文件**,该开关在生产构建里恒为 false → 每帧 store 路径是当前唯一实况。开了信号模式后只有 `AssistantTurn.tsx:141` 一个叶子经 signal 更新。
- 附带伤害:每个 flushFrame 还执行 `persistence.hot(msgs)`;`usageAccumulator.ts:118` 的 RAF flush 每帧 `void saveSession(...)`——流式期间高频后台持久化写。

### 2. ThoughtTicker(JS 泵 + 布局抖动)

`MorphVerb.tsx:205-254`(`pump`):setTimeout 泵,**16-38ms 一拍(≈26-60Hz)**,每拍:
1. `renderTrack()` 全量重建 `track.innerHTML`(DOM 节点每拍销毁重建);
2. 紧接着 `pinEnd()` 读 `clientWidth`/`scrollWidth` → **强制同步 layout**,再写 `transform`。

write→read→write 的经典布局抖动。思考期间持续运转;若子代理栏多个 ticker 并存则叠加多份。注释里明确写了目的是"避免 30-60Hz 的 React 重渲",但把成本换成了每拍强制 reflow + innerHTML。

### 3. 布局属性过渡(展开/收起时每帧全树 reflow)

- `.xy-pane-slot` / `.xy-pane-slide`:`width` / `flex-basis` 420ms 过渡(`app-chrome-shell.css:237-252`)——面板开合的 420ms 内每帧重排整块内容;`will-change: width`(`:245`)对非合成属性无效。
- `.xy-workflow-fold`:`grid-template-rows` 0.4s(`workflow-agent.css:254`);`dock-panels.css:120` 同款。
- `.xy-header-tools`:`grid-template-columns` 300ms(`app-chrome-shell.css:290`)。
- 排除:`ChatPage.tsx:192-231` 三块全屏 pane 用的是 `opacity/transform`(合成属性),无问题。

### 4. 持续重绘型无限 CSS 动画

- `.xy-thinking` / `.status-shimmer`(`sidebar.css:225-256`):`background-clip: text` + `background-position` 3.2s infinite——**文本逐帧重新栅格化**;流式期间每条 thinking 行都在跑。
- `.xy-tool-live-dot`(`sidebar.css:263-278`):`box-shadow` 动画 xy-tool-live-pulse 1.6s infinite——box-shadow 每帧重绘,live 工具行越多越重。
- `agent-map.css`:`xy-map-flow`(stroke-dashoffset 0.9s infinite)、`xy-map-pulse`(opacity + **stroke-width** 1.8s infinite)——SVG paint 属性动画,running 节点/热边越多重绘面积越大;replay 播放时 `trailAnimate=true`(`AgentMapPanel.tsx:710`)所有 trail 一起流动。
- 排除:`xy-run-breath`/`xy-run-halo`、pet 呼吸、usage-skeleton-shimmer 均为 transform/opacity,合成器友好。

### 5. 大面积 backdrop-filter(GPU 常驻成本)

`overlays.css:8-9` 与 `ux-toast.css`:`blur(32px) saturate(1.45)`;`immersive.css:20`:`blur(18px)`(全高侧板);`app-turn-rail.css:139`:`blur(10px)`(全高轨道);CommandPalette / ImageReader / SettingsModal 全屏 `blur(3px)`;`AppShell.tsx:208` 壁纸动态 `blur(Npx)`。本身不动画,但它们覆盖区域之后的任何滚动/动画都要付出额外的合成成本,是"卡顿放大器"。

### 6. `useStreamTypewriter` hook:永续 RAF(休眠地雷)

`useStreamTypewriter.ts:239-291`:effect 挂载即启动 RAF 循环,`tick` 末尾**无条件** `requestAnimationFrame(tick)`——backlog 为 0、画面静止时也每帧空转(每帧做 `startsWith` / 必要时 `Array.from`)。当前 grep 确认生产组件未调用此 hook(store 只引纯函数、测试均 mock),**暂不影响线上**,但谁复用它谁就得到每气泡 60fps 空转。

### 7. 低频心跳(轻微,列档备查)

1s 级 `setInterval` + setState:`ImmersiveClock.tsx:10`、`SessionJobsBadge.tsx:77,130`、`ActivityLog.tsx:557`、`PermissionDialog.tsx:75`、`SubAgentView.tsx:109`;`FilePreview.tsx:425` agentBusy 时 700ms 轮询 checkStat。每秒一次小重渲,量级小,不构成主因。

---

## 现成的对照实验手段(不动代码可先验证)

设置里有全局"平滑"开关(`data-smoothness` / settingsStore.smoothness),CSS 侧动画(嫌疑 3/4/地图)都有 `animation: none` 回退。建议:
1. 流式输出时卡 → 关 smoothness 对比:仍卡 = 嫌疑 1(JS/架构层,不受该开关控制);变流畅 = 嫌疑 3/4。
2. thinking 行滚动时卡 → 观察 agent-bar ticker(嫌疑 2),悬停它减速时帧率反而回升即是实证。
3. 开合侧栏/面板瞬间掉帧 → 嫌疑 3。

## 修复方向备忘(本次未动)

- 嫌疑 1:打开 `VITE_XY_STREAM_SIGNALS=1` 验证信号模式收益,达标后按 AGENTS.md 准入流程并入主链路。
- 嫌疑 2:renderTrack 改为只更新 hot 块/光标两个节点;pinEnd 的读布局与写 transform 分帧。
- 嫌疑 3:width/grid 过渡改 transform/clip-path 或 FLIP。
- 嫌疑 4:box-shadow 动画改伪元素 opacity;shimmer 限流或降面积;地图 stroke-width 不进 keyframes。

---

## 追加排查:点击切换对话大延迟(2026-09-05 下午)

数据层无问题:`selectSession`(`spaceSessionSlice.ts:617-721`)是乐观切换,同步落 activeId 不阻塞;冷会话走「IDB 先行 → 服务端回填」+ 骨架屏;消息无淘汰(常驻内存,无 evict 逻辑)。**延迟全部在渲染层,根因链如下:**

### S1(主因)≤150 轮的会话全量挂载,切换 = 一次性同步渲染整个会话

- `VirtualRoundList.tsx:38` `VIRTUAL_WINDOW_THRESHOLD_ROUNDS = 150`;`:105` `allMount = !smoothness || !ioAvailable || count <= 150`。
- 轮数 ≤150 就不做窗口化——150 轮 ≫ 绝大多数真实会话规模,等于**平时所有会话都是全挂载**:每次切换把全部轮次(PromptBubble sticky 包装 + AssistantTurn 的 Streamdown/Shiki markdown 全量解析 + done-on chrome + ActivityLog merge)在**一个同步 React commit**里挂出来,随后浏览器还要对整棵新 DOM 做完整 style+layout 才能首绘。延迟 ≈ O(全会话消息量),与视口大小无关。
- 窗口化路径(>150 轮)本身是健康的:视口 ± overscan ∪ 最新 2 轮,其余 spacer 占位(`RoundMount` IntersectionObserver + 高度缓存)。

### S2 关掉"平滑"反而更卡(负面交互)

`!smoothness` 也强制 allMount——用户为减卡顿关掉平滑后,**任意长度会话**的切换都全量挂载。与嫌疑 3/4 的"关平滑减负"形成反直觉对冲。

### S3 跨会话零缓存:key={round.id} 全变,往返切换每次全价

RoundHost 按 `round.id` keyed(`MessageList.tsx:555`)——切会话时旧轮全卸、新轮全新挂载,markdown 解析、mergeTurnActivity/finalRoundProseMessageIds(`RoundHost.tsx:247-265`)等 O(全量) useMemo 全部重跑。来回切同一对会话没有第二次便宜。

### S4 落地期多次强制布局

`useMessageListViewport.ts:441-521` 切换帧内:保存旧滚动点 → `applyLanding` 写 scrollTop(强制 layout)→ flushStuck/syncTopFade → rAF 补吸顶;windowed 模式 `VirtualRoundList.tsx:177-189` 还要同步读 scrollHeight 取窗。逻辑本身正确(为防首帧闪跳),但叠加在 S1 的巨型首帧上被放大。

### S5 轻微:每轮一个 sticky 层

每轮 `xy-prompt-sticky` 带 `zIndex: 40 + roundIndex`(`RoundHost.tsx:349-355`)——大会话几十个独立 sticky 层/堆叠上下文,布局与合成成本随轮数线性涨。

### 验证手段(不动代码)

1. 长会话 vs 短会话切换延迟对比:应随轮数近似线性增长 → 证实 S1。
2. 同一对会话来回反复切:每次都同样卡 → 证实 S3(无缓存)。
3. bench harness 已有会话切换预算(`benchHarness.ts` warm <50ms,BASELINE.md:68),可直接复跑取证。
4. 关/开 smoothness 对比切换延迟:关了更卡 → 证实 S2。

### 修复方向备忘(未动)

- S1:阈值按消息量/字符量计并大幅调低,让真实会话走窗口化;或 settled 历史轮先挂轻量占位(高度缓存已有,成本可控)。
- S2:allMount 判据剔除 `!smoothness`(窗口化才是不卡的正解,不该被平滑开关劫持)。
- S3:RoundMount 高度缓存已有;可给 markdown 结果加跨会话 LRU(按 roundId/文本 hash)。
- S4:S1 解决后自然收敛。
