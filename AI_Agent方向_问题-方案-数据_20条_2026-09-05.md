# AI Agent / LLM 应用方向 ·「问题 → 方案 → 数据亮点」20 条（2026-09-05）

> 用法：每条 = 一个真实工程问题 + 解法 + 可复现数据。可直接当简历 bullet（看「简历写法」行），
> 也可当面试的 20 个预埋钩子（面试官顺任意一条问，你都有三层答案）。
> 数据全部出自 XEYO 实测（口径见括号内脚本/账本），无编造。

---

### 01｜长会话把 KV 缓存全打废
- **痛点**：请求前缀每轮变一字节，缓存全 miss；多轮对话输入费线性爆炸。
- **方案**：稳定前缀三保险——system 段 memo 化+锁段序、投影缓存签名（消息数/冻结点/摘要指纹）、易变内容全部尾插。
- **数据亮点**：生产 **4198 次请求实测 KV 命中率 median 93.9%**（p75 97.2%），累计命中 **1.02 亿 tokens**，输入费用省 **75-84%** [实测]
- **简历写法**：设计稳定前缀上下文架构，生产实测 KV 缓存命中率 93.9%，输入成本降 75%+

### 02｜长会话撑爆上下文窗口
- **痛点**：200 轮会话 prompt 涨到 175k，窗口溢出即报错。
- **方案**：C0 截断/C1 冻结/C2 摘要三级压缩 + 压力门（窗口自适应）与收益门（重算成本被摊平才压）双门裁决。
- **数据亮点**：605 调用重放四配置对照——压缩后输入 **-84%**（4.47M→712k tok）、成本 **-31%**（干净口径）、答对率 **+4.2pp** 不降反升 [实测]
- **简历写法**：三级上下文压缩使长会话输入降 84%、成本降 31%，答对率不降反升

### 03｜压缩 = 失忆（丢报错栈/丢关键路径）
- **痛点**：摘要折叠把刚出现的报错栈压没了，模型下一轮开始瞎猜。
- **方案**：三配套——citation 锚点（⟦notes:msg:i⟧ 溯源）+ A2 碎片还原（压缩时抓拍原子入 sqlite，按锚点找回原文）+ A3 逃生舱（最新报错栈+文件路径强制原文保留）。
- **数据亮点**：报错栈保真 **105→105 字符零损失**；极小预算 400 字符下栈仍完整；旧栈折叠但 retrieve 字节级找回 [实测]
- **简历写法**：设计"可还原压缩"：引用锚+碎片抓拍+逃生舱，压缩后关键故障信息零丢失

### 04｜LLM 做摘要不可复现
- **痛点**：用 LLM 生成压缩摘要，同配置三次跑出 62/92/83%——质量不可控。
- **方案**：选型实验（确定性 vs LLM vs LLM+分段 vs 语料膨胀），确定性摘要（角色份额 0.78/0.25）为生产，LLM 旁路保留为默认关的实验开关。
- **数据亮点**：确定性 **83.3%（Δ+4.2pp）零成本** vs LLM 三次 62/92/83%（不可复现）vs LLM+膨胀 **46%（负优化）** [实测]
- **简历写法**：用数据否决 LLM 摘要方案，确定性摘要以零成本拿到 +4.2pp 质量提升

### 05｜工具输出把上下文灌爆
- **痛点**：一次 pytest/git log 返回上千行，几轮就把窗口吃满。
- **方案**：输出精简管线（Bash 家族语义压缩：错误块保真+成功折叠）+ registry 级 16k 预算闸。
- **数据亮点**：pytest 输出 **1126→78 tok（省 93.1%）**、tsc 省 90.1%、git log 省 79.9%；注入开销仅 75-86 tok [实测]
- **简历写法**：语义化工具输出精简，pytest 类输出 token 省 93%，注入开销 <90 tok

### 06｜大文件读取消耗巨大
- **痛点**：模型想看一个函数，却把 411 行文件全读进上下文（20k tok）。
- **方案**：codeindex 符号索引（ast/tree-sitter，内容哈希缓存）+ Read 的 symbol 定位读取 + 上下文 pack 打包。
- **数据亮点**：symbol 读 vs 全量 **20615→175 tok（省 99.2%）**、1.8ms vs 32.3ms；重名符号自动给候选清单 [实测]
- **简历写法**：基于符号索引的精准读取，大文件上下文消耗降 99%

### 07｜易变状态污染对话前缀
- **痛点**：任务状态/提醒/开关每轮都变，写哪都破坏 KV 缓存或混淆说话人。
- **方案**：T_now 登记表（硬顶 22 块、契约测试执法）+ "环境声道"注入——易变块装入仅存在于投影的伪造 tool 对（xeyo_env_notice），与用户意图消息结构上物理隔离。
- **数据亮点**：结构隔离根治说话人混淆；工具名不进 tools 数组；厂商拒绝时 4xx 自动回退降档 [设计+实测]
- **简历写法**：发明环境声道注入法：伪造 tool 对承载易变上下文，结构级根治说话人混淆且前缀零损伤

### 08｜提示注入劫持工具调用
- **痛点**：工具输出/网页内容里的恶意指令诱导模型越权。
- **方案**：γ4 围栏（所有工具输出包 untrusted 标注）+ 授权指纹只含工具身份不含参数（注入免疫）+ 文件系统内容级硬 DENY 三层。
- **数据亮点**：注入实例推演——指纹防"身份伪造"+filesystem 防"内容越界"，`.env` 类读取被内容层拒绝 [设计]
- **简历写法**：三层注入防护（围栏/身份指纹/内容 DENY），恶意文档无法伪造已授权工具或越权读密钥

### 09｜授权被参数注入放大
- **痛点**：传统"命令前缀授权"下，参数变化等于换了命令。
- **方案**：授权指纹 v2 = sha256(工具身份)，**参数不进指纹**；v1/v2 版本隔离防旧授权复用；撤销即 DENY。
- **数据亮点**：契约测试六项（伪装字段免疫/回环/DENY 压过 grant）；发现并记录 Bash 前缀放大缺口并列入 P0 [实测+审计]
- **简历写法**：设计参数免疫的授权指纹协议，并用契约测试锁死"DENY 不可被授权穿越"等单向性

### 10｜自动推进的 Agent 失控跑飞
- **痛点**：Goal 自动轮 + 后台任务一旦循环自触发就停不下来。
- **方案**：三重护栏——32 轮硬顶 × 连续唤醒预算 3 次（与 jobs 共享）× 仅人工恢复；armed 状态不持久化（重启不复活）。
- **数据亮点**：护栏全机器执法；合成轮复用整条生产管线而非旁路脚本 [设计]
- **简历写法**：自动推进 Agent 的三重失控护栏（轮次/唤醒预算/人工恢复），armed 状态重启不复活

### 11｜进程崩溃后任务现场全丢
- **痛点**：死在两个工具调用之间，未配对 tool_use、半截消息、任务状态未知。
- **方案**：断点续跑三件套——未配对修复 + 中断锚 + hydrate fail-closed 合成（写工具中断标"结果未知"而非假装成功）。
- **数据亮点**：恢复全程无需人工修数据；SSE 断连转 detached 续跑 + cursor 补帧 [设计]
- **简历写法**：崩溃恢复三件套使长任务断点续跑零人工干预，中断工具结果 fail-closed 标注

### 12｜回滚误伤用户手动改动
- **痛点**：Agent 回滚文件会把用户后来的修改一起冲掉。
- **方案**：内容寻址快照（SHA-256）+ 恢复前工作区指纹预检 + scoped restore——漂移即拒绝并报错，绝不静默覆盖。
- **数据亮点**：预检+条件恢复+双向日志（审计/rewind_events/WAL）可对账；diff 佐证断言"佐证只加分不降权" [设计]
- **简历写法**：设计 scoped 回滚：指纹预检防覆盖用户改动，双向日志全链可对账

### 13｜优化全靠感觉上线
- **痛点**：Agent 的"改进"大多凭体感，负优化常被当宝。
- **方案**：数据准入流程——旁路灰度（feature flag）→ 冻结真录离线重放（200 轮/605 调用）→ 证据门裁决（收益/成本/回归三门槛）。
- **数据亮点**：实测否决 3 项候选——Pareto 加权（质量 +6pp 但成本 **1.51x**）、驻留分（成本 1.20x）、动态 R（零收益）[实测]
- **简历写法**：建立证据门准入机制，实测否决 3 项负收益候选优化（成本超限 51%-20%）

### 14｜LLM 应用成本是黑盒
- **痛点**：不知道钱花在哪、值不值。
- **方案**：三本账物理分离（计费/C2 压缩事件/校准事件）+ 每请求级核算（model/session/tokens/cost）+ 成本双源（官方金额优先/峰谷价目兜底）。
- **数据亮点**：**5094 请求全量账本**、41.5 元可逐请求归因；命中率与成本联动分析（transition 谷底单枪成本 5-10 倍）[实测]
- **简历写法**：每请求级成本核算体系，5094 请求全量归因，压缩收益用"干净口径"成本法验证

### 15｜跨会话失忆，经验不复用
- **痛点**：每个新会话从零开始，项目惯例反复交代。
- **方案**：六层记忆（会话任务态→跨会话检索→长期库→任务归档）+ NightShift 夜间蒸馏（diff 佐证/墓碑/冲突 supersede）。
- **数据亮点**：sqlite 索引热路径墙钟 **-15.2%** 命中逐位一致；差分重写稳定型省 20%；晋升全走守门（置信度封顶 0.85/1.0）[实测]
- **简历写法**：六层记忆体系：候选-守门-晋升三级流水线，跨会话经验自动沉淀且可审计回滚

### 16｜多工具并发写坏文件
- **痛点**：并发写/写旧版本互相覆盖。
- **方案**：分区执行（读类并发、写类串行）+ WriteStore 乐观锁（base_hashes 内容哈希，拒盲写/拒 stale）+ 过宽写范围自动降只读。
- **数据亮点**：stale/missing_read 双门 + Windows CRLF/UTF-16 归一化哈希防假冲突；跨会话冲突给"行区间+归属归因" [设计]
- **简历写法**：内容哈希乐观锁+分区执行，多 Agent 并发写零覆盖，冲突归因到会话级行区间

### 17｜模型反复用错工具（cat 查文件、grep 当读）
- **痛点**：重复低效调用，token 与轮次双浪费。
- **方案**：透明路由（只读命令自动改道专用工具）+ 渐进强制 + 纠偏统计。
- **数据亮点**：416 次真实调用审计，误用率 3.85%、**纠偏有效率 68.8%**；配合 Read pack 调用 **3→1 次** [实测]
- **简历写法**：Bash 透明路由自动纠偏（有效率 68.8%），配合 pack 读取使工具调用次数降 67%

### 18｜死循环烧钱
- **痛点**：模型原地打转、廉价工具无限循环。
- **方案**：BudgetTracker（256 轮/64 工具调用/USD 三维 + grace 防误触）+ RepeatCallGuard/ZeroHitTracker + 唤醒预算共享。
- **数据亮点**：超限急停有单测覆盖；grace 窗口防"单轮爆发误触收尾"（已知取舍：只提醒不拦截，硬上限兜底）[实测+设计]
- **简历写法**：三维预算护栏+重复检测防死循环，grace 窗口设计消除单轮爆发的误停机

### 19｜评测自己骗自己
- **痛点**：Agent 评测常见训练数据污染/判分泄露/口径漂移。
- **方案**：4 个反作弊 shadow 模块（盲审 leakage/污染门在入口拒病态环境/严格环境档/四字段报告口径强制）+ 判分测试后置（print-expected 作弊结构上不可行）。
- **数据亮点**：评测设施评级 A-；已知短板也诚实登记（retrieval 自证、反作弊未常态接线）[实测]
- **简历写法**：自建反作弊评测基建（盲审/污染门/严格档），判分结构性防泄露，并公开登记自身短板

### 20｜不知道压缩后模型有没有"变笨"
- **痛点**：命中率≠智能，压缩可能悄悄降低答对率。
- **方案**：命中率与正确率**独立双指标**评测——同轮 A/B（表A-real 24 题×3 次）+ LoCoMo-Refined 定位能力边界。
- **数据亮点**：表A **v61 83.3% vs project 79.2%（+4.2pp）**，v61 无独有零分题；同时定位出"时间锚点丢失"缺口（when 类题 F1 0.011）并给出修复方向 [实测]
- **简历写法**：双指标证明"压缩省 84% 输入但答对率 +4.2pp"，并用 LoCoMo 精确定位剩余能力边界

---

## 附：组合建议

- **简历一句话 bullet** = 「01 或 02 的数据」+「07 或 13 的机制名词」：例如
  *"设计环境声道注入与三级压缩管线，长会话输入降 84%、生产 KV 命中率 93.9%，并以证据门机制否决 3 项负收益优化"*
- **面试深挖链**（每条追问三层都有答案）：
  - 01→怎么保证字节稳定（投影签名）→签名有哪些字段→压缩时怎么办（跳变合法）
  - 02→怎么决定压不压（双门）→摘要怎么生成（确定性选型 04）→怎么验证不变笨（20）
  - 13→门长什么样（三门槛）→被否决的是啥（B1/B2/B3 数据）→否决了怎么办（开关关死，面板标注）
- **诚实边界**：Live 深度评测（LoCoMo 全量/多模型对照）受 API 欠费受限；aging 收益基准因开关漂移待修——被问到如实讲，反而是加分的素材。
